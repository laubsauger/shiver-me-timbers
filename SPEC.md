# SPEC

## §G GOAL
Web pirate combat demo (SoT-class water/sky/ropes/destruction) @ WebGPU three.js TSL. Company showcase — visual bar high.
+ **RAFT 2100** (2026-08-21): 2nd mode, same repo/engine. Ambient first-person exploration, ⊥ combat. Kon-Tiki balsa raft (1947 bones, 2100 dressing) in flooded Sierra-like archipelago; diegetic radio → bearing → island → on-foot clues → Alan Watts teaching (placeholder audio, swappable). Design doc: artifact 9c312a95 (scratchpad `raft2100.tpl.html`); ref `docs/raft2100/kon-tiki-reference.md`. Pirate mode untouched.

## §C CONSTRAINTS
- three.js `WebGPURenderer` + TSL only. WebGL path ⊥. No fallback code.
- Shaders in TSL nodes; raw WGSL only where TSL can't (FFT butterfly OK).
- Desktop Chrome/Edge/Safari-26. 60fps @ 1440p, Apple Silicon / discrete GPU ref.
- TypeScript + Vite. No game engine libs. Physics hand-rolled (buoyancy, projectile) — no ammo/rapier unless task says.
- Modular files: 1 system = 1 dir, file ≤ ~250 lines soft cap. Shader chunks own files.
- All tunables ∈ `params/*.ts` modules → Tweakpane debug panel. Magic constants in shaders ⊥.
- Sim deterministic & serializable (multiplayer later): fixed timestep, seeded RNG, sim state plain-data store.
- Ships procedural, destruction-first (modular pieces). Later AI-gen mesh swap ! keep same socket/piece contract.
- Heavy sims = compute passes (`Fn().compute()`), storage buffers/textures. Frag-shader ping-pong GPGPU ⊥.
- Ref doc: `docs/handoff.md` + PNGs. YouTube talk y9BOz2dFZzs = ground truth for look.

## §I INTERFACES
- app: `npm run dev` → Vite @ localhost, single page canvas.
- gate: no WebGPU → static "WebGPU required" page, zero crash.
- input: WASD/mouse sail+steer, camera orbit+follow, Space fire cannon, Tab debug panel.
- input/freecam: `C` toggles free↔follow. Free: WASD fly view-axes, R/F rise/descend, Shift fast ×12, Ctrl slow ×0.1, mouse look, wheel = travel speed. Free swallows WASD/R/F in capture phase (⊥ leak to rudder); keyup never swallowed; Q/E sail trim still live. Free ⊥ ship-relative → no snap-back by construction; dives to `freeMinY` −300m.
- debug: Tweakpane — ∀ params/* groups, weather presets (calm|swell|storm), perf HUD (fps, ms per pass).
- file: `src/params/*.ts` — one module per system, typed, hot-reload.
- state: `SimState` plain JSON-serializable object — ships, wind, time, seed, projectiles, damage.
- assets: `assets/` textures (foam crest/soft, flowmap noise, cloud distort cubemap, wood PBR). Procedural where possible.
- src layout: `src/core|ocean|foam|deckwater|flowfoam|clouds|ropes|ship|sea-physics|combat|ai|sky|params|debug|state|ui`.
- ui: DOM overlay (no canvas UI) — HUD, pause menu (Esc), settings. Settings persist localStorage.
- ui/settings/graphics: res scale, quality preset, AND per-feature toggles — reflections, caustics, deck water, spray, shadows(+map size), post FX, rain, island/palm density. Every expensive system ! individually switchable from the menu, not only Tweakpane.
- ui/settings/audio: master / music / sfx / ambience, each independently. Missing entirely today.
- ui/cinematic: fullscreen defaults to CINEMATIC — every debug overlay hidden (Tweakpane, perf HUD, dev readouts). `F1` toggles the dev layer back on/off. Photo mode hides the game HUD too, leaving only the rendered frame. Purpose: capture/recording (§T.39).
- ui/hud: apparent-wind indicator + sail-trim/reef readout (pairs w/ the flag telltales — see the wind, then answer it).
- audio/music: music bus + playlist engine — multiple tracks, shuffle/sequential, crossfade, per-track gain, ducking under combat. Tracks drop into `assets/audio/music/`.
- raft/app: `raft.html` + `src/main-raft.ts` = 2nd Vite entry (`rollupOptions.input`). Shares modules, NEVER imports `src/main.ts` nor pirate-only singletons (`setActiveCaustics`/`setActiveDeckWater` only if raft boot owns them). `npm run dev` serves both.
- raft/input: pointer-lock FP. WASD walk (ship-frame), mouse look, `E` interact (grab tiller/guara/sheet/radio/ladder/mat), `Ctrl` crouch (auto in cabin), `Space` jump-ashore/swim-up. DEBUG only (dev layer on): `A/D` oar, `Q/E` guara fwd/aft, `W/S` sheet, `Y` tune ± (T = FP toggle), `N` skip-to-dawn, `J` jump-to-island. Debug keys ⊥ required for play; every play action reachable by walking.
- raft/radio: `src/radio/` — stations data (freqMHz, worldPos, fragmentPool[], nightPool[]); `tuner.ts` pure fn (freq, heading, dist, isNight) → {clarity, strength}; lock → horizon sprite + chart overlay marker.
- raft/teachings: `src/teachings/manifest.ts` — `Teaching{id, islandSeed, station, clues[], night[]}`, `Clue = audio(prop, at, clip, radius) | object(prop, at, text)`. Audio = `assets/audio/teachings/<teaching>/<clue>.mp3`, build-time glob like music, credited `CREDITS.md`. Placeholder MP3s until Foundation clips chosen.
- raft/ship: 3rd blueprint `buildRaftBlueprint(raftParams)` in `src/ship/` — same `PieceDef[]` contract (§V13/§V18), new `PieceKind`s (`log|crossbeam|bamboo-deck|guara|cabin-wall|thatch-roof|bipod-mast|steering-oar|crate|splashboard|stern-block`). Selectable in pirate sim too (player ship) via `?ship=raft` + `preview.html?ship=raft`.
- raft/preview: `src/ship/preview.html?ship=raft&view=...&station=tiller|radio|bow|nest|cabin` = ship-only lookdev. Review gate for R1.
- src layout +: `src/player|radio|teachings`, `src/ship/raft*`, `src/sailing/raft*`, `src/island/sierra*`, `src/core/grade*`, `src/params/raft.ts|radio.ts|player.ts|grade.ts`.
- docs: `docs/raft2100/` — reference + (later) lookdev PNGs per phase.

## §V INVARIANTS
V1: WebGPU absent → gate page, console clean, no exception.
V2: sim tick fixed 60Hz, decoupled from rAF. Same seed+input log → identical `SimState` hash.
V3: render reads `SimState`, ⊥ writes it. One-way data flow sim→render.
V4: ocean = Tessendorf FFT spectrum → compute IFFT → displacement+choppy StorageTexture. Geo high-freq kept; style from shading only.
V5: SSS fake: choppiness mask × dot(L,V) boost on wave sides. No physical SSS.
V6: foam: Jacobian det < bias → inject foam RT; RT progressive-blurred ∀ frame; blurred mask mixes crest-tex → soft-tex.
V7: storm preset → amp↑ & Jacobian bias↓ → big foam patches. Weather presets only touch params, no code path change.
V8: buoyancy samples same FFT displacement data GPU wrote (readback or CPU mirror of spectrum) — ship float matches visible waves, drift ⊥.
V9: deck water: Mei 2-pass (outflow biased by ship rotation → inflow+resolve), B=volume G=wetness, evap const/frame → deck dries. Deck material roughness↓ where wet.
V10: intersection foam: depth-compare in water TSL material → mask RT → advect by flowmap + blur, accumulate prev frame. Works ∀ hull waterline + rocks.
V11b: cloud cores = POLYGONAL MESHES (talk "Clouds: Iteration", ref Paths of Hate), billboards only for 'fluff' on top. Pure-billboard puffs ⊥ — they cannot hold a sculpted silhouette. Storm clouds ! read as monuments: anvil/mushroom forms w/ internal structure, strong colour grade, sitting ON the horizon (talk "Clouds: Concept"). Sky behind = SATURATED cyan-blue, strong vertical gradient — pale/washed ⊥.
V11: clouds: fwd-render cores → RT (sunlight R, skylight G, alpha B, depth A per clouds-channels.png) → depth-scaled blur compute → camera-pinned quad + distortion cubemap. Raymarch ⊥.
V12: ropes: 1 compute thread = 1 rope, verts → StorageBuffer → instanced/batched mesh. CPU per-frame rope math ⊥. Near ropes ! DYNAMIC (V42); static catenary is the far/fallback regime only.
V42: near-field ropes = real per-particle sim (Verlet/PBD chain per rope, 1 thread = 1 rope looping its own particles), driven by gravity + ship acceleration + apparent wind, w/ distance constraints & anchor pinning. Canned sin() sway ⊥ as the only motion. ! carry secondary motion: lag under pitch/roll, gust whip, slack when anchors close. Degrade to the static catenary by distance, crossfaded (V41 regimes).
V13: ship = piece graph (hull segments, planks, masts, yards, rails; optional parent links → detachable subtrees) + named sockets (rope anchors, cannon mounts, damage zones, fixtures). Destruction ops only via piece graph ops.
V14: cannonball hit → damage zone swap intact→holed piece + splinter burst; hole below waterline → flood rate↑; flood ≥ cap → sink sequence. Mast hit ≥ threshold → mast piece detaches, ropes re-solve (V12 handles).
V15: enemy AI: state machine (patrol→engage→broadside→flee) reads/writes SimState only, tick-rate = sim tick.
V16: ∀ tunable ∈ params module & appears in Tweakpane. Shader literal consts ⊥ (except math consts π etc).
V17: frame budget @ ref GPU: compute total ≤ 4ms, render ≤ 8ms. Perf HUD shows per-pass ms; regression > 20% = bug.
V18: piece meshes swappable: piece contract = {id, socket transforms, damage states, AABB}. AI-gen mesh drop-in ⊥ code change outside ship/pieces data.
V19: ocean FFT ≥ 3 cascades (≈250m/60m/15m domains), ≥ 512² each, independent choppy+Jacobian per cascade. Visible tiling @ any demo camera ⊥.
V20: ocean visual bar = reference parity: side-by-side vs `docs/final-full-result.png` (primary, in-game SoT), `docs/ocean-foam-result.png` + talk footage. ! match: turquoise SSS glow thru crests, dense sun sparkle glints, warm-tinted foam, atmospheric haze on distant islands. "good enough / later" ⊥ — user signs off ocean before T22 polish counts done.
V21: game UI: AAA pirate theme, designed w/ frontend-design skill — cheap clipart/templated look ⊥. DOM overlay reads settings store; Esc → pause (sim tick halts, render continues); settings survive reload.
V22: ∀ visual system (ocean, clouds, sky, ship, foam, terrain, ropes, fx): agent/code done ≠ task done. Task `x` ! requires in-browser screenshot check vs refs; hero visuals (ocean, clouds, ship) ! user signoff. Unit tests + tsc alone ⊥ sufficient.
V23: TSL chained math w/ reordered args (`a.mix(b,t)` → receiver=FACTOR via mixElement) ⊥. ! use functional forms `mix(a,b,t)`, `smoothstep(e0,e1,x)` for 3-arg math. Chained `x.smoothstep(e0,e1)`/`x.step(edge)` OK (receiver=x) but ! comment the reading at use site.
V24: water transparency (refs docs/underwater-*.png): near-surface see-through — submerged geo visible, tinted by depth (turquoise→deep teal, exp falloff), screen-space refraction distortion via scene color+depth textures. Opaque-wall water @ grazing/shallow view ⊥.
V25: underwater camera: submerged → full-screen underwater grade (teal exp fog by dist, desat blue-green, soft god-ray fake OK, slight view wobble), waterline crossing → split view w/ meniscus band, no pop. Weather/day-cycle drive underwater tint too.
V26: ocean reflections = PLANAR (mirrored scene pass: ship, islands, clouds visible in water) per talk 03:59 — SSR ⊥, analytic-sky-only ⊥ final. Reflection res ≤ half, blur ok, fresnel-blended.
V27: deck water = event-driven per talk 11:12: bow-immersion sensor (immersion+speed gates, same signal as bow spray) → splash() injection; runs for player ship only (1 ship budget). Passive always-on splashing ⊥.
V28: GPU safety: ∀ shader division ! floored divisor (`.max(ε)`); ∀ dispatch count & buffer size from sanitized construction-time ints; ∀ caller-fed uniform ! finite-guarded; dead/invisible particles ! zero-size (no opacity-0 rasterization). TSL Loop ! literal bounds.
V29: compute-written storage buffer sampled by render ! SEPARATE read-only view (`storage(buf.value,type,count).toReadOnly()`). `.toReadOnly()` on the write-side node ⊥ — setAccess returns `this`, mutates the shared node, compute writes then compile vs a read-only binding & vanish SILENT (no error, no NaN, buffer stays 0). ∀ new compute→render buffer ! one browser readback proving non-zero.
V30: ocean view = open 360° horizon, ≥ ~4km readable sea. Visible world-edge/cutoff ⊥, fog wall ⊥, "square of water" ⊥. Displacement/normal fades ! ride LOD, never end in a hard ring.
V31b: the colour-space trap has a SECOND face: maths that is correct in shape but done in the WRONG SPACE under-corrects silently instead of breaking visibly (measured: a desaturation done in sRGB missed by 3% vs linear). ∀ operation composed with an albedo multiply ! happen in LINEAR. Name the two entry points distinctly in code (`setLinear()` vs `setSrgb()`) — they are one overload apart.
V31: colors authored sRGB (hex/params) ! enter via `new THREE.Color(hex)` or `setRGB(r,g,b,THREE.SRGBColorSpace)`. Bare `setRGB(r,g,b)` / `new THREE.Color(r,g,b)` ⊥ — writes LINEAR working space w/ no transfer fn → ~2× too bright + desaturated, silently. Applies ∀ color: material, light, fog, uniform.
V32: sky background ! camera-anchored @ infinity (`scene.backgroundNode`, depth off). Finite world-fixed dome ⊥ — ship travels unbounded, so any radius eventually clips vs camera.far (→ clear-color void cone) or is exited entirely (→ pale ball). Fog far ! saturate exactly where water ends: earlier = haze wall (V30), later = hard water/sky seam.
V37: closed-hull proof = weld shell pieces → count tris per edge. Edge used once = open boundary; only legit boundary is the top rim. ! also assert consistent winding (a flipped strip reads exactly like a hole). Containment/silhouette sweeps ⊥ sufficient — they prove pieces sit inside the outline, not that the surface is closed. Mating pieces ! SHARE their sampling constants; two polylines through one curve w/ different sample points agree only at the ends.
V38: procedural relief ! surface-gradient method on screen-space derivs of the height (one eval). three's `bumpMap()` ⊥ w/ procedural nodes — it re-samples its TEXTURE at UV+dFdx(UV), so a procedural node returns the same value 3× → zero gradient → no relief, NO ERROR. ∀ height field feeding it ! C0-continuous: any `step()`/per-tile constant spikes to a 1px line under differentiation.
V49: cross-owner aliasing: when material A multiplies its screen-differentiated height by a factor sampled from B's texture, the product rule hands the normal `H·dS/dx` — B's minification aliasing amplified by A's own magnitude. NEITHER owner can see it alone (A's field is fine; B's texture is fine; the bug lives in the PRODUCT), and it hides entirely while the factor is constant (dry deck ⇒ dS/dx = 0). ∀ factor scaling a differentiated height ! band-limited or read from a filtered tier by its OWN owner.
V44: ∀ value reaching an ADDITIVE light slot (`emissiveNode`, addLight…) ! provably ≥0 AND bounded AT SOURCE, not clamped after. Attenuation/occlusion ! expressed MULTIPLICATIVELY (less light arriving = a multiply), never as a negative addend. Corollary, learned the hard way: **flooring a divisor bounds the DIVISION, not the QUOTIENT** — `ray/max(y,ε)` is finite but can pin at 1/ε, and its finite difference then explodes. Clamp the magnitude of the RESULT too. Bounded factors ⊥ ⟹ bounded product.
V48: ∀ texture sampled under minification ! a mip chain (`generateMipmaps`, `LinearMipmapLinearFilter`) or an explicit filtered tier. Unmipped minified sampling = per-pixel aliasing, and it is WORSE when the sample feeds a screen-space-differentiated height (relief/normal) — the derivative of noise is louder noise. FOURTH occurrence: foam StorageTexture (no mips possible → reduction chain), ocean far-field, deck field, and mast staves — the last needs no texture at all, just `fract(atan2(z,x)·n)` going sub-pixel, which `reliefNormal` then differentiates into random normals and diffuse averages to BLACK. ∀ PROCEDURAL periodic term ! band-limited by `fwidth()` too, not only textures. Symptom is always "uniform per-pixel speckle over a whole surface", never a localised artifact. SIXTH occurrence + the refinement that finally killed it: band-limit ! be measured against the SHARPEST FEATURE's own width, ⊥ against the REPEAT. A 0.044m caulk seam inside a 0.55m plank goes sub-pixel 12× sooner than the plank does, so a `fwidth(plankCoord)`-vs-period gate (which fixed the masts, where feature≈period) left the whole hull+deck speckling over the entire distance band between the two. Cure = 2 halves, both required: (a) WIDEN — transition width ≥ 2 px of its own coord's fwidth, so the fn stops varying faster than the sample grid (1 px ⊥: neighbours still straddle the full step); (b) FADE amplitude ×feature/effective, = the average that pixel should have seen. (a) alone smears a 4cm groove over a whole plank at range; (b) alone leaves the step. Reaches 0 on its own once the repeat is sub-pixel ⟹ no separate period gate for EDGES; smooth per-period terms (crowns, per-board tone steps) keep the period gate. `src/ship/bandLimit.ts` + tests/shipDetail.test.ts "§V.48" block (CPU transliteration pair).
V46: weather is LOCALISED, not global. Storm cells occupy world regions w/ soft edges; you sail INTO and OUT OF them, and can see clear sun on the horizon while rain falls overhead (talk footage). Global preset lerp of the whole world ⊥ as the final model — presets stay the param vocabulary (V7), but their strength ! be a field over world XZ.
V47: storm atmosphere ! anisotropic: haze/fog scattering STRONGER away from the sun, thinner along the sun vector, so a lit patch reads through the murk. Rain ! wind-driven (visibly slanted, direction from the same wind the sea + sails read), depth-faded, w/ per-position phase (B4). Uniform grey soup ⊥.
V45: ratlines/rungs & anything spanning ropes ! read the SAME solved sample buffer the shrouds render from (rope points StorageBuffer), sampled at matching curve parameter t on each neighbouring shroud. Static geometry authored against straight-line anchors ⊥ — the shrouds sag (catenary) and move (V42 dynamics), so authored rungs float in mid-air. One source of truth for a rope's position, always.
V43: **SoT parity is the MINIMUM bar for EVERY visual system**, not just ocean (V20 generalised, user 2026-08-12: "this is the bar for everything — water and environment"). ∀ system (islands, terrain, vegetation, ship, sky, clouds, fx, UI) ! judged side-by-side vs SoT reference before `x`. "Procedurally plausible" ⊥ — silhouette, art direction, density & material richness ! read as authored. Generated ⊥ excuse for generic.
V41: ropes ! 2 render regimes per talk: NEAR = shaded tube (normals, texture); FAR = simpler TRANSLUCENT pass, no textures/normals, w/ **phone-wire AA** [Persson 2012] — when projected width < 1px, widen geo to ~1px and scale alpha by (trueWidth/drawnWidth) so perceived thickness & energy are conserved. Fixed-radius sub-pixel tubes ⊥: they shimmer or vanish. Crossfade regimes, no pop.
V40: WebGPU per-stage resource limits ! REQUESTED at device creation (`requiredLimits`), clamped to `adapter.limits`. THE BINDING WALL IS SAMPLERS, NOT TEXTURES — this invariant was written about the wrong axis. Metal caps samplers per function at 16, so on Apple silicon `adapter.limits.maxSamplersPerShaderStage` IS 16 and asking for it buys NOTHING. Every FILTERABLE texture gets its own `${name}_sampler` (never shared); an unfilterable one (min===mag===Nearest, no compareFunction) gets none, which is why the two viewport reads are free. Dedup is by texture UUID not by node (3 `viewportDepthTexture` reads = 1 binding), and stages count separately (`uniformGPU` keyed on (node, shaderStage)) — so displacement sampled in vertex AND fragment costs both. Overrun ⊥ degrades: `CreateBindGroupLayout` fails, pipeline invalid, MATERIAL NEVER DRAWS, console names the descriptor not the feature that added the 17th. LEDGER + TRIPWIRE = `tests/oceanBindingBudget.test.ts` — ! recount there in the SAME commit as any binding change. NOW: ocean fragment **16 sampled textures / 14 samplers**, vertex 3/3 (was 18/16). RECLAIMED: (a) `src/sky/uncoloredShadowNode.ts` subclasses `ShadowNode` and reimplements `setupShadow` WITHOUT `texture(shadowMap.texture, shadowCoord)` ⟹ `shadow()` binds ONE (depth) not two. Legit ∵ nothing here casts a COLOURED shadow: the pass writes `vec4(0,0,0,1)` onto a target cleared to `(0,0,0,0)`, so that RT is a pure "did a caster cover this texel" stencil. Cost = upstream's fractional-alpha ring in the OUTER half of the PCF penumbra, ~7.8cm @ shadowExtent 80 / mapSize 2048. VSM THROWS rather than silently rendering different. (b) the 3 cascade `derivatives` StorageTextures → ONE `THREE.StorageArrayTexture(n,n,3)`: one binding, ONE SAMPLER, 3 layers (−2/−2). Verified live: 1 GPUTexture 512² arrayLayers 3 rgba32float. SPENT: +1 on foam's 2nd filtered tier (cascade 1). 2 spare, earmarked for the foam art textures (§I, §T.5, §T.22). Texture count now fits the WebGPU DEFAULT exactly ⟹ textures ⊥ the binding axis any more, but KEEP the `requiredLimits` raise: one more texture puts it back over. ARRAY TEXTURES (three r180): a FILTERABLE array texture ⊥ SAMPLEABLE IN THE VERTEX STAGE AT ALL — `WGSLNodeBuilder.generateTextureSampleLevel` and `generateTextureLevel` DROP the depth index in their filterable branch and vertex always routes there (`textureSample` is fragment-only); `generateTextureGrad` drops it too and `console.error`s outside fragment. The emitted WGSL is INVALID. ONLY the unfilterable `textureLoad` path carries the layer index. ⟹ `derivatives` (fragment-only, + `textureLoad` in the foam/spray compute passes) IS an array; `displacement` (filtered in vertex by `positionNode`) ⊥ and ! stay 3 textures. `StorageArrayTexture` has ZERO usages and ZERO tests inside three — we are its first user. 2 further traps, recorded in the ledger test: `textureStore(t,uv,v).depth(i)` stacks the UNLAYERED node (`textureStore` calls `toStack()` immediately, `.depth()` returns a CLONE) ⟹ write `storageTexture(...).depth(int(l)).toWriteOnly().toStack()`; and `.depth()` silently resets storage access (`TextureNode.clone()` ⊥ copies it). ∀ cascade layer access ! go through `sampleCascadeLayer`/`loadCascadeLayer`/`storeCascadeLayer` (`src/ocean/oceanTextures.ts`). ⟹ three's `CSMShadowNode` STILL RULED OUT: a full `ShadowNode` per cascade, so even with the uncoloured subclass that is +1 texture +1 sampler PER EXTRA CASCADE on top of the 14.
V39: perf numbers ! measured via GPU TIMESTAMP QUERIES (`renderer.trackTimestamp = true` + `resolveTimestampsAsync('render')`), min-of-N. ALL WALL-CLOCK TIMING ⊥ in a hidden tab — rAF fps AND `renderAsync()`+`onSubmittedWorkDone()` alike: canvas swapchain acquisition is throttled when `visibilityState === 'hidden'` and DOMINATES the wall clock, so the number goes content-INDEPENDENT. Measured: same scene reads 8.19ms by timestamp vs 760ms by wall clock; hiding the ocean moved wall clock 28.2→22.65ms while the true cost is 7.1 of 8.19ms, and one "baseline" repeat read HIGHER than the ocean-hidden case. Tell (both layers): hiding 1M tris barely moves the number. `renderer.info` ⊥ without `info.reset()` — it ACCUMULATES across renders (read 3322 calls/7.13M tris; true 367/978k). Cost of an occluder can be NEGATIVE (ship = −1.06ms: hull fragments are cheaper than the sea they cover).
V35: ref GPU (Apple Silicon) = TILE-BASED DEFERRED. Index buffers ! row-major/spatially-local. Front-to-back "early-Z" primitive ordering ⊥ — consecutive tris landing in different screen tiles forces the tiler to hold every tile's prim list open → frame 1 never completes, reads as a browser hang. Immediate-mode raster optimisations ! assumed to transfer.
V36: ∀ crest/foam/SSS threshold ! expressed as a multiple of live sea σ (`heightRms`, published by the ocean & refreshed on spectrum rebuild). Absolute metre gates ⊥ — they silently change meaning when the spectrum moves (a 0.25m gate meant top 9% of the sea, later 36%). `amplitude` ⊥ as normaliser: it fell 0.75→0.32 while Hs ROSE 2.3→2.8.
V34: caustics ! derived from the LIVE FFT surface (divergence/Jacobian of refracted sun rays), projected along the refracted sun direction & depth-attenuated. Tiled scrolling caustic-texture loop ⊥ ("nasty loop look"). Receivers: hull @ waterline, seabed shallows, beach. ! reuse the existing per-cascade Jacobian/derivative data — a second full-res surface eval ⊥ (V17).
V33: ship orientation single-owner (was §B.6, promoted after 2nd near-miss): sailing owns yaw ANGLE **and** yaw RATE (`angularVelocity[1]`) + heel-as-offset, and ! preserve buoyancy's pitch (Tait-Bryan decompose). Buoyancy owns pitch/roll dynamics & ⊥ write `angularVelocity[1]` at all — even decay/smear. Physically free: vertical probe forces give `cross(r,[0,f,0]).y ≡ 0`, flood-list torque y-free. Silent overwrite = helm's built-up turn bleeds away.

V50: a force that is physically an INTEGRAL over a body (∫ pressure·dA, hull heave = ∫ρg·h(z)·b(z)dz) ! be SAMPLED as one: ≥ 3 stations per shortest wavelength it must answer, weighted by the body's own section. N hand-placed probes ALIAS it — 8 probes over a 35 m hull summed to −0.28 where the integral is +0.10 at λ40 (WRONG SIGN: hull rose on crests) and to ≈0 at λ50, a null right through the sea's energy band (mean λ≈69 m). Fails SILENT and reads as feel, never as a crash: "doesn't react to the waves", "sinks into them", "not dynamic". ∀ such response ! verified as a SWEEP over wavelength, ⊥ one wavelength — the notch hides between samples, and a λ150 spot-check passes on the broken layout. The sweep axis is not only wavelength: HEADING and REALISATION are the same trap. sea-physics' own feel fixture sampled one heading (identity quaternion) at one seed for 40 s and read maxPitch 18.0°/green 2.17% as "the answer", where the same sea swept over 8 headings × 4 seeds gives 9.3–21.8° and 0.00–1.39% — it was quoting p85 of a distribution. Bounds set from a single cell of a sweep move whenever the sea's DIRECTIONALITY moves, with no bug anywhere. Corollary: once the integral is real it does its own low-pass (a 35 m hull cancels an 8 m wave to 5%), so any Gaussian "footprint" smear stacked on top is either redundant or the thing eating the swell.
V51: ∀ SimState channel (each position/velocity component, the quaternion) = EXACTLY ONE integrator per tick. Other systems contribute FORCE/torque into the accumulator the owner integrates; they ⊥ run their own `x += v·dt`. A second integrator is silent — no error, no NaN, no drift, just a channel scaled by the number of writers (measured: ground speed exactly 2.000× velocity, so the ship outran her own wake, drag equilibrium and wave-encounter frequency at 26 kn of "13 m/s"). 3rd sighting of the two-owners shape after §B.6 (orientation) and grounding-vs-thrust; single-owner ! stated at the top of every module that writes shared state.
V52: ∀ coefficient that sets the response of MORE THAN ONE physically independent DOF ! be separately scalable per DOF. Heave, roll and pitch damping all came off one `buoyancyDamping`, so the (correct) decision to damp heave hard — the fix for "we're losing contact with the waves" — silently took roll to ζ 0.41, ~4× a real hull's, and the ship stopped rolling at all. Neither tuning pass can see the other's damage: each DOF looks defensible alone. Tell = a tuning note on one axis quoting a user report about a different axis. Damping ratios ! MEASURED per DOF (free decay → T and ζ), ⊥ inferred from the coefficient.

V53: ∀ hand-tuned physical constant (mass, inertia, stiffness, damping) ! carry a DIMENSIONLESS check against the real object, asserted in a test — ⊥ tuned by its effect alone. Every one of the galleon's numbers "worked" individually: roll period 7.1 s and pitch 4.1 s are both right for a 35 m ship. The ensemble was a 150 t model of a 600 t ship, because the periods had been bought by inflating the inertias around a mass 4× too small — pitch gyradius 0.73·L and roll 1.32·B against real 0.25·L / 0.40·B, and a heave period of 2.30 s against the closed form 2π√(d(1+a)/g) = 4.9 s. Reads as "feels a little bit too light", never as a wrong number. Checks that survive retuning: displacement vs ρ∇, stiffness vs ρg·A_wp, gyradius/L and /B, every natural period vs its own closed form.
V54: a lumped support/contact model ! give its sample points the BODY'S OWN EXTENT. Stations on the y=0 waterline plane with force = k·max(depth,0) make the hull an infinitely thin sheet: rise the whole equilibrium immersion (0.44 m) and support is not reduced, it is GONE, and the body is then ballistic at exactly −g with no water term at all — measured 15.4% of ticks and 1.87 s of continuous air in storm. Support ! fade over the real extent (zero buoyancy needs the KEEL clear, not the plane) AND ! stop growing past full submersion (bounded result, §V44). Tell = "it catches airtime / feels light" while the restoring force reads exactly −g for whole seconds. Corollary: giving the body extent RAISES the forcing everywhere (a station over a trough still makes torque, correctly), so the per-DOF damping (§V52) ! be re-measured after — roll went back to 180° capsize on the same coefficients.
V55: ∀ oscillator whose RATE can vary ! accumulate PHASE (`φ += ω·dt`, wrapped to [0,2π)). `time × rate` ⊥ — `t·ω(t)` is a valid phase only while ω is CONSTANT; the instantaneous frequency is `ω + t·dω/dt`, so ELAPSED TIME MULTIPLIES EVERY WOBBLE IN THE RATE, unbounded, and can drive it NEGATIVE (ripple reverses). Measured on the flags: 0.93Hz intended → 2.2Hz @15s, 5.8Hz @60s, 59.5Hz @600s, aliasing against the display past ~30Hz. Tell: LOOKS FINE ON A FRESH RELOAD, unbearable ten minutes in — survived three flag passes for exactly that reason. Wrap the accumulator (precision), and give a detuned harmonic its OWN accumulator: multiplying a wrapped value by a non-integer ratio breaks continuity at every wrap.
V56: the sea's OWN PIGMENT is a floor, not a base layer. ∀ term composited onto the water — sky reflection, Fresnel, foam, wake, spray, haze, glint, SSS — ! be judged by whether the water still reads as WATER at that view angle, distance and sea state. No single term may drive the surface toward neutral grey, and their SUM may not either even where each is individually defensible. Test the ratio that matters: sea chroma (or body-colour weight) sampled across view azimuth AND across distance, floored — not mean luminance, which a grey wash passes. FIVE separate complaints have been this one thing wearing different clothes: §B.12 cow pattern (sun-independent SSS), the teal hull (ambient desaturation in the wrong space), the rotation blowout (unbounded view-dependent lobe, 3.83x azimuth ratio), the grazing grey wash (`grazingSaturation` 0.55→0.15 under physical Fresnel — distant water is ENTIRELY grazing), and the wake reading as a white curtain instead of disturbed water. Each was fixed locally; none made the sea robust. A physically-correct term that erases the pigment is still a defect (§V.20 is a LOOK bar, not a physics bar).
V56b: A FLOOR CANNOT FIX A WEIGHT. §V.56's pigment floor guards against a term DRIVING chroma down; it is inert where the sea is simply outweighed. Measured at 400m/2° grazing: composite chroma 0.258 sits comfortably above the floor, so raising `pigmentFloorChroma` 0.14→0.26 moves it by 0.0001 — the water is not grey, it is 81% REFLECTED SKY BY WEIGHT. Distinguish the two before reaching for a knob: chroma-below-floor ⟹ floor; weight-dominated ⟹ the blend (`grazingSaturationFar`) or the amplitude (`reflectionStrength`). Also: restoring surface DETAIL restores the VARIANCE of the Fresnel weight, not its MEAN — past Schlick saturation more slope scatters the per-pixel weight around 0.81 without moving the average, which is why a detail fix can be visibly right (structure returns) and leave the tint complaint exactly where it was.
V57: TSL mutators (`assign`, `addAssign`, `mulAssign`, `.toVar()`) are VALID ONLY INSIDE A LIVE `Fn()` BODY. `Node.prototype.assign` (three TSLCore.js) needs `currentStack`; with none it logs ONCE and RETURNS `this` UNCHANGED — the write is dropped and the graph still builds, compiles and renders. ⟹ ∀ node graph built at MODULE SCOPE or in a plain helper called outside `Fn()` silently loses every mutation. Measured: the ocean's whole §V.5 turbulent sub-noise (every `microDetail*` param) never reached a pixel, while the params read as tuned. Tell: `THREE.TSL: No stack defined for assign operation` at boot — it looks like noise and is the ONLY warning you get. Build slope/normal/colour graphs INSIDE the `Fn()` that consumes them, or return a value instead of mutating. Cf. §V.29 (`toReadOnly()` mutating a shared node) — same family: a TSL call that looks like it returns a new thing and does not.
V58: a DIRECTION-FREE metric cannot generate ORIENTED features. det J is AREA compression: its super-level sets are round and one-sized by construction, so foam injected from it needs a smear kernel to fake a shape — and a smear kernel with ONE global axis IS the shape (measured: cap axial spread 2.1°, length CV 0.08, every cap within ±2° of `windDirection+90°` across a 420m tile). Breaking is UNIAXIAL: use the minimum eigenvalue λ⁻ = ½(tr − √(tr²−4·det)) — Tessendorf 2004 eq.45-49, "the minimum eigenvalue is the actual signal of the onset of folding". a=−0.3,b=+0.3 ⟹ det J = 0.91 (compression and stretch CANCEL in the product) while λ⁻ = 0.70. λ± needs only trace + det, both already published, so the anisotropy moves to the INJECTION and the blur stays isotropic — shift-invariance and exact mass conservation KEPT, not traded (2.1°→21.4°, CV 0.08→0.68). GENERAL RULE: before adding a directional filter to make something look oriented, ask whether the SOURCE metric threw the direction away.
V59: a MOMENT used as the σ of a quantity ! share that quantity's DIRECTIONALITY. `spectralSteepness` measures RMS ∂Dx/∂x — transfer kx²/|k|, a PROJECTION ONTO X — but what sets the spread of det J and λ⁻ is the jacobian TRACE, transfer (kx²+kz²)/|k| = |k|, which is direction-free. The projection is a valid proxy ONLY while a single train runs with the wind, which is exactly why one constant appeared to serve every band of every preset. Add a second wave train at an angle and it collapses: swell at ~57° to x ⟹ cos²θ ≈ 0.29, measured `jacobianSigma` 0.38× the realised σ in calm cascade 0, λ⁻ mean/steepnessRms −2.99 against a −1.06 constant. Publish the direction-free |k| moment (RMS of ∂Dx/∂x + ∂Dz/∂z) and have consumers take THAT. Blast radius is wider than foam: `effectiveChoppiness` divides by `steepnessRms`, so the §V.44 anti-fold cap is WEAKER THAN BELIEVED on a swell-dominated sea. Converse of §V.58 — that one says a direction-free metric cannot make oriented features; this says a directional moment cannot measure a direction-free quantity. Tell: one constant that fits every band of every preset is suspicious, not reassuring.
V60b: a TRIGGER ! be reachable in the state space the system actually PRODUCES. Crest spray gated on `det J ≤ 0` — self-intersection — which is exactly the event `effectiveChoppiness` clamps the sea to PREVENT (§V.44 anti-fold). Measured on realised fields: min det J over a 400m domain is +0.255 at swell, +0.491 at calm, so the gate passed 0.000% of the sea. Not rare — NEVER, at every preset but storm, for the whole project. Two systems each individually correct: the clamp guarantees det J > 0, the gate fires below 0. §V.49's shape in the TIME domain rather than the spatial one. Test the shape: sample the driving quantity's realised RANGE (not its σ, and not the closed form) and assert the threshold falls inside it. A z-score is not enough — 6σ of a distribution that is hard-bounded above the threshold is 0%, not 'rare'. Related: a threshold on a quantity another subsystem CLAMPS ! cite that clamp, or it will be re-derived against an unclamped model.
V60: a GATE may never be narrower than the BODY it multiplies. `smoothstep(t, t+0.55, cov)` over a body curve `1−exp(−1.6·cov)` (ramp ≈1.44) is 2.6× narrower, so the gate RE-IMPOSES a hard edge after every softening stage upstream — the blur, the fluff and the depth ramp were all working and all unreachable. Assert `gateWidth ≥ ln(10)/bodyDensity` at the params. COROLLARY, and it is the reason the blur could never have worked: when a channel is an additive SUM of premultiplied contributions (ONE/ONE), interior values run 3–6 while alpha saturates by ~1.2, so softening scales as `blurRadius / interiorCoverage` — MORE OVERLAP MAKES THE EDGE HARDER. Widening the kernel moves the ramp's tail; the visible edge does not move. Tell: the densest clusters are the sharpest, which is backwards. Cf. §V.49 — the defect is in the product of two individually-defensible terms.

V61: ∀ resampling of a field ANOTHER system owns, the sample registration (half-texel offset, origin, phase) ! be expressed in the OWNER's units — world metres, or the owner's texel — ⊥ in the consumer's own grid units. A literal `-0.5` answers "half of WHOSE texel?" with "mine", and the consumer's grid is free to move: the CPU ocean mirror read at half a 64² texel while the GPU read at half a 512² texel, so the sea the ship floated on sat domain·(1/128 − 1/1024) away from the sea drawn — 2.9 m at a 420 m cascade, 6.90 m once the domain grew to 1010 m. Silent by construction (both fields look like plausible oceans; only the REGISTRATION differs), and interpolation quality is the false lead — cubic made it fractionally WORSE, which is the tell that the error is not sampling. ∀ mirror of a GPU field ! one test comparing it against a finer mirror of the SAME modes; the two must agree to reconstruction error, and "same modes" ! be proven separately (energy kept = 100%), or the test cannot tell registration from truncation.

V62: A KNOB THAT DRIVES NOTHING IS A DEFECT, and this project produces them faster than it finds them. TEN occurrences: B18's shape, `renderer.trackTimestamp` set after construction (Backend copies it once, in its own ctor), `resolveTimestampsAsync` never called so the pool filled and numbers went silently wrong, `setFeatureSink` registered ONLY for 'shadows'/'spray' so postFx/postAo/postBloom/postVibrance/postVignette drive nothing, T36's `uDynamic` captured once with no `.onFrameUpdate` — AND ten more rope uniforms beside it (gravity, damping, windForce, gust x2, maxStray, strayFraction, teleport, simDistance, simFadeBand) so ~15 Tweakpane sliders are dead while `params/ropes.ts` claims "everything else is live via uniforms" — **RECOUNTED §T.48: it was 26 of 31, ⊥ eleven. THE COUNT IN THIS ENTRY WAS ITSELF UNDER-MEASURED**, ∵ only the compute pass was audited and the 3 RENDER modules (ropeMesh/ratlineMesh/blocks: every colour, roughness, the §V41 px thresholds, the block detail band) were never counted. ∀ audit of this defect ! sweep the WHOLE dir mechanically (`grep 'uniform('` vs the write sites), ⊥ the file the bug was reported in, `ropeParams.dynamic=false` behind a comment claiming a fix "in flight" that landed IN THE SAME COMMIT, and `devicePixelRatio` never re-read after boot (no matchMedia listener; setSize preserves a stale ratio). THE TELL: a value read ONCE at construction into a closure. A uniform without an update hook, a feature flag without a sink, a ctor param assigned post-hoc. Every one failed SILENTLY — nothing errors, the control just does nothing, and it is discovered by a user reporting that a fix "did not work". Wire the sink IN THE SAME CHANGE as the knob, or do not add the knob.
V63: MEASUREMENT VALIDITY IS PART OF THE RESULT. The renderer is NOT frame-deterministic across sessions — same code, two sessions, ocean RMS 16.18, because foam's blur is one blur per RENDERED frame ("age = blur count") and rigging integrates per frame, so 56fps and 35fps hold different state at the same sim TICK. Any screenshot A/B ! freeze the tick AND force the camera (kill rAF — `__game.setSpeed(0)` does NOT pause the sim, it ran to tick 9350 while "frozen"); that recipe measured 0.0001 repeatability. Also void: wall clock in a hidden tab (§B.25) — `visibilityState` goes hidden on focus loss and rAF stops dead, giving a black canvas over a live DOM; `renderer.info.render.calls` (4238→38978 within ONE build); and GPU timestamps, which are STILL dead after the ctor fix — `info.render.timestamp` reads a constant 29.688 ms across 90 consecutive frames. A number you cannot defend is worse than no number: say you could not measure (§Rule 8).
V64: SUB-PIXEL DETAIL ! CONVERT ITS VARIANCE, NOT DELETE IT. §V.48b says fade to the feature's own MEAN; that is necessary and NOT sufficient. `normLod` fades each cascade's normals to flat as they go sub-pixel — the correct mean, since a zero-mean slope field averages flat — but never carries the VARIANCE forward, so shading slope RMS falls to 59% at 53m, 31% at 130m and EXACTLY ZERO past 365m: half a kilometre of perfect mirror before haze starts at 900m. Worse, deleting it blinds the only variance estimator in the file (`dFdx(normalWorld)`) to precisely the band that should have widened the lobe. The deleted slope ! come back as ROUGHNESS. Corollary: check EVERY normal-sensitive channel, not just the specular lobe — Tokuyoshi/Kaplanyan specular AA shipped here and did not cure the complaint because the Fresnel weight `pow(1-cos,5)` (d/dcos ~ -5 at grazing) had no protection at all, and the gold/teal flip the user sees IS that weight coin-flipping.
V65: A SILHOUETTE COMPLAINT CANNOT BE FIXED IN THE INTERIOR. Detail MULTIPLIED onto a mask's interior leaves the OUTLINE untouched, and the eye reads the outline. Foam: every detail layer was `rawFoam.mul(detail).mul(knee)` while the boundary came from `smoothstep` on an isotropic gaussian blur — a circle BY CONSTRUCTION — so "blotchy, reads as discs" and "the edge of the cresting looks sharp and unnatural" were ONE defect, cured only by making the breakup a per-texel DISSOLVE THRESHOLD so the boundary is the texture's own torn contour. Sails: `x = (u-0.5)*width` is a pure function of u with NO wind term, so at full canvas the projected outline is exactly the flat rectangle for every wind, angle and trim — "the corners don't have to be perfectly straight aligned" is unreachable by tuning, and a z-only displacement can never round a corner. ASK: can the term I am changing move the boundary? If not, no amount of it will answer the complaint.
V66: SCALE A FEATURE BY ITS OWN DIMENSION. §B.20 (band-limit on the plank PERIOD 0.55m when the seam is 0.044m — 12x too late) is one instance of a general error. Others: `sailBillow` documented and applied as a fraction of the sail's DROP when camber is a section bowing across its WIDTH (main course 24.8% of chord at the default sea, 40.7% in storm, against a real 10-15%); `microFade` gated against `microScale` 2.4m when the sharpest wavelet is 0.643m and, since slope ∝ k, contributes 3.73x MORE slope than the base it is gated by; and `lodWeight` measuring camera distance (the VERTEX Nyquist limit) where `pixWorld` sits twelve lines up and the docstring already warns distance is no substitute at grazing (1/sin θ). A test that asserts an ABSOLUTE metre value against the wrong dimension locks the error in (§Rule 6) — assert the RATIO.
V67: CLOTH ! CONSERVE ITS OWN AREA. A belly that adds z without pulling x inward GROWS the surface: it inflates like a balloon, not a fixed piece of canvas, and that reads wrong at every depth so no amount of camber reduction fixes it. Arc-length compensation (arc ~ c(1 + 8/3(d/c)^2), hold arc constant, shrink projected chord) is two multiplies and is the single cue that separates cloth from balloon — it also gives the sheets and blocks something real to follow. Same class: §V.45's CPU/GPU mirror requirement means NO GPU-side PBD cloth, because 24 of 68 ropes resolve endpoints through `sailClothPoint` and would all detach. Stateful cloth owns its state CPU-side at fixed sim dt and publishes SCALARS.

V68: a floating body answers the PRESSURE FIELD over its wetted volume, ⊥ the free SURFACE. Linear deep-water dynamic pressure decays `e^(−k·d)`, so the wave part of the vertical Froude–Krylov force on a wall-sided section is `ρg·A·ζ·e^(−k·T)` — the SMITH correction. ⟹ **a hull low-passes the sea BY ITS OWN DRAFT**, as a consequence of where it sits, ⊥ as a filter someone tuned: at T 2 m a 189 m swell arrives at 0.94, an 8 m ripple at 0.21. Buoyancy sampled `heightAt` ⟹ EVERY wavelength had full authority whatever the draft; the stand-in was a Gaussian patch across the BEAM, wrong 3 ways at once — (a) a spatial kernel cannot express a per-WAVELENGTH factor, (b) it is FLAT along the hull, so a HEAD sea (where the pitch forcing lives) got no depth attenuation AT ALL, (c) σ 3.5 m spread each station over 10.5 m, wider than the 8.5 m beam it averaged across, at 5 ocean samples/station (180/tick). A per-mode factor belongs in k-SPACE: applied at the mirror's spectrum it costs one multiply-add per mode and **NO extra IFFT** — it rides in the FREE IMAGINARY HALF of the Dz transform (Dz's spectrum is Hermitian ⟹ its IFFT is purely real; `e^(−k·d)·h` is even×Hermitian ⟹ `i·`that is anti-Hermitian ⟹ purely imaginary; the two separate EXACTLY, measured residual 2.9e-7 m on σ 0.94). Per-CASCADE attenuation ⊥ — cascade 0 spans λ 40–1010 m, `e^(−kd)` 0.65→1.0 inside its own band. MEASURED heave RAO λ8/20/150/300: 0.024/0.105/1.08/1.09 → 0.005/0.056/0.99/1.05 — short end ÷5, long end −8% and TOWARD the physical asymptote 1.0 it used to overshoot; peak vertical accel in the shipped swell 2.60→1.65 m/s²; buoyancy step 0.292→0.084 ms/tick/ship. ∀ force that is ∫pressure ! read the attenuated field; ∀ GEOMETRIC query (waterline, cutwater, spray, green water, camera) ! keep the true surface — §V.8 is a claim about geometry. COROLLARY, and the reason the Gaussian was load-bearing: it was HIDING a §V.50 aliasing on the other axis. The port/stbd pair is 2-pt Gauss–Legendre and cannot integrate a wave shorter than the beam (1.95× the true roll moment at λ8). 3-pt (0, ±b√(3/5)) is exact through quintic and leaves Σw, Σw·x², Σw·z² IDENTICAL ⟹ every stiffness, period and inertia untouched.
V69: **ADDED MASS IS NOT DAMPING AND THE TWO ARE NOT INTERCHANGEABLE.** Damping opposes VELOCITY (bleeds energy, reads SLUGGISH); added inertia opposes ACCELERATION (reads HEAVY). ∀ DOF w/ a hydrodynamic added mass ! carry it in EVERY channel it acts in, ⊥ only the one that was easy to write. Heave had `addedMassHeave`; pitch and roll had NOTHING, so the only thing resisting a pitch reversal was the rigid hull — T_pitch 4.48 s against T_heave 5.43 s, i.e. she pitched half again as fast as she heaved, which the user reads as "it can flip forwards and change its momentum basically instantly… it has more inertia than that". §B.37 KNEW ("a lumped model has no pitch added-inertia") and paid in DAMPING for two rounds. The added inertias are ⊥ new constants: same entrained water, same stations, redistributed — a section's heave added mass ∝ b² where buoyancy ∝ b, so the added water's gyradius is SHORTER than the hull's (0.23·L vs 0.25·L) — then taken about the hull's own axes: A55 = A33·Σa·z², A44 = A33·Σa·x². ONE coefficient, three DOFs, ratios = GEOMETRY (⊥ §V.52: these DOFs are not independent, they are one field of water). Splitting it out FREES the rigid gyradii to be real (0.34·L/0.56·B → 0.25·L/0.45·B): §B.27 derived them by holding the periods fixed, so whatever the model was missing ended up INSIDE the gyradius, where §V.53's dimensionless check could only widen its bounds to accommodate it. MEASURED shipped swell: peak pitch angular accel 60→34 °/s², peak pitch 16.4°→8.1°, pitch-rate reversal interval 1.14→1.61 s at 3× energy, heave tracking 0.877→0.919, **ζ_pitch 0.412→0.342 with the damping coefficient UNTOUCHED**. STABILITY: it only ever DIVIDES BY MORE (`torque/(I + A·wet)`, A ≥ 0) ⟹ strictly more stable than the step it replaces, no implicit treatment needed, and it moves no FORCE so it cannot change where she floats.
V70: **A BAND LIMIT ! FADE TOWARD WHAT AN INFINITELY-DISTANT PIXEL WOULD SEE, ⊥ UNCONDITIONALLY TOWARD ZERO.** §V.48's cure is 2 halves, WIDEN + FADE; the FADE half is right for a GROOVE and WRONG for a STEP. ∵ a groove's band-limited mean IS the surrounding level ⟹ amplitude→0 converges to the correct answer. A step's mean is the RAMP between its 2 levels ⟹ amplitude→0 converges to a FLAT surface, i.e. DELETES the feature instead of filtering it. STEP ⟹ **WIDEN ONLY** (transition = max(authored width, 2px of its own coord's `fwidth`)) + gate the AMPLITUDE on the PERIOD — which §V.48 already blesses for "smooth per-period terms (crowns, per-board tone steps)", ∴ this is a REFINEMENT, ⊥ an exception. DIAGNOSTIC that separates the two: ask what ONE infinitely-distant pixel sees. Groove → the surrounding level. Step → the average of the 2 levels. Fade toward THAT. §B.48 is the price of getting it backwards: the deck had no step AT ALL ∵ the only step-safe construction anyone reached for was "crown it to zero at both seams" — which is band-limit-safe and also a perfectly flush deck.

V71: A PART POSITIONED AGAINST ANOTHER SYSTEM SURFACE ! RESOLVE AGAINST THAT SURFACE LIVE SHAPE, NOT ITS REST SHAPE. DIAGNOSTIC: if part A is placed relative to surface B, ask what happens when B MOVES — and the answer ! come from B own evaluator, not from an authored constant that happened to match B rest pose. THREE instances, three systems, one shape: ratlines authored against straight-line anchors while the shrouds sag (§V45); the buntline routed to a fixed masthead socket while the cloth bellies across its path — and note ANY chord across a convex surface crosses it TWICE, so re-pointing it anywhere fails identically, the cure is intermediate leads that RIDE the surface; reef points holding a fixed orientation against a surface that now curves, plus a second defect underneath where the authored station disagreed with its own uv by 8% of width, latent until the offset was read. ALL THREE WERE INVISIBLE until the host surface started moving more: they are latent defects that a FIDELITY IMPROVEMENT converts into visible ones, which is why all three surfaced the day the sail camber became genuinely wind-dependent (4640977, 8.1% -> 14.2%). TRANSFERABLE WARNING: improving one system realism exposes everything that was quietly assuming its old, stiffer behaviour — expect a wave of "new" bugs after any fidelity increase, and read them as latent rather than introduced. WHY THE TESTS MISSED ALL THREE: each part was tested against its OWN authored geometry rather than against the host surface, so all three passed while being wrong. A part-vs-host test ! evaluate the host.
V72: **SHOALING ! KEY ON DEPTH, ⊥ ON DISTANCE TO LAND** — ∴ "calm beaches AND rough cliffs" is ONE rule, ⊥ a rule + an exception: a shelving beach HAS a wide shallow band ⟹ wide calm zone; a cliff HAS NO shallow band ⟹ sea stays rough to the rock. ⊥ ADD A CLIFF CASE, there is nothing to except. TERM A = `tanh(k·d)` PER CASCADE (§V.19 band-split ⟹ each band HAS its own λ): ⊥ a smoothstep someone liked — tanh(kd) IS linear theory's deep/shallow transition (same factor in ω²=g·k·tanh(kd)) and hits 0.996 at d=λ/2, the textbook criterion, w/ NO authored endpoint. k = the band's ENERGY-WEIGHTED mean (`spectralMeanWavenumber`, live ∵ cascade 0's mean λ moves 249→87→143 m across calm/swell/storm as wind sea and swell trade dominance — a baked constant is wrong on 2 of 3). ⊥ GROWTH: real shoaling STEEPENS+GROWS before breaking (Green's law, Ks>1); attenuation ONLY, ∵ user asked for calming and `shoreRunup.ts` already owns the beach — a DELIBERATE artistic simplification, ! say so or it reads as a physics error. THE ONE COMPRESSION: world ⊥ planet-scale (floor 45 m where a 143 m storm swell wants 71) ⟹ k floored at `π/(shoalDeepFraction·openDepth)`, openDepth = DATA from the seabed field ⊥ an import of params/island. MEASURED price, the whole of it: `1−tanh(k·45)` = **5.7e-5** ∀ band ∀ preset ⟹ THE SHIPPED OPEN OCEAN CANNOT MOVE. TERM B = depth-limited breaking, a soft clip on the SUMMED elevation: EXACT IDENTITY below `shoalBreakerIndex` (0.78, McCowan — below it a wave is ⊥ breaking), saturating to `shoalColumnCeiling` (0.85) ⟹ (1−ceiling)·d of water ALWAYS remains. Written `min(|y|,knee) + head·tanh(excess/head)` — ONE expression IS both branches, C1 at the join (tail slope at 0 = sech²(0) = 1) ∵ a hard `min` draws a PAINTED RING along a depth contour. B is DISJOINT from A, measured: **never fires at calm or swell, any depth 1–200 m** (0.00%), fires 21.6%@1m / 7.0%@10m / 0.8%@20m / 0@45m at storm ⟹ the seas the user iterates on get pure A and their look is ⊥ exposed to the breaker constants. §V.8 — 2 TRAPS THAT MAKE "SAME FORMULA" INSUFFICIENT: (a) depth ! be read at the GRID COORD `q` the mirror's inverse-displacement solves for, ⊥ at the query `x` — the GPU shoals the vertex it is DISPLACING, so `x` puts the 2 seas 1–2 m apart horizontally near shore (§B.34 by another route); (b) depth ! be measured vs STILL WATER y=0, ⊥ vs the displaced surface — the displacement is what is being attenuated, so keying it on itself is a FEEDBACK LOOP, and a still-water datum lets both sides agree WITHOUT either knowing the other's displacement. ∀ scaled term ! be scaled on BOTH sides incl. the HORIZONTAL: the GPU's fixed point is `x = q + shoal(depth(q))·D(q)`. JACOBIAN ! carry the same gain (`1 + Σ(w−1)·g`, which keeps it ≈1 AT REST as every reader assumes) or the foam gate fires over a flattened lagoon = WHITE CAPS ON GLASS. NORMALS ! carry per-cascade `f_i` AND the clip's own derivative (free: `1−sat²`) or a flattened sheet is shaded w/ the slopes of the wave that was clipped away. §V.64 REFUSAL, and someone WILL try to "fix" it: shoaled-away slope ⊥ ROUGHNESS. §V.64 is about SUB-PIXEL variance, where the detail IS still there and roughness is the honest fate; shoaled water is GENUINELY calmer and there is no missing microfacet distribution — routing it to roughness makes a glassy lagoon read as FROSTED GLASS, the exact opposite of the look. That energy belongs to the SHORE (runup, swash, breaking). §V.48 — THE VERTEX STAGE CANNOT MIP ITSELF: three r180 `WGSLNodeBuilder._generateTextureSample` rewrites EVERY non-fragment read as `textureSampleLevel(…, 0)` — LEVEL ZERO, hardcoded, ∵ `textureSample` is fragment-only ⟹ mipping the texture buys the VERTEX stage NOTHING and the level ! be handed in (`seabedHeightLodNode`, level = log2(vertex spacing / texel), so it rides the §V.30 LOD). The FRAGMENT tint had the same unmipped exposure ALL ALONG and is fixed for free by the mips. §V.40 — **THE CONTESTED SPARE IS A FRAGMENT SPARE**: bindings key on (node, shaderStage), so vertex was 3/3 against 16 w/ TWELVE FREE while fragment fought over 1. Check WHICH STAGE before calling a feature unaffordable. RESIDUAL, bounded ⊥ chased: GPU reads the baked r16f (~6.8 m texels over a 7 km archipelago fit), CPU reads the exact sampler ⟹ ≤0.34 m depth error on a 1:20 beach ⟹ **≤0.02 m of surface height**. DOCUMENT the bound. MEASURED end to end: swell trough at 2 m depth −3.84 → −1.24 m ⟹ **a 2 m lagoon holds 0.76 m** (the §T.52 basin was sized at 4.5 m ONLY ∵ this did not exist); storm clears (1−0.85)·d at EVERY depth against a raw −24.3 m that drains everything shallower than 24 m today. CHANNEL SEPARATION, by luck of the existing wiring and worth ⊥ "unifying": `shoreRunup` scales on `ocean.heightRms·2` = the SPECTRUM-level DEEP-WATER Hs, so calming the surface ⊥ weakens the run-up — storm still runs the tongue ~4.9× further up the sand over water that is now navigable.

V73: A CHARACTER ON A SHIP STANDS ON A MOVING PLATFORM, AND EVERY GROUND-CONTACT ASSUMPTION BREAKS. docs/character_movement_specification.md is written for STATIC terrain — raycast down, lerp the IK target, compensate the pelvis. Our deck heaves, pitches, rolls and yaws every tick, and deckHeightfield.ts already publishes per-plank height to a few mm over 512x192. (a) LOCOMOTION SPEED ! be measured in the SHIP frame, not the world: a character standing still on a ship making 8 m/s is not running, and a world-frame speed drives the timeScale blend to full sprint with the feet planted — the anti-ice-skating mechanism inverted into a machine for producing it. (b) the IK raycast target ! resolve against the deck LIVE transform, which is V71 exactly; three instances of that shape are already recorded here (ratlines, buntlines, reef points), every one invisible until the host surface started moving. (c) the source spec delta*12 target lerp is tuned against a floor that does not move; on a deck pitching at up to 34 deg/s2 (093a7a5, after added inertia; 60 before) a lag that reads as smoothing on land reads as feet sinking through planks. (d) a 0.3 m step offset against a deck that is itself accelerating vertically is a different problem from a staircase, and the obstacles move too — COAMING_HEIGHT 0.18, GRATING_HEIGHT 0.09, plus heel and green water. AND THE TICK DISCIPLINE: the sim is fixed-tick with render interpolation, and 6f3c588 established the drawn hull time advances by exactly frameDt to 0.000 microseconds — character state ! integrate at sim dt and interpolate for render exactly as the hull does, or it will jitter against the deck it stands on in precisely the way the hull does not.

V74: A MODEL MAY BE APPLIED **ONCE** TO A GIVEN SURFACE. Where two subsystems both model the same quantity, the downstream one ! be a **FLOOR**, ⊥ a multiply. §B49: sand modelled wetness itself (`mix(dry 0.95, wet 0.3, gloss)`, gloss EXACTLY 1 below the waterline) and `applyWaterLighting` multiplied it AGAIN by the caustics module's water-film gloss 0.22 ⟹ **0.066 roughness = A MIRROR**: GGX at α=r²=0.0044 gives D(0) ≈ 16,400, so the sun's disc lands WHOLE on a huge near-planar seabed at 920 scene-linear = **230× the ACES white point**. DIAGNOSTIC that separates this from its neighbours: multiplicative stacking of the SAME quantity ⊥ a term applied to things it does not govern (that is a DOMAIN error — a shadow multiplied into the sky reflection). Both are wrong; the cures differ. LATENT SECOND INSTANCE, unfixed: `shade = mix(1, shadowMask, uShadowStrength)` where shadowMask ALREADY carries `shadow.intensity` 0.85 — inert only ∵ shadowStrength is 1.0, and its own docstring says "drive it from 1".
V75: §V.48 BAND-LIMITS THE **HEIGHT** FIELD AND SAYS NOTHING ABOUT THE **NORMAL**. `FILTER_PIXELS` = 2 is a Nyquist margin **CALIBRATED FOR UNIT GAIN**. Mikkelsen's `scale` IS dimensionless ⟹ a bump gain multiplies TRUE SURFACE SLOPE: §B50 took a 2.0° plank crown to **34.4°** and a 2.6° tilt to **42.4°**, and a 42° normal swing reconstructed from 2.5 samples/period is a moiré generator BY CONSTRUCTION. ∴ any term with a gain ! inflate its own filter by that gain (`1 + max(0,g−1)·slope/width`, exactly 1 at g=1 so honest callers ⊥ move), and §V.64 requires the removed variance be **CONVERTED**, ⊥ deleted — to ROUGHNESS only if the surface is specular-dominated, else to TONE/AO (a rough dielectric at roughness 0.74–0.82 aliases in DIFFUSE N·L, where roughness buys nothing). AND THE CHEAPER CURE, when the feature is an oscillation: differentiate it **ANALYTICALLY** — `h=(H/2)sin(2πc)` ⟹ evaluate the cosine EXACTLY at the fragment and screen-difference only `c`. **Differencing h itself is what makes the moiré.** Measured: the deck carried +3.6..+5.3 of excess checker energy before its fix; the analytic seabed ripple carries ≤+0.48 and DECAYS TO EXACTLY ZERO with distance.
V76: **EVERY INSTRUMENT IN THIS STACK CAN SILENTLY RETURN THE PREVIOUS ANSWER.** ⊥ an error, ⊥ a NaN — the prior frame's data, indefinitely, LOOKING LIKE A PLAUSIBLE STEADY STATE. Assume staleness; prove liveness with a control that MUST change. EIGHT recorded instances: three's `Animation` bumps `nodeFrame.frameId` from its OWN rAF (a hidden tab stops it ⟹ `post.render()` re-composites a stale scene texture); `readRenderTargetPixelsAsync` on a REUSED target returns frame one forever, and returns the 256-byte-row-padded buffer un-stripped; `canvas.toDataURL()` byte-identical 700 ms apart under load; `ctx.drawImage(webgpuCanvas)` frozen in a hidden tab (99 sim ticks, max pixel diff **0.00**); the GPU timer latched 29.688 ms all day and later overstated GPU **3.3×**; its pool overflowed ⟹ a top-8 that still rendered and still looked authoritative. ∴ `NaN` on the HUD is a FEATURE, an overflow ! SUPPRESS THE RANKING, and a foam accumulator that advances per RENDERED frame means `ui.pause()` ⊥ freeze it (null control measured meanDelta 31.6 against an A/B of 3.2 — the diff was measuring the ocean). Full recipes: `docs/agent-browser-harness.md`.
V77: TWO CONSUMERS RECONSTRUCTING THE SAME FIELD **WILL** DRIFT. ONE EXPRESSION, ⊥ two that agree today. FIVE instances, one system: the beach apron compressed `−rimDepth` inside the grid while `heightAt` returned it raw outside (**4.25 m step** on the footprint boundary); a shelf ramp keyed on `hypot` against a SQUARE grid (12.3 m corner disagreement); `waterHeightNode` summed the **RAW** cascade displacement while the ocean draws `Σhᵢ·shoal(kᵢ,d)` through `breakerClip` — measured **−1.945..+1.610 m** across a beach where the drawn sea sat at **0.000..0.281 m**, and THAT ONE SIGN ERROR PRODUCED FOUR SEPARATE USER REPORTS (false absorption on dry sand marching with the swell; no darkening on genuinely submerged sand half a cycle later; a wet band **46 m inland**; a speckled boundary); `sky/lighting.ts` computing `fog.color` with a comment claiming to "reproduce that exact blend" that structurally CANNOT (skyDome is AZIMUTHAL through `sunSide`, the CPU form is not) ⟹ a **21–25/255 seam swinging 11 units with azimuth**, so no tint could dial it out; and the underwater window left on a pre-fix formulation 600 lines from where the same defect was already cured. THE CURE IS ALWAYS THE SAME: pass the RENDERED value, or export the one function. `f62e037`'s terrain passes `max(−positionWorld.y,0)` ⟹ ON DRY SAND DEPTH IS EXACTLY 0, so every shoal factor is `tanh(0)` and a dry beach CANNOT be told it is submerged — by construction, ⊥ by a threshold.
V78: **RADIANCE IS ⊥ CONSERVED ACROSS A REFRACTIVE BOUNDARY** — the invariant is `L/n²`. Air→water MULTIPLIES by n² = 1.777, ∵ the 180° hemisphere compresses into the 48.6° critical cone (Snell's window) and the same flux occupies less solid angle. ∴ the sky seen from BENEATH is BRIGHTER per steradian than from above it. Absent from the whole tree until §B54: the window was `mix(horizon, zenith, upDot)` with `upDot` spanning 0.661→1 ⟹ **only the top 34% of a two-stop authored ramp, never reaching the horizon colour at all** — measured drawn/correct **1.07 at zenith and 0.33 AT THE RIM**, i.e. 3× too dark exactly where the compressed horizon and the sunset band live. The centre was right ONLY BY LUCK (a 1.15 art knob landing near n²). CURE: index by `sqrt(1 − n²(1−upDot²))` and fill with `skyDomeColor(refracted)` — the sky asked along the pixel's own ray, ⊥ a second authored gradient (V77).
V79: **THE OCEAN IS PER-PRIMITIVE BOUND AT A GRAZING HORIZON — ⊥ CPU, ⊥ fill, ⊥ vertex, ⊥ draw calls.** THIS ENTRY HAS BEEN WRONG TWICE AND BOTH WRONG VERSIONS WERE CITED INTO AGENT BRIEFS; recorded as corrections ∵ each one sent real work at a phantom. **v1 said CPU-bound** — falsified: the main thread is **idle 44% of wall time** (busy 8.5-12.9 ms of 23-28 ms), draw calls cost **3.8 µs ⊥ 34** (removing 110 with the planar reflection bought 0.42 ms), all five `updateMatrixWorld` walks = 0.27 ms, `combat.update` = 0.012 ms. **v2 said vertex-bound** — also falsified: **the ENTIRE vertex stage is 0.48 ms, 4.7% of the mesh's 10.2 ms** (measured skyward, ocean off-screen, every vertex still shaded). WHAT IT ACTUALLY IS, by isolated interleaved legs: displaced+horizon **10.20 ms**, flat (same fetches, magnitude × 0) **4.50**, no fetches **4.33**, nadir with the sea filling MORE of the frame **2.26**, ocean off-screen 0.48. ∴ **all three cascade fetches at all 263,169 verts = 0.175 ms** (a hard ceiling on any fetch-skip scheme) while **displacement alone = 6.07 ms** — same shader, same fetches, only magnitude differs. ⊥ FILL EITHER: 16× fewer pixels took the FLAT ocean 4.36 → 1.78 ms but made the DISPLACED ocean **SLOWER**, 10.31 → 14.39 (fewer larger tiles ⟹ more primitives per tile). THE SUB-NYQUIST SKIP IS REAL AND WORTHLESS: **353,568 of 789,507 fetches/frame (44.8%) are multiplied by an exactly-zero weight** — cascade 1 zero past 1039 m (51.8% of verts), cascade 2 past 222 m (82.5%), cascade 0 never (its cut radius is 10.9 km against a 4.6 km rim) — and a bit-exact skip measured **≤0.08 ms**. Built, measured, reverted. **WHERE THE FIDELITY-FREE WIN IS: THE MESH IS RADIALLY OVERSAMPLED AGAINST THE SCREEN.** The clipmap equalises world angular size (`spacing/r = const`), but a radial step's screen extent goes as `h·Δr/r²`: from a 12 m eye one ring is **0.49 px at 500 m, 0.24 at 1 km, 0.12 at 2 km — EIGHT RINGS PER PIXEL**. Rings past ~1 km are **48% of all triangles** carrying nothing the screen can resolve radially. ∴ **decouple RADIAL from ANGULAR density** — angular stays 512 (it sets the horizon silhouette and the lateral detail the user refused to trade), radial past ~500 m drops 4-8×. ⊥ `gridSegments`, which cuts BOTH axes and coarsens the mid-field (512 → 256 = +13 fps, REFUSED by the user). Caveat: the law is camera-height dependent (freecam reaches 300 m, where a 2 km step is ~3 px) and the geometry is baked at construction ⟹ wants a height-aware rebuild or the warp moved into the vertex shader, which is affordable now the vertex stage is proven at 0.48 ms.
V80: A TEST THAT PINS A **DECISION** RATHER THAN A **PROPERTY** FAILS ON EVERY LEGITIMATE MOVE OF THAT DECISION — and, worse, PASSES WHILE ENFORCING THE DEFECT. SIX re-cuts: `|Δ(peak/radius)| < 0.05` REQUIRED peak/radius constant, and a constant ratio IS "a big island is a scaled copy" — it passed the entire time the islands were cones; `ratio > 0.28` on every island FORBADE the sandbar the references are half made of; a cliff-fraction floor demanded a 45° rise over 25 m that an 11 m sandbar physically cannot have; a sunset-grade assertion bound to `skyParams.timeOfDay` broke when the default moved for an unrelated reason; a shadow-strip bound likewise; a wind-line gate asserted the ladder's MEMBERSHIP (`['f1','f2']`) where its own comment stated the property ("bare below Beaufort 3"). CURE: assert the PROPERTY (sub-linear growth; the world SPANS 3× in ratio; cliff coast UNEVENLY; `gate(w)===0 ⟺ w < onset` ∀ rung), and NAME the hour/condition the property belongs to rather than reading a knob that is free to move. COROLLARY: a test can be worse than none — `expect(s.y).toBeGreaterThan(0.5)` justified in its own comment by a FALSE premise ("screen UV, so y grows upward") wrote the bug down and then asserted it.
V81: **RAFT 2100 ⊥ TOUCHES PIRATE BOOT.** `src/main.ts`, `index.html`, galleon/brigantine blueprints, combat/ai dirs: diff = 0 for any R-task (except additive `?ship=raft` switch in main.ts, one task, T91). Test: `tests/raftIsolation.test.ts` reads `src/main-raft.ts` via `?raw`, asserts no import of `main.ts|combat/|ai/`; `git diff --stat` on pirate files = 0 at phase sync.
V82: **RAFT GEOMETRY TO SCALE, FROM THE REFERENCE, ⊥ EYEBALLED.** Every dimension in `raftParams` cites a row of `docs/raft2100/kon-tiki-reference.md`. Uncertain values (mast h, leg spacing, topsail/mizzen, beam) = params marked `// EST`. Tests assert PROPERTIES (§V80): centre log longest & outer shortest & symmetric stagger; cabin clear volume ≥ 2.4×4.3×1.2 m; cabin ridge < 1.6 m (⊥ cheat-height); opening on STARBOARD; deck present fwd+stbd, absent port-of-cabin+stern; 5 guaras in chinks; oar between thole-pins on block over 3 projecting centre logs; mast crossing h ∈ [8.5, 10].
V83: **RAFT DECK = THREE SURFACES AT THREE HEIGHTS + ROUND LOGS.** Walk surface is raft's OWN heightfield writer (`raftDeckField.ts`), ⊥ the planked-camber writer. Logs round (height falls toward chinks); mats proud by crossbeam Ø; cabin floor proud again by box height. Heightfield `solid` = cabin walls, mast legs, crates, stern block. Chinks 2–8 cm = ⊥ fall-through (foot radius > chink). Deck edge = real edge → water (§V85).
V84: **PLAY = WALK + TOUCH; HOTKEYS = DEBUG ONLY.** Every sailing/radio/sleep action has a diegetic station (socket on raft) reachable on foot, and a station-bound interaction. Hotkeys gated behind dev layer. Test: ∀ action in `raftActions` ∃ station socket resolving on the assembly (§V71) AND action fires via interact at that socket in a headless sim; hotkey path disabled when `devLayer=false`.
V85: **FP LOCOMOTION IN SHIP FRAME (extends §V73).** Player state in `SimState.player` (optional, additive): pos in RAFT-LOCAL coords while aboard, world coords ashore/swimming. Speed measured relative to deck. Overboard → swim, raft drift ≤ swim speed × 0.5 → always catchable. Crouch auto when head clearance < eye height (cabin). Step-ashore when deck edge within 1 m horiz / 0.6 m vert of ground. Tests: rolling-deck walk keeps deck-relative speed ±5%; swim catches raft from 20 m in < 60 s at max drift; crouch on sill, stand at door.
V86: **RADIO IS A PURE FUNCTION OF (freq, heading, dist, night).** `tuner.ts` ⊥ three.js, ⊥ audio. clarity = window(Δf); strength = falloff(d) × cardioid(heading−bearing); night ×2 range. Lock = clarity·strength > θ for 2 s, hysteresis on release. Tests: cardioid monotone toward station; lock needs dwell; unlock below θ−h; night doubles lock distance; NaN-safe.
V87: **CONTENT IS A MANIFEST, ⊥ CODE.** Swapping any clip/text/clue position = edit `manifest.ts` + drop MP3. No code path branches on teaching id. Test: manifest schema validates; every clue `at` lies on its island with slope < 35° and above waterline; every night clip gated on all island clues found; a synthetic 4th teaching loads with 0 code changes.
V88: **REVIEW IS BATCHED AT PHASE SYNC, ⊥ PER TASK.** Per task done = tsc + vitest (GPU-free) + `npm run build` (both entries). Browser/§V22 happens ONCE per phase (R1…R5) from a fixed station list + `tod` list through `preview.html?ship=raft` (R1) or `raft.html?at=...&tod=...` (R3+), screenshots to `docs/raft2100/lookdev/R<n>/`, user signs the set. Between syncs no agent opens a browser except the designated lookdev agent.
V89: **GRADE = LUT AFTER TONEMAP, IDENTITY-SAFE.** `src/core/grade*`: 32³ LUT(s) blended by tod band + LGG. Identity LUT ⇒ output Δ < 1/255 (test on CPU mirror of the sampler math). Grade off = node bypassed, ⊥ identity-sampled (§V17).
V90: **SIERRA ISLANDS WALKABLE.** New archetypes (dome, drowned ridge, cirque) at 256² grid; beach band continuous from waterline; ∀ clue path segment slope < 35°; one Half-Dome-like silhouette unreachable (≥ 1.5× furthest station). Tests on heightmap only.
V91: **MOON HAS ITS OWN ORBIT; DISCS EXIST ONLY ABOVE THE HORIZON.** moonDirection ⊥ derived from `sunDirection(t − 24·phase)` (same track, time-shifted ⇒ moon rides beside the sun near new/old phase). Moon = own track: ecliptic-offset orbit (inclination param) + phase angle from sun → direction continuous in t, ⊥ NaN, elongation from sun = phase·180° ± inclination. Sun and moon discs/glow/halo gated to 0 below −(discRadius + margin) elevation; key-light direction used by ocean/caustics ⊥ rises above the horizon during handover (keep §moonCycle nadir route). Tests: moon elongation monotone in phase; at phase<0.1 moon ⊥ within 15° of sun when sun elev ∈ [−5°, 5°]... (⊥ possible: new moon IS near sun — but then its lit fraction ≈0 ⇒ disc intensity ≈0; assert disc·lit = 0 within 15° of sun); disc intensity 0 ∀ elev < −margin; sky palette at sunset driven by TRUE sun elevation only.

## §T TASKS
id|status|task|cites
T1|x|scaffold: Vite+TS+three WebGPU, gate page, rAF+fixed-tick loop, SimState store|V1,V2,V3
T2|x|debug shell: Tweakpane auto-bind params modules, perf HUD per-pass timers|V16,V17
T3|~|FFT ocean compute: spectrum gen (Phillips+wind), IFFT passes, 3-cascade displacement+choppy+Jacobian StorageTextures|V4,V19
T4|~|ocean surface mesh+material: clipmap grid, TSL cascade-blend displace, SSS fake, specular+sparkle glints, depth-based color|V4,V5,V20
T5|~|foam sim: Jacobian inject compute, progressive blur ping StorageTextures, crest/soft tex blend|V6
T6|~|weather presets: calm/swell/storm param sets, wind vector, storm foam bias|V7
T7|x|buoyancy: CPU spectrum mirror or readback, sample heights @ ship probe points, float+righting torque|V8
T8|~|proc ship: piece graph gen, 2 classes (galleon 3-mast per docs/ship-*.png + brigantine 2-mast), sockets, sail states, wood PBR tri-planar|V13,V18
T9|~|sail+steer: rudder, sail trim vs wind, ship kinematics on water|V2,V8
T10|x|camera: follow+orbit, deck walk optional later|-
T11|~|ropes: catenary compute + instanced render, anchor @ sockets, re-solve on mast move|V12,V13
T12|~|deck water: Mei 2-pass compute 512x192, rotation bias, wet material hook|V9
T13|~|intersection foam: depth mask in water material, advect+accum compute, flowmap noise tex|V10
T14|~|clouds: core billboards/geo → 4ch RT, depth-scaled blur compute, frustum quad + distort cubemap|V11
T15|~|sky+light: day cycle, sun/sky params, tonemap, fits painterly look|V16
T16|~|cannons: aim, fire, ball ballistic in sim, muzzle fx|V2,V3
T17|~|destruction: damage zones, piece swap holed, splinters, mast break + rope re-solve|V13,V14,V12
T18|.|flooding+sink: hole-below-waterline ingress, list/trim, sink sequence|V14,V8
T19|~|enemy AI ship: state machine, broadside logic, uses same ship systems|V15
T20|~|island: heightmap terrain, rock intersection foam, waterfall plane w/ flow foam|V10
T21|x|audio: waves, creaks, cannon, ambience (howler or WebAudio thin)|-
T22|.|polish pass: LUT/grade, vignette, foam art tex, param tuning vs talk footage|V17
T23|.|perf pass: budget audit, pass timings vs V17, resolution scaling knob|V17
T24|.|ocean acceptance: side-by-side capture vs reference PNGs+footage, iterate until user signoff|V20
T25|~|game UI: pause menu (Esc), settings screens (graphics res-scale/quality, audio volumes), HUD frame — frontend-design skill, pirate AAA theme|V21,V16
T26|~|palms+vegetation: proc palm geo, TSL wind sway (trunk+frond flutter), instanced scatter API|V16
T27|~|terrain materials: stylized tri-planar rock, sand/beach w/ shore blend + sparkle, proc noise texs|V16
T28|~|water transparency: depth-tinted see-through + screen-space refraction in surface material|V24,V20
T29|~|underwater mode: submersion detect, underwater grade, waterline split + meniscus|V25
T30|~|planar water reflections: mirrored scene RT, half-res, fresnel blend in surface material|V26,V20
T40|.|boot time: 58s cold, 52s of it shader compilation (measured; GPU process died 3× during boot, reliably when 2 instances compile concurrently). Defer non-first-frame pipelines, cut material-variant count, warm in background after first paint|V17,V39
T39|.|sunset showcase: golden-hour time-of-day tuned as a hero look for capture — warm graded sky, sun low + visible, glint road on the water, warm rim on the ship, distant island silhouettes in haze. Ref: SoT sunset still. User wants to RECORD this|V43,V20,V22
T38|x|localised weather: storm cells as a world-XZ field w/ soft edges, preset strength sampled per position; sail into/out of a squall, clear sun visible on the horizon while it rains overhead|V46,V7,V2
T37|x|rain + storm atmosphere: wind-slanted rain (GPU particles/screen layers), sun-anisotropic haze, distant lit patches, deck/lens response|V47,V46,V28
T36|.|dynamic ropes: Verlet/PBD chain per rope in compute, gravity+ship accel+apparent wind, anchor pinning, distance constraints; static catenary demoted to far LOD|V42,V12,V41
T35|.|SPEC'D ONLY, POSTPONED (user): ray-traced reflections/GI as an optional quality tier above T30 planar. Eval WebGPU RT availability in three; must stay opt-in, planar = default path|V26,V17
T34|~|ship detail pass: plank geo w/ real gaps+thickness (⊥ painted lines), chamfered edges (90° arrises ⊥), mast taper/hoops rework, midship rail height, stern rails, quarterdeck stair, stern cabin windows, pennants/flags, fine fittings. METHOD: hero side + full shot side-by-side vs docs/ship-full-view.png + ship-reference-schema.png, enumerate every missing element, then close the list. "Ultra regular everywhere" ⊥ — variation/asymmetry/wear ! present|V13,V18,V20,V22
T33|~|shoreline: island scatter into scene, seabed depth field → ocean shallows tint + buoyancy merge, wave runup/lapping foam on beach, wet-sand band|V10,V34,V30,V8
T32|~|caustics: refraction-Jacobian caustic map from live FFT surface, sun-angle projected onto hull waterline + seabed/shallows/beach|V34,V20,V17
T31|~|deck water wiring: bow sensor → splash(), stateTexture → deck material wet hook, ship-rotation tilt feed|V27,V9

T41|.|foam persistence + crest-threshold spray: decayHalfLife 0.9s gives mean visible age 1.29s vs 6-10s wave periods, so foam dies before its crest moves on and cannot trail. params/spray.ts has NO threshold param at all. Ref docs/inspo/ship/ref-broadside-sails-spray-foam.png shows veined foam trailing down wave faces + discrete spray sprites detaching above a cresting threshold|V65,V63
T42|x|foam relief wiring: channel A generated by foamPattern and never consumed. Foam composites as ALBEDO-MIX ONLY (no normal, no roughness) — the mechanical cause of the user's repeated "painted on / not 3-dimensional". Same defect class suspected on the deck planks (T44)|V65
T43|.|INTERIM per-pixel crest-foam gate (surfaceMaterial ~883-951) is a smooth gate on a smooth field = an unbroken ribbon along every crest contour, and it combines by `max` so it OVERRIDES the torn foam silhouette where it fires|V65
T44|x|deck planks read as painted-on flat wood despite deckHeightfield's 411 lines and a per-board `plank` height channel (§T.34 was the same complaint). Diagnose BEFORE building: does the height reach a NORMAL, or only albedo? Ref docs/inspo/ship/ref-deck-planks-railings.jpeg — the occlusion BETWEEN planks does most of the 3D read|V65,V48
T45|~|railings all round the ship except two staircase gaps port/starboard of the wheel. LANDED, ⊥ user-verified (no browser slot). Was ⊥ "missing rails" but rails IN THE WRONG PLACE: 7.5m of each 28m main run drawn INSIDE the solid castle blocks + through both bulkheads, 4.4m of each quarterdeck run inside the cabin, 13 stations REGARDLESS of run length (2.154m spacing vs ref 0.2-0.3m, 7-10x too coarse, every member an axis-aligned box), NOTHING anywhere cut a gap, BOTH ladders on starboard, neither flanking the wheel, and the aft one climbing INTO the sterncastle block. Fixes: `pieceGeometryRail.ts` = one run builder for curved+straight (turned lathe balusters at a SPACING ⊥ a count, 2-board chamfered cap, sill, newels at every span end, solid waist boarding); runs span the OPEN WAIST only w/ railLengthFactor demoted to an END CAP for the castle-less brigantine; `companionwayStations()` in blueprintParts = ONE source both the ladders and the rail gaps derive from (§V.37), offset CLAMPED to the deck it lands on ∵ a fixed 2.2m overhangs the narrower forecastle. Waist panel 0.52m closes the 0.72m band of open air the deck-water solver already believed was a 0.95m wall. Masts/yards: `pieceGeometrySpar.ts` — hoops at a spacing, woolding turns, partner collar, 16-sided, yards tapered to BOTH ends w/ slings (were bare cylinders, §T.34's oldest open item)|V37,V66,V62,V2
T46|x|starfield + night clouds: ZERO matches for stars in src/sky, no night handling in src/clouds. The SoT night refs (docs/inspo/night/) are a dense starfield over a blue→purple→teal gradient with clouds still lit pale grey. Largest single gap to reference. Procedural only — the one spare sampler is contested|V48,V40
T47|.|lantern distribution: 4 forward + stern group per docs/inspo/ship/ref-deck-planks-railings.jpeg. DECIDE which are real PointLights vs faked glow+emissive — three folds the light set into every material's cache key, so one scene.add(light) recompiles the ocean|V17,V40
T48|x|**§V22 SIGNOFF TAKEN** (main thread, isolated snapshot on its own port): with the game running, `__params.ropes.colorHex` → red and `nearWidthPx`/`farWidthPx` ×4 changed the drawn rig LIVE, no reload — and ratlines and shrouds followed too, which is the part a unit test could not have told us ∵ they are separate materials seeded from the same params. Restored after. T36 rope flip. **DEAD UNIFORMS DONE, COUNT WAS WRONG IN BOTH DIRECTIONS THAT MATTER**: `src/ropes/` holds **31** `uniform(...)`, of which **5** were live (`uRopeCount` written by index.ts, `uDeltaTime`/`uTime`/`uWind` via `.onFrameUpdate`, `uCameraPos` ∵ its Vector3 is MUTATED in place by onBeforeRender ⟹ the copy IS the binding) and **26** frozen — ⊥ eleven. Measured on HEAD: `ropeParams.gravity = 3.21` ⟹ uniform still 9.81; `simDistance = 45` ⟹ still 120. ⊥ ONE was deletable: every one of the 26 is READ in its shader graph and every param it mirrors still exists, so the fix is 25 wired + 1 declared static (`uMinRung` = `MIN_RUNG`, the ship's ratline-plan const, ⊥ a param — commented so it is ⊥ re-flagged). CURE = `refresh()` per module (ropeCompute/ropeMesh/ratlineMesh/blocks), aggregated as `ropes.update()`, called per frame in main.ts before each `renderer.compute` — the foam/caustics/palm convention (`refreshCausticsUniforms`), ⊥ `.onFrameUpdate`, ∵ a plain call is testable headless. Colours mutate IN PLACE (`u.value.set(hex)` = `setHex(hex, SRGBColorSpace)`, §V31) — reassigning `.value` a fresh Color is the §V29 binding-drop shape. Scalars go through `pushParam`, which DROPS non-finite (§V28: these scale geometry, NaN vertex = §B5 wedge). Cost nil: three's UniformsGroup `updateNumber`/`updateColor` compare before writing & a uniform write ⊥ recompiles ⟹ no change-gate needed. TRIPWIRE `tests/ropeUniforms.test.ts` (6 tests, all 6 FAIL on HEAD), incl. a derived check that ∀ `createRopes(` handle in main.ts has an `<h>.update()`. `params/ropes.ts` header now states what is live vs startup-only vs build-time vs fallback-only — it claimed "everything else is live via uniforms" while 26 were frozen. ⊥ BROWSER-VERIFIED (§V22). STILL OPEN: latent first-frame whip at ropeCompute.ts:324-346 (leash/write loop outside the simWeight>0 branch sets the seeded flag while simPrev stays at buffer-zero, and the leash parks ropes just under teleportDistance so no reseed fires — first Verlet step reads vel = cur-0); GPU gust term is a sin() with no CPU mirror and no test|V62,V45
T49|.|manual sail bracing: docs/transcript-learnings.md:80 records the talk saying players adjust sail HEIGHT and sail ROTATION; §I exposes only Q/E trim and the brace is fully automatic. Gameplay gap, needs a user decision|-
T50|x|sparkle screen-density: cellSize ∝ pixWorld makes sparkle count per screen area CONSTANT at every distance. Physically it should rise with distance until it merges into sheen — constant density is why it reads as a stipple layer laid OVER the sea rather than glints ON it. Art call, needs a user decision|V64
T51|~|OCEAN SHOALING — LANDED as §V.72, ⊥ BROWSER-VERIFIED (§V22, no slot; the §T.52 lagoon is the venue and calm+swell are the framings to judge). WAS: displacement is depth-BLIND. The seabed field (`src/island/seabed.ts`) drives COLOUR ONLY — `seabedShallowFactorNode` → the §V24 tint mix — and NOTHING anywhere scales displacement by depth, so the FFT surface swings its full open-ocean amplitude over a 1 m floor exactly as over 45 m. MEASURED deepest trough, 60 s × a 320 m patch through the CpuOcean mirror: calm −1.20 m, swell (shipped default) −3.96 m, storm −18.3 m. ⇒ ANY seabed shallower than the trough is EXPOSED: the water mesh drops BELOW the sand, loses the depth test, and the shoreline flickers dry once per wave. This is why shore break cannot read right ANYWHERE, ⊥ only at the §T.52 lagoon — and it is what forced that lagoon's floor to 4.5 m instead of the 1–3 m asked for, i.e. the venue was sized around the defect. FIX = depth-attenuated displacement in the ocean VERTEX stage + THE MATCHING CPU MIRROR (`src/sea-physics/cpuOcean.ts`). The mirror is ⊥ optional: it is what buoyancy floats on, so a one-sided change puts the hull and the drawn sea on DIFFERENT SEAS — §V.8's exact failure, and §B.34 is what that costs when the two drift by metres. TWO PREMISES HERE WERE RESOLVED THE OTHER WAY, recorded ∵ both look right: (a) "shoaling RAISES amplitude before it breaks ⟹ ⊥ a plain multiply-down" — TRUE of the ocean and DELIBERATELY ⊥ MODELLED (§V.72): growth near shore fights `shoreRunup.ts`, which already owns the beach, and the user asked for CALMING; what replaces the growth term is depth-limited BREAKING, which is the part of that physics the shore actually needs; (b) the fix is ⊥ "attenuate displacement" alone — the JACOBIAN and the NORMALS carry the same gain or the foam gate whitecaps a flat lagoon and the shading disagrees w/ the geometry. LANDED: `src/ocean/shoaling.ts` (CPU+TSL transliteration pair), `spectralMeanWavenumber`, per-cascade `OceanCascade.meanWavenumber`, vertex+fragment in `surfaceMaterial.ts`, `CpuOcean.setSeabed` + `shoaledSum`, 3 params in params/ocean.ts, seabed texture MIPPED + `seabedHeightLodNode` (§V.48), ledger vertex 3/3→4/4, `tests/shoaling.test.ts` (19). OPEN, ⊥ built ON PURPOSE: SHORE-BREAK WHITEWATER. Shoaling removes wave energy and the honest fate is foam, but the ocean material's foam block FORBIDS a second foam source by name (a smooth gate on a smooth field = an unbroken ribbon — here following a DEPTH CONTOUR, i.e. a painted ring round every island). It belongs in `src/foam`, where the accumulator + fold metric + breakup field already turn an injection rate into broken caps; the seabed depth would have to reach that compute pass. Until then the shoaled sea simply sheds LESS foam near shore|V8,V24,V4,V61,V72
T52|~|SHOWCASE LAGOON + dev jump: a hand-placed 260 m `lagoon` island at (900, 150) — off the starboard bow at spawn — taking ONE of the 5 scatter slots, seeded into `generateIslandSites` FIRST so the rejection loop routes the others around it at every seed. Venue for the systems that had nowhere to be observed: `shoreRunup.ts` swash, §V24 shallows tint, §V34 caustics, the ship's shadow on a sea floor, sand/wet-sand/waterline. THREE things the generator could not express and now can: (a) FORCED archetype (`pickArchetype` is seed-only; lagoon comes up 23% of seeds); (b) per-island PARAM OVERRIDES (`createIsland` overrode radius alone, and `islandPeakHeights` scales peak with radius so a 260 m island was a 115 m mountain); (c) an AUTHORED BASIN — every feature merges by `smoothMax` so the field could only ADD land, and the bay was whatever the noise left (22–45k m² at 5–8 m, uncontrolled). Basin gates on the ARCHETYPE's `land`, ⊥ on the finished height: an h-gate LOOKS safer and fires NOWHERE, ∵ coast noise (±16 m at this peak) has already filled the bay with dry land. Basin CENTRE ⊥ the island origin — the `lagoon` archetype's lobes reach ±33–67° and close their own gap from the sides, measured 1–7% of bearings open / widest mouth 2–22° = a landlocked CRATER; pushed out along the least-land bearing it is a cove (worst-case mouth 136° over 5 world seeds). Basin runs BEFORE the rim envelope or it overwrites the guaranteed rim depth and leaves a 7.5 m STEP in the seabed field on the footprint boundary. JUMP = `J` cycles anchorages / `Shift+J` spawn / `?at=lagoon` / `__game.jumpTo`; teleport zeroes velocity+spin, yaw only, and DROPS buoyancy's per-location `prevHeights` (else she arrives fighting 8 m/s of surface velocity from a sea she was never in). ⊥ Tweakpane: `panel.ts` only `addBinding`s primitives, no button and no change-callback path — a knob there would be no-op thirteen (§V.62). Default spawn UNMOVED, deliberately. ⊥ BROWSER-VERIFIED (§V22)|V2,V43,V62,V54,V8

T53|.|character controller foundation: capsule + step offset + WASD/mouse per docs/character_movement_specification.md. NO character system exists today — no skeletal animation, no AnimationMixer, no glTF/FBX assets anywhere in src/. Greenfield, and Scenario C in that document own audit. deckHeightfield already solves the walkable-surface query (512x192, per-plank mm), so a full BVH may be unnecessary|V73,V8
T54|.|in-place locomotion blend: idle/walk/run weights + timeScale matching. CRITICAL DEVIATION from the source spec: speed ! be measured in the SHIP frame (V73a) or a stationary character on a moving ship animates at a sprint. The blend is standard; the input to it is not|V73,V66
T55|.|foot IK on a moving deck: raycast against the LIVE deck transform (V71), lerp the target, compensate the pelvis. The source spec delta*12 constant is tuned for static ground and needs re-deriving against measured deck angular rates|V73,V71
T56|.|crew/NPC presence: the SoT references in docs/inspo show crew working the deck, the capstan and the rigging. Distinct from T53 — an NPC needs no input handling and can be far cheaper, but shares the moving-platform problem entirely|V73
T57|~|planar reflection: establish whether it runs every frame, at what resolution, with what culling. A perf capture observed **18 passes with NO reflection pass among them** and left it unresolved; it is a full SECOND pass over the scene on a frame that is CPU-bound at 595 draws (§V79)|V79,V17
T58|.|shadows on and under water: the hull's underwater shadow is COMPLETELY SHARP (a real one softens with depth — penumbra grows with caster distance, and light already scattered by the column arrives from many directions), and the surface shadow is projected onto a FLAT PLANE, never distorted by the wave geometry it lands on (§V71). User: "it doesn't really care about what's happening on the surface of the water". Related and unbuilt: the one bullet of the shadow-split research never done — LERP to a bluer shadow colour rather than MULTIPLY, which is the "too saturated" half|V71,V74,V43
T59|.|CROSS-SYSTEM CONTRAST: wake, bow spray and crest mist are all invisible against ambient whitecaps. THREE independent agents reported it — a wake before/after needed wind dropped to 4.0 m/s to capture anything, and bow spray measures **median sprite 3.4 px at 0.053 peak alpha, additive**, with `distFade` already 0.64 ∵ `fadeNear` is 25 m and ship-attached spray is permanently inside a fade tuned for CAMERA-spawned crest mist. Needs per-pool opacity/fade overrides in `sprayPool.ts`|V43,V79
T60|.|WIND LINES read as painted into the sky, ⊥ as wind moving through. Confirmed by toggle as the source of the horizon rake. The lattice is wildly anisotropic: **13 of 49 streaks survive the gate looking downwind, 2 crosswind** — a starburst on one heading and a single line on another, inherent to a pencil about a horizontal axis. Waviness is INVISIBLE in the axis views (0.12 spacings = 0.88° of azimuth ≈ 9 px at 30° out), which defeats the "waviness is load-bearing" argument exactly where the feature is most prominent. Gust-model design sketch written (windowed bundles travelling along their own meridians, one shared accumulator, ~19% sky coverage against 100%); NOT built. Positional gusts belong on the SEA as cat's-paws, ⊥ in the sky (a plane intersection divides by `dir.y` and breaks at the horizon, which is where the bearing read lives)|V43,V55
T61|~|the SUN is not visible through Snell's window, and pre-sunset is the worst case: at tod 17.6 the sun sits at θ_water **48.28°, 0.33° inside the critical angle**, where the window is only **52.8% open** ⟹ 47% of the sun's own pixel is dark TIR ceiling. Needs `skySunTerm` exported alongside `skyDomeColor` (one line — `sunTerm` already exists as a local) and threaded through; §V.26's no-mirror-disc exclusion ! be SCOPED to the above-water reflection, ∵ the "clean circular blob" argument is about reflection off a rough interface and does ⊥ transfer to transmission through it|V78,V26
T62|.|god-ray raw-vs-fogged ordering: `godRays.ts` samples `scenePass 'output'` (the RAW scene) while `postPipeline` adds `underwater.additive` AFTER `underwater.apply` ⟹ shafts bypass the volume everything else went through, and `postPipeline`'s own header forbids exactly this. Confirmed MASKING some of the underwater darkness. Needs an RTT of the volume's output|V78
T63|~|**THE SHELL IS CUT NOW — geometry removed, ⊥ a decal.** Measured at HEAD: the breach was `outerR = max(0.3, min(sy,sz)·0.3)` = **3.588 m across, 10.25% of a 35 m hull and 11.2× the drawn ball**, sized off the PIECE'S BOUNDING BOX so every section got the same hole whatever hit it; `buildHoledVariant` merged `[base, ring, disc]` with the base loft VERBATIM, so **0 of 64 rays crossed the opening** and a centre ray hit planking TWICE; the station was AUTHORED (`zMid`, `y = −draft·0.25`) and never read the hit, opening a hole up to 5.8 m from the shot; and it was ONE hole per piece EVER, ∵ built at the hp threshold crossing, so hits 3 and 4 changed nothing. NOW: `src/ship/hullAperture.ts` replaces an integer block of the shell's parametric grid with a polar annulus, outer ring WELDED onto the coarse grid's own boundary polyline (no T-junction), inner ring on a jagged two-harmonic star, collar extruded inboard for plank thickness, interior vertices evaluating the SAME `sectionHalf`/`hullTopY` the shell is built from (§V.72). **0.800 m opening** (2.5 ball diameters, 2.29% LOA), **60/64 rays pass** against 0/64 on an intact control, up to 5 holes per section AT THE HIT, `addBreach` 0.63 ms median / 1.20 ms worst once per hit, +25,032 verts on a fully shot-up galleon (+17.4%), **draw calls unchanged**. The flat ring and disc are GONE — a flat overlay at this station measured **1.04 m off the planking at 0.72 m radius** ∵ the round bilge turns hard vertically — replaced by shards anchored on rim points with apexes re-seated on the loft and a dark cone recessed 0.55 m whose mouth also sits on the loft. `HitEvent.direction` now carries the unit velocity (it was DISCARDED inside `testHits`) and `ejectaAxis` leans the burst along its TANGENTIAL component — ⊥ the raw ball direction, which points INTO the hull and passes through zero when mixed (§V.28). AUDIO WAS ALREADY RIGHT and untouched: `elevenlabs-wood-splinter-a/b/c.mp3` are pooled and `audio.event({kind:'ballHit'})` fires PER HIT at `splinterGain·force`. **§V22 SIGNOFF TAKEN** (main thread, isolated snapshot, own port): four 0.80 m apertures placed on the starboard topsides render as RAGGED OPENINGS with splinter shards standing off the rim and a dark recess behind — ⊥ the flat disc. Valid despite the freeze below ∵ the aperture is STATIC GEOMETRY rebuilt synchronously by `addBreach` and the captured frames differ. NOTE `forceHit` carries no varied point, so six forced hits on one piece merged into ONE breach (the `r1+r2` reject) — the multi-hole path had to be exercised through `addBreach` directly. STILL OPEN: there is NO INTERIOR, only a 0.55 m cone that parallaxes; hits above the sheer CLAMP DOWN up to 0.6 m ∵ the parametric strip ends at the rail; closely-spaced hits MERGE (`addBreach` rejects within `r1+r2`) so a concentrated pounding gives one hole ⊥ a chewed patch; and **FLOODING NOW DIVERGES FROM THE VISUALS ON PURPOSE** — `recordBreach` still fires only on the threshold event while the shell is cut on EVERY hit, so visible holes ≥ flooding holes, left alone rather than change sink balance unilaterally. WAS: COMBAT HITS DO NOT REGISTER. User: "if I shoot a plank, I'd expect a hole and wood splintering and breaking and flying off, and a cracking sound." FOUR things, only the invisible one exists: the splinter burst FIRES (`combatFx`, `recordBreach` on the `holed` crossing) but reads as generic particles ⊥ wood; the breach IS recorded (the `holes` array, FIFO-capped at 32 in d270926) but whether PIECE GEOMETRY changes is unchecked; DEBRIS that flies and lands is ABSENT; and `audio.event({kind:'splinter'})` fires into NOTHING — `public/sfx` has **no wood/crack/splinter asset at all**, against 13 generated SFX for other events. The ElevenLabs key that generated those is on file|V14,V43
T64|.|RADIAL LOD TRANSITION IS A WALL. The radial law is right (rings past ~1 km are 48% of triangles at 0.12 px of radial extent, §V79) but a step change in radial density is a step change in what the surface can represent. Needs vertex MORPHING toward the parent position across a band so both levels agree exactly at the boundary. The "colour change" the user reports is probably ⊥ colour: a coarser mesh carries a different slope distribution ⟹ different normals ⟹ different specular/Fresnel — and §V.48b's fade-to-own-mean crossed discontinuously is a hard tonal edge BY CONSTRUCTION (precedent: a sparkle term whose own mean stepped 2.52× per octave of footprint, 37513f4). ! be judged UNDER MOTION — a seam invisible in a still that swims is worse|V79,V48
T65|.|HAZE STARTS TOO LATE. User: "we can bring the start of the haze a little bit earlier… the far view is great but it feels a little bit excessive." `hazeStrength` 0.9, `hazeFalloff` 0.18, plus `fogFar`. Cheap, and it independently softens T64 — but ! ⊥ be used to MASK T64; fix the seam first and say which is which|V43
T66|.|THE BLUE SUN REFLECTION, SEVENTH ATTRIBUTION. bb5b3cb fixed ONE emitter (submerged seabed, wetness applied twice ⟹ roughness 0.066 ⟹ a mirror at 920 scene-linear) and was verified OVER A SUBMERGED SHELF at tod 15.0/12.0 — peak 365→2.8, px above white 756→0. The user's remaining case is DEEP WATER ALONGSIDE THE HULL, so the fix does not reach it. Already eliminated across earlier rounds: god rays, SSS lobe, pigment floor, caustics, sparkle, foam, and the ocean surface itself. NARROWING QUESTIONS unanswered: does it stop at any depth, and does it move with the CAMERA (a specular lobe) or with the WAVES (something painted)|V74,V76
T67|.|FOAM DOES NOT FOLLOW THE KEY LIGHT AT ALL. `surfaceMaterial.ts:1720/1729` lights foam as `foamCol·(ndl·lightGain·0.6·shade + lightFloor + 0.1)` — a scalar with a **0.72 floor**, never multiplied by `uSunLightColor`. ∴ **a whitecap is 66–100% as bright at midnight as at noon**, at any sea state, independent of the moon. Same shape as fa2eef4's bug (the water's idea of "how bright is the key" was the key's HUE). A real remaining contributor to "night too bright in general" — but fixing it moves DAY framings too|V74,V72
T68|.|three r180 `Sampler.set texture()` builds a fresh `onDispose` closure and hands it to `removeEventListener`, which removes BY IDENTITY ⟹ removes nothing. **~5,600 listeners already accumulated** (1,029 on the sun shadow depth texture). LATENT only ∵ `NodeSampler.update()` early-returns on an unchanged value and nothing here ping-pongs a sampler's texture — but `addEventListener` does an O(n) `indexOf`, so it is ONE PING-PONG FROM QUADRATIC. Two sites are one edit away: `ReflectorNode` assigns every frame keyed by SOURCE CAMERA (a second camera rendering the ocean — photo mode, minimap, probe pass — turns it on), and `PassNode.toggleTexture` is a real per-frame two-object swap dormant only ∵ `getPreviousTextureNode()` has zero callers (any TRAA/motion blur switches it on). Cure belongs on OUR side; ⊥ patch `node_modules`|V76
T69|.|THE GPU TIMER OVERSTATES BY ~3.3× AND NOBODY KNOWS WHY. Confirmed live on a clean capture (`ovf 0`, `batchFrames 1`, `reads 696`): `total('render')` 81.26 ms against a 24–28 ms wall frame, 81.26/25 = 3.25×. Now established as a MULTIPLICATIVE SCALING ON THE WHOLE RENDER POOL ⊥ a per-pass artefact, which is what let 076ad93 reconcile a HUD reading of 15.79 ms against 3.3 ms of ground truth. Until explained, **per-pass HUD numbers are ORDINAL ONLY**|V76,V17
T70|.|WAVES DO ⊥ LAP AGAINST THE HULL. User: "we don't get a feeling that the waves are lapping against the boat — they're just moving through it." `intersectionMaskNode`/`worldIntersectionMaskNode` and `createWaterlineBand()` exist; establish whether they only TINT, which would be the same defect as the wake (a shading response where an interaction is wanted). What makes a hull read as IN the water: the WETTED LINE riding up and down the topsides as a wave passes, local pile-up to weather and drop to leeward, and foam keyed on RELATIVE motion (a ship stopped in a swell still has water working along her side). `hullContact` already publishes per-station immersion and burial rates — but note `cutwater.halfWidth` is **0 on 53% of ticks** ∵ the crossing is normally the stem, where a hull comes to a point (§B.56)|V71,V43
T71|.|CRINGLES: ropes attach to sails at NOTHING. User: "I'd like little O-rings in the spots where ropes are attached to the sails, so there's an actual hole with an O-ring and the sail is attached to that with a little loop." The real part is a CRINGLE — a reinforced eye worked into the boltrope at clew, reef band and buntline station, with the rope reeving THROUGH it. `sailShape.ts` already knows every attachment point (`SAIL_ANCHOR_UV`, pinned by §V.71's anchor-continuity test at max step < 0.01·drop across the whole travel), so the POSITIONS exist and only the hardware is missing. Instanced — one ring mesh, one draw for all nine sails' worth|V71,V43
T72|.|RUNNING RIGGING DOES NOT REEVE. User: "we have blocks in front of the sails and it seems like they're not properly connected and not wired up in a way it would behave on an actual sailboat — and maybe there's also too many of them." A block is a PULLEY: a rope ! enter, turn about the sheave, and leave at a different angle, with the block hanging from its own strop and ALIGNING to the resultant of the two rope tensions. Establish what the current blocks are attached to and whether any rope actually changes direction at one. Also COUNT them against a real rig and cull — a real square-rigger's blocks are few and each has a named job (sheet, tack, clew, buntline, halyard). Note §T.48: the dead rope uniforms (§V.62) were the prerequisite — DONE (26 found, 25 wired, 1 static), so this is unblocked|V62,V43
T73|~|**LANDED — the enemy is the brigantine, awaiting §V22 signoff.** `main.ts`: `enemyBlueprint = buildBrigantineBlueprint()` (was galleon). MEASURED: 2 masts/6 sails/30 m/4+4 hull guns/82 ropes/58 rungs vs galleon 3/9/35 m/4+4/122/92. THREE defects fixed on the way, ⊥ one: (1) she was built `sails:false` — `updateRig`+sail driver early-out on zero sails, so the AI would have sailed her at 0.7 trim under bare poles and nothing throws; now `sails:true`, and the per-class §T.75 brace/clearance suite passes her with canvas. (2) `createCombatArena` took the PLAYER blueprint for ids it force-hits on SHIP 1 — `mast-main`/hull ids happened to coincide (§V77: two that agree today); now takes `enemyBlueprint`. (3) red canvas per ref: `ShipAssembly.setSailTint(hex)` → `userData.sailTint` → OBJECT-group uniform `tint` in `sailDriver.ts` (same mechanism as `dropScale`) multiplied into the sail base colour ⟹ backlit transmission + lee darkening inherit it; `shipMaterialParams.npcSailTint` 0x9e2e28; ONE shared sail material, NO new pipeline (§T.40 kept). `tests/enemyBrigantine.test.ts` walks assembly→rigging→ratlines→blocks→deck field→battery→hit targets→arena on her (properties: FEWER masts, SHORTER hull, every yard has a sail — ⊥ the numbers, §V80). STILL GALLEON (reported, ⊥ fixed): `seaPhysicsParams` is ONE global hull (35 m footprint, 604 t, draft 2.0/freeboard 2.6) ⟹ she floats and pitches as a galleon — `buoyancy.ts`/`sailing/*` owned by the §T.82 agent, needs a per-class `SeaPhysicsParams` passed through `stepShipBuoyancy`'s existing `p` arg; `sharedPieceMaterials` bakes the GALLEON deck-field texture ⟹ her deck relief/wetness samples the galleon's field projected into her bounds (cosmetic at 100+ m, fixing = a second material set = pipelines, §T.40 says no); `enemySpawn.ts` `CLEAR_RADIUS 22` is the galleon half-length (conservative for a 15 m half-length, harmless); her rope instance passes no `blocks:` (pre-existing). Third hull ⊥ attempted. SIGNOFF: at boot she ! read as a two-master with dark red sails, guns in the hull ⊥ on deck, sheets clear her topsides at full brace, no white sail. NPC SHIP IS THE SAME HULL. **RE-REPORTED, and the variant IS ALREADY BUILT — this is WIRING, ⊥ construction.** User (again): "the NPC boat was still the same. Maybe we can have a third boat or two, and make sure they're different types." `brigantineParams` is registered as `ship-brigantine` in `params/ship.ts:159`, `buildBrigantineBlueprint` exists in `shipBlueprint.ts:31` (2-mast, ~30 m, guns HULL-mounted ⊥ deck-mounted), `rearMastHeight: 0` already means "no rearmast", `shipRigging.ts:188,285-288` already skips sockets a class lacks WITHOUT forking a code path (§V18), `deckHeightfield` generates per-blueprint so she gets hers free, and `camStations.ts:54` already returns null for a crow's nest she has not got. ∴ the gap is that the ENEMY is constructed from the galleon blueprint, ⊥ that no second class exists. A THIRD hull is then a params entry + a blueprint, on rails that are already proven by the second|V43,V15 User: "it would be nice if the NPC ship riding around would not be exactly the same ship but a different variant — maybe a smaller one, one mast less." Reference: `docs/inspo/ship/ref-npc-smaller-variant.png` — a two-masted brigantine, red canvas, visibly lighter than the galleon. `shipParams` is already CLASSED (`galleonParams`, registered as `ship-galleon`), so the seam exists; a second class is params + a rig tier count, not a new pipeline. Cheap silhouette variety at the range the AI actually sails at, and it also makes "which ship is that" readable in combat **PER-CLASS SEA PHYSICS LANDED (the STILL GALLEON item above).** `waterlineOf(ShipClassParams)` in `params/seaPhysics.ts` reads a class's design-waterline plan off ITS OWN loft params (stem 3/7 along the bow wedge, transom at 16.5/17.5 of the half-length, half-beam = beam/2 — the ratios that reproduce the §B22-measured galleon plan 19/−16.5/4.25 EXACTLY, §V77 one expression); the three ex-module-constants in `buoyancy.ts` are now `hullBowZ/hullSternZ/hullHalfBeam` on `SeaPhysicsParams`, station cache keyed on (slices, plan). `seaPhysicsParams` re-registered `sea-galleon` (values byte-identical); `brigantineSeaParams` = `sea-brigantine`, DERIVED ⊥ typed: plan 16.2/−14.14/3.6 (L_wl 30.34), draft 1.7 / freeboard 1.8 (`brigantineParams`), A_wp ratio 0.724 → spring 1.76e6, ∇ ratio 0.615 → mass 372 t (same Cb ∵ same `hullMath` loft family; heavier than a historical ~200 t brig ∵ her authored loft is fuller), damping ∝ √(KM) (ζ unchanged), inertia at the galleon's gyradii (pitch 2.14e7, roll 3.90e6), grounding ∝ mass; rides 0.37 m past her marks = the galleon's 0.22·T. `main.ts` passes it on ship 1's `stepShipBuoyancy`. MEASURED flat calm: settles to her own draft, trim 0, heel 0; heave period ratio = √(immersion ratio). `tests/seaPhysicsClass.test.ts` pins the properties (§V80). STILL GALLEON-GLOBAL, stated: `cpuOcean.smithDepth()` = ONE pressure-head field at the galleon's draft·scale for both hulls (spectrum-level, §B42 — she feels the sea through a 2.0 m keel, ⊥ 1.7); wake Kelvin hull = PLAYER only by design; deck-field bake + `CLEAR_RADIUS 22` as above; grounding/hullContact are player-only **DECK FITTINGS LANDED (user: "the small boat is missing the proper steering wheel, the helm and this kind of stuff").** She had `capstan:false`, no `buildFurniture`/`buildHeadWorks`/`buildSternDetail`, no lantern posts — the galleon's wheel lived in the QUARTERDECK code. Fixed on the SAME builders, §V18/§V77: `helmStation(p)` in `blueprintParts` = ONE expression for which deck the helmsman stands on (sterncastle-deck at sl2−0.7 when she has one, else MAIN DECK at −0.4·L = z −12 on her) read by the socket (`buildDeck` declares `socket-wheel` when the helm lands on the main deck, `buildCastles` when on the quarterdeck), the wheel (`buildFurniture`) and the binnacle (`buildSternDetail`) ⟹ §V37 they cannot drift; `capstanStation(p)` = `0.18·L` CLAMPED clear of the fore fiferail (naive 5.40 put her drum 0.02 m INSIDE her fore pin-rail at 6.23; galleon 6.30 unchanged, clamp never bites) and now feeds BOTH the deck socket and the piece (was two literals); `buildLanternPosts(p)` factored out of `buildCastles`, posts stand on `freeboard+sterncastleRise+cabinHeight` (= the stern-cleat taffrail expression in blueprintEnds — cabin roof on the galleon, main deck on her), `lanternPostHeight` 0→1.6 on `brigantineParams` so the lamp clears her rail; `buildHeadWorks(p,{figurehead})` takes the flag `buildBowAndTransom` already had; `buildSternDetail` skips `stern-lights` without a gallery and flies the ensign off the TRANSOM head without a cabin; grating at `mainMastZ−2.5` (galleon −2 as ever). SHE NOW CARRIES: wheel (0,1.85,−12) + wheel-disc (hub 2.78) + `socket-wheel` (−11.7), binnacle (0.95,·,−13.05), capstan z 5.28, main hatch −5.5, lantern posts ±1.50/−14.5 with `socket-lantern-port/starboard`, ensign, headrails, catheads + anchors. 49→66 pieces, EVERY kind one the galleon already draws ⟹ no material, no pipeline (§T.40). WHEEL SPINS: `setHelmAngle` keys by KIND `wheel-disc` and `main.ts` already calls `updateRig(enemyAssembly,…)` with her rudder ⟹ free. LANTERNS DO NOT LIGHT: `createLanterns` in `main.ts:517` is built ONCE for `shipAssembly` (player) and `LANTERN_SOCKETS` are resolved on it alone — her sockets are declared (data, free) but unlit; lighting her = a second `createLanterns({ids})` placed off `enemyAssembly.socketWorldPosition` per frame, 2 more point lights in the scene + 2 more flame sprites — NOT added (light count/pipeline budget is the ocean agent's call). SKIPPED: `buildDeckRails` (rails castle decks only; her waist rail already runs −12.75..12.75), figurehead (her bow declares no socket, by the existing flag). GALLEON PINNED: `pieceDigest` 94:b2123106 (id|kind|parent|position FNV) in `tests/enemyBrigantine.test.ts` — byte-identical before/after. 7 new tests, properties (§V80): deck height, aft third, clear of mast foot + rudder head, socket within 1 m of the wheel, capstan gap >0 to every pin-rail, posts on a deck she has with lamp above rail, every parent resolves, brig kinds ⊆ galleon kinds. ⊥ BROWSER-VERIFIED (§V22)|V43,V15
T74|.|**THE SAIL IS A BULGE PUSHED INTO A PLATE, ⊥ A SHEET IN TENSION.** User: "it feels like we're pushing a bulge into a concrete plate instead of the cloth stretching as a whole and really shaping the bulge FROM THE CORNERS where force is applied, and respectively the horizontal beam on the top… they should only be anchored where it makes sense, and in this case only at the corners with the ropes — that's where the tension point should be." THE PHYSICS THEY ARE DESCRIBING: a square sail is an INEXTENSIBLE MEMBRANE whose boundary is fixed only at the head (bent to the yard along its whole length) and at the two CLEWS (sheet and tack). Everything else is FREE EDGE. ∴ (a) the bulge is ⊥ authored depth — it is the surface a sheet takes when it has MORE CLOTH than the chord between its fixed points, so depth follows from excess length and pressure, ⊥ from a camber parameter; (b) the LEECHES and FOOT ! pull INWARD in catenaries between attachment points — the reference's scalloped foot is the signature, and a straight edge is the tell that the quad is fixed; (c) tension CONCENTRATES at the corners, so the cloth near a clew is flat and taut while the belly is slack — the opposite of a uniform displacement field; (d) the whole sheet moves, so filling a sail ! shorten its projected span. Current `sailShape.ts` adds camber as a DISPLACEMENT ON A FIXED QUAD, which is why the edges stay straight and the corners never move. Refs: `docs/inspo/ship/ref-sail-corner-tension.png`, `ref-sail-scalloped-foot.png`. Prerequisite for §T.71's cringles to mean anything — a rope reeving through an eye that cannot pull is decoration|V71,V43,V66
T75|.|**BRACE LIMIT: THE YARDS ALREADY CUT THROUGH THE SHROUD FAN AT THE SHIPPED 35°.** User: "they still can't freely enough rotate to be really 90 degrees to the side — I think we're limited to 45 degrees of some sorts." `braceMax` is 0.61 rad = 35°, and raising it was MEASURED AND REFUSED for two independent reasons: (a) it changes nothing about the reported symptom — luff is IDENTICAL at every heading between 35° and 45° (0.877/0.877, 0.236/0.236, 0.053/0.053, 0.013/0.013, 0.000/0.000), because `fill = max(eff(θ_hull), along(β))` and `along` never beats `eff` in the drawing range; the luff lives in `trimEfficiency`'s ±30° dead zone in `shipKinematics.ts`, ⊥ in the clamp; and (b) THE GEOMETRY GIVES NO STOP AT ANY USABLE ANGLE — measured yard-vs-shroud axis-to-axis gap falls to **0.005 m at 30°** (rear lower yard vs its first shroud) and surface-to-surface contact lands at **~21°**, so from 30° up the yardarm crosses shroud after shroud. ∴ the ORDER IS: fix the shroud fan / yard length in `blueprintRig` so a real stop exists, THEN set `braceMax` to it. Raising the clamp first would be silent no-op fifteen. Also here: the `shipRigging` galleon sheet-clearance failure (a sheet entering the hull at brace −0.61 rad) has been the one known-red test all session and is the same geometry|V62,V66
T76|.|**MANUAL BRACING ON Q/E — set a course, then work the sails.** User: "we probably want the rudder on A and D… and then the sail orientation we should be able to change with Q and E, so we can manually rotate the sail into the wind WITHOUT NEEDING TO STEER THE BOAT, because that's how it is right now, which is kinda weird. We always need to steer and then just hope to catch the wind. Would be much more interesting to set a course and then use the sails." A/D IS ALREADY the rudder (`steerLeft`/`steerRight`). The gap is that Q/E are a SECOND TRIM PAIR — `trimOutPrimary: Q` / `trimInAlternate: E` duplicate `W`/`S` — while the brace is FULLY AUTOMATIC (`rigTrim.ts:146` computes `target = braceAngle(...)` from the wind with no player input anywhere). ∴ free Q/E for brace, keep W/S for how much canvas is set. This is §T.49 with a binding: `docs/transcript-learnings.md:80` records the talk saying players adjust sail HEIGHT and sail ROTATION, and §I exposes only trim. **BLOCKED BY §T.75**: manual bracing is pointless while `braceMax` is 35° AND the geometry gives no stop — measured yard-vs-shroud contact at ~21°, so the yards already cut the fan at the shipped limit. Fix the shroud fan first, then give the player the range. Declare in `CONTROL_GROUPS`; no rebinding chassis|V62,V15
T77|~|the STEERING WHEEL spins too fast. User: "our steering wheel is moving a little bit fast." It follows `ship.rudder` through `helmTurnsLockToLock` 3.5 (`helmWheelAngle = rudder × turns × π`), which is correct for a vessel this size — 8 handles passing the eye 28 times across full travel. ∴ the wheel is faithful and THE RUDDER ITSELF slews too fast. MEASURED: `rudderRampRate` 2.5 → 0→1 in **0.40 s**, `rudderSpringRate` 3 → 1→0 in 0.33 s; 1.75 turns of wheel in 0.4 s = **~1575°/s**. And the AI helm wrote `intent.rudder` STRAIGHT into `ship.rudder` — no ramp at all, her blade snapped in one tick. CURE: ONE slew, `slewRudder(current, target, dt)` in `shipKinematics.ts`, run by the keyboard collector (held direction / 0) AND by `aiShip` (from `ship.rudder` toward her intent) — §V.77, one expression. Rates 0.3 over / 0.25 back ⟹ 0→1 **2.0 s**, 1→0 **2.5 s**, wheel **~315°/s** (~0.5 turn/s, 8 handles at a countable pace); lock-to-lock 6.7 s. A 1/60 s tap at that ramp would move 0.005 ⟹ `rudderTapStep` 0.03 (one spoke, ~19°) on a fresh PRESS only, never across midships. Wheel untouched: still `ship.rudder × turns × π`, no second smoothing (0f20b96 stands). Tests: hard-over 3..6 s, release ≥ going over, tap 0.03..0.1, AI per-tick Δ ≤ ramp, wheel == rudder same tick|V62,V77,V80 RETUNED before commit: the agent's 0.3/s (3.3 s over, forced by my own ≥3 s test band) would have read sluggish against the same evening's "rudder too stiff" report; shipped 0.5/s = **2.0 s hard over, 2.5 s back, ~315°/s of wheel (0.9 turn/s)**, test band 1.5–6 s
T78|~|WAKE ↔ BUOYANCY MIRROR — LANDED `94f79cf`, THREE THINGS STILL OPEN. `d9e8e22` wired the wake into `positionNode` ⟹ the DRAWN sea carried wake elevation and `cpuOcean` did not: a LIVE §V.8 divergence, the same "different sea from the one drawn" shape that produced four user reports at `f62e037`. Closed by `WakeElevationField` + `setWakeField()`, `heightAt` adding wake at FULL amplitude and `pressureHeadAt` at `smithDepth()`, both sampled at the GRID COORD (§V.72a) and OUTSIDE `shoaledSum` ∵ the GPU adds wake AFTER `totalDisp` — neither shoaled, fetch-limited nor breaker-clipped. Also fixed the §V.2 defect: `advance()` ran in the RENDER block on `frameDt`, so trail resolution was ∝ frame rate (the `52cd1b5` shape); new `advanceWake(dt)` in the fixed tick, placed after sailing (planar pose final, §B22) and before buoyancy — track is BYTE-IDENTICAL at 30/60/144 fps over 720 ticks. Agreement worst **0.311 m AT THE WEDGE APEX**, 0.007 ahead of the cutwater, 0.041–0.043 astern, RMS 0.004 outside the apex. Stability swept 3 seas × 3 speeds against a no-wake control on an identical trajectory: heave RMS ≤0.3%, pitch ≤+1.5%, roll ≤+3.6%, mean sitting height ≤13 mm, no second-half growth. OUTSTANDING: (a) **§V22 VISUAL SIGNOFF ON THE WAKE — none taken**, Chrome would not composite the hidden tab and the rAF pump livelocked — **AND A SECOND ATTEMPT FAILED THE SAME WAY, recorded ∵ it produced a CONVINCING FALSE POSITIVE**: the tab went `document.hidden`, **0 rAF callbacks in 2.5 s**, `state.tick` frozen at 200 and the ship not moving, while CDP captures KEPT RETURNING DIFFERING FRAMES (§V.76 exactly — the renderer still paints, the SIM does not advance). On that frozen clock `wakeHeightCpu` read **exactly 0 along and across the whole trail**, `trackLen` stuck at 1, and `advanceWake`/`setShip` called ZERO times. THAT READS EXACTLY LIKE A DEAD WAKE AND IS ⊥ EVIDENCE OF ONE — the fixed tick was not running at all — so it was retracted ⊥ filed. Nor is it rescuable in-page: `docs/agent-browser-harness.md` §1(a) says install the MessageChannel pump BEFORE the module script, and installing it AFTER is useless ∵ the loop's pending rAF is already queued against the frozen original so the chain never re-arms, and **no loop handle is exposed on `__game`** (`app` = renderer/scene/camera/controls only) to kick by hand. ∴ inject the pump ahead of boot OR expose a step handle — and §T.79's four-minute boot is what makes retrying expensive; (b) **the TSL↔CPU FORMULA TWINS ARE UNTESTED** — the "GPU side" measured was `slickFieldCpu`/`bowSlopeCpu`, so if `slickFieldNode`/`bowSlopeNode` have drifted from their CPU twins NOTHING here catches it, and that is the module's whole stated contract (§V.77); (c) the apex error is a §V.48 gap on the LATERAL feature width — `innerFade`'s full-amplitude half-width is `0.265·d` = 0.13 m at d = 0.5 m, HALF A TEXEL, carrying the full `a/k ≈ 0.30 m` on a 0.234 m grid — and unlike the vertex-spacing gate ruled out earlier this one IS mirrorable ∵ the texel belongs to the REGION, ⊥ the camera; it changes the drawn bow wave, so it is stated and left. **FINISH PASS (user, in-game after `cfd02f6`: "the bow wake displacement is a bit chunky and sharp edged — right direction mechanically").** RASTERISED the CPU field (= the GPU field, §V.8) over 120×80 m at 7/10 m/s, η + |∇η| (scratch PNGs, ⊥ browser): max |∇η| **1.48** at the cutwater against a train slope of 0.06 — FOUR hard edges, none in the cos carrier. (1) `innerFade = 1 − smoothstep(0.75·kelvin, kelvin, |y|)`, kelvin = d·tanθ: ZERO wide at the stem ⟹ the full a/k (0.5 m) on a 0.13 m sliver = (c) above, and a feather 0.088·d wide along the wedge ⟹ a 0.5 m cliff over 0.9 m at d = 10, an envelope 3–10× steeper than its wave = the "sharp edge". NOW envelope `halfBeam + kelvin` (a hull's train starts the BEAM wide; apex 12 m ahead of the stem), feather ≥ `transFeather·λ` (new param, 0.16) so the envelope gradient ≤ a·1.5/(2π·transFeather) = 1.5× the crest slope BY CONSTRUCTION, measured on a |y| rounded over beam/4 (no fold under the keel), and the train takes `envelope` WITHOUT the 0.8 m `bowClip` (which would re-cut it ahead of the stem as a 0.3 m step; the lane + divergent keep the clip). (2) **THE STATIONARY PHASE WAS NOT STATIONARY**: `kelvinPhaseCpu` = (g/v²)(d **+** |y|t)√(1+t²) while 2rt² − t + r = 0 solves the **−** sign (component aft and INWARD) ⟹ ∇φ carried (∂φ/∂t)·∇t with ∇t's √ singularity at the cusp: |∇φ|/k measured 1.36 @ r=0.25, 3.3 @ 0.33, **28 @ 0.353** — a crease along the whole wedge edge (shading too, via the divergent train) and crests bowing FORWARD. Sign fixed in both twins + the propagation vector (aft·cosθ − outward·sinθ); |∇φ| = k to 1e-8 over the wedge, so "∇η = slope exactly" is now TRUE rather than claimed; `tT` held at 1/√2 outside the wedge (was 2r, a phase ∝ r²) so the feather fades a continuation of the cusp wave. (3) eddies switched on over the foam's 3 m `sternOnset`: 0.4 m in 3 m = 0.2 slope, 2× their own face → `3·coreR` (11.5 m ≈ one shedding period), first core within 1.13× of the street. (4) bow mound chevron on |side|: C⁰ fold down the centreline ahead of the stem, 0.27 slope jump → `moundNoseCpu` √(s²+r²)−r, r = thick/2, in foam AND surface (paint stays on shape), soft sign keeps ∇η = slope. AFTER: max |∇η| **0.24–0.26** (was 1.48/0.64), at the stem (mound face 0.17 + train); wedge edge a λ/6 shoulder. RESOLUTION IS ⊥ THE LIMIT, stated not raised: texel 120/512 = **0.234 m**, `elevTexture` rgba16float 512² = 2 MiB, LinearFilter, written fresh every advect pass (no temporal comb); mesh ~1.1 m at the hull; finest feature now ≥ 3.2 m (mound) / 5 m (feather @ 7 m/s) ≫ both. COST: ~6 ALU/texel in the compute pass (2 sqrt for the soft |y|, 1 min), 1 uniform, 0 textures/draws/vertices; CPU mirror same ops. REMAINING, stated: inside d < 12 m the wedge is narrower than the beam and the LOCAL k goes k0 → 1.5k0 across it (a/k changes by ⅓ within the wedge — the dispersion relation, ⊥ an envelope), under the keel; `tests/wakeFinish.test.ts` excludes the hull footprint for that reason. Apex mirror band (a) unchanged in the ratchet (measured within it). `tests/wakeFinish.test.ts` §V.80: |∇φ| = k ∀ r both branches + aft-inward sign; train max|∇η| ≤ bound at 7/10; whole field ≤ Σ parts; no stem needle; η continuous across r = 1/(2√2); eddy onset ≤ interior + ramp; mound ∇η continuous through side = 0 and = slope. `flowfoam` wedge-clearance test re-cut to the new envelope. trim (§T.82) + wakeSea mirror unchanged-green. ⊥ BROWSER-VERIFIED (§V22): signoff should see the bow wave as ONE soft shoulder from the stem back along the V with no bright line at the 19.47° edge, transverse crests arching BACK toward the cusps (⊥ forward), no needle/flicker at the cutwater and no step in the water just abaft the transom|V8,V2,V72,V77,V48,V80
T79|~|**FIXED, AND RE-MEASURED THE SAME WAY IT WAS TAKEN: 238.3 s → 33.2 s BOOT, 230.6 s → 26.2 s COMPILE (7.2× / 8.8×).** Cause was ⊥ our shaders: three r180 has every TSL node infer its type by asking its CHILDREN (`OperatorNode.getNodeType` queries both operands, `MathNode` up to three via `getInputType`, `SplitNode`/`VarNode`/`ContextNode`/`CacheNode` all forward) and **memoises NOTHING** — and a TSL expression is a DAG, ⊥ a tree, so ONE query at the root re-walks every shared subexpression once per path reaching it. Exponential in DEPTH, ∴ ⊥ proportional to output, ∴ the 0.7 B/ms. Headless proof, `getNodeType` calls per DISTINCT node: **29,712× stock → 21× memoised** (stripped) and **18,199× → 13×** (shipped config); `Node.build` itself was 5.0× and always healthy. CURE `src/core/nodeTypeCache.ts`: prototype memo keyed on cache id + buildStage + shaderStage + subBuildFn, BYPASSED while `subBuildLayers` is non-empty, invalidated by a global epoch on every setup-stage `Node.build` (∵ it forces missing setups DURING generate). 13 real materials build byte-identical WGSL against stock three. **PER-OBJECT, ocean-surface `sync` 196,139 → 13,591 ms (14.4×) at IDENTICAL 140,411 B WGSL / 15 pipelines / 4 programs** — same output, less work — and `warm:islands` 20-23 s → 5.1 s. **CAVEAT: the BEFORE runs carried two subagents and five vite servers, the AFTER run is a single context on an idle machine, so the exact factor is confounded; the headless ratio (181× on the shipped config, uncontended both legs) is the clean one.** NOW OPEN, AND IT IS THE NEW BIGGEST BUCKET: **the 549 of 628 objects that create ZERO pipelines cost 16,256 ms of `sync` — 41% of the core scene's remaining 39,616 ms, MORE than ocean-surface's 13.6 s.** Each still builds a `RenderObject`, computes `getMaterialCacheKey()`+`getDynamicCacheKey()` and sets up bindings before `_needsRenderUpdate` returns false. SECOND: ocean-surface is STILL the largest single object at 13.6 s. THIRD, from three's source ⊥ measured: `Pipelines.programs.*` key on the WGSL SOURCE STRING, so 4 programs = 2 distinct (vertex,fragment) pairs and the other 13 pipeline keys differ ONLY in render-context state (`WebGPUBackend.getRenderCacheKey` = blend/depth/stencil/side/frontFace + sample count + colour space + colour format + depth-stencil format); `Nodes.getForRender` caches the built state by `initialCacheKey`, so a target-format-only difference costs ~0 — but `getDynamicCacheKey` hashes the SCENE'S LIGHTS and `receiveShadow`, ∴ **anything changing lights or environment forces a FULL REBUILD**, a live suspect given `warmDeferred()` adds objects one at a time AFTER the core compile. FOURTH: `postPipeline.ts:370`'s comment is STALE — it binds `scenePass.renderTarget` before `compileAsync`, so that call does ⊥ warm the canvas context as claimed. WAS: **BOOT IS FOUR MINUTES AND ONE OBJECT IS 80% OF IT.** User: "boot up is definitely taking crazy long nowadays — it's horrible, horribly slow." MEASURED IN-BROWSER, TWO CONSECUTIVE BOOTS of the same snapshot: TOTAL **238.3 s** then **243.8 s**, of which `shader compile` is **230.6 s / 236.8 s**. Everything else is small — renderer 0.02-0.06 s, scene build 5.5-6.1 s, first frame 1.5 s, `warm:islands` 20-23 s, `warm:enemy rigging` 3.0 s. **THE SECOND BOOT IS NO FASTER, so this is ⊥ a cold driver cache — it is paid EVERY TIME.** `compileProfile.ts`'s per-object ranking names it outright: **`ocean-surface` / MeshBasicNodeMaterial, `sync` 179,740 ms then 196,139 ms, 15 pipelines, 4 PROGRAMS, 140,411 B of WGSL.** ∴ ~49 s of MAIN-THREAD JS PER PROGRAM. `sync` is documented in that file as "TSL graph build + WGSL codegen + bindings" — **⊥ the driver, ⊥ Tint, ⊥ pipeline creation: it is OUR JavaScript**, which is also why nothing caches it. AND IT IS ⊥ PROPORTIONAL TO OUTPUT: the same run's `warm:enemy rigging` emits **902,626 B of WGSL in 2,880 ms** = 313 B/ms, against the ocean's 140,411 B in 196,139 ms = **0.7 B/ms — 440× worse per byte emitted**, in the same process, on the same frame budget. A 6.5× SMALLER shader taking 68× LONGER is superlinear behaviour in the node graph, ⊥ a big-shader cost, so the fix is a COMPLEXITY BUG in the build/codegen walk (memoisation, a repeated subgraph re-expanded per reference, or an O(n²) dedup), ⊥ "make the ocean shader smaller". START by profiling `surfaceMaterial.ts`'s graph build under the JS profiler and counting NODE VISITS against unique nodes. Second lead: 15 pipelines from ONE object against 4 programs — establish what the other 11 cache keys are (the post pass target, the canvas, the planar-reflection target, shadow) and whether any are compiled and then never drawn. Third: **549 of 628 profiled objects create 0 pipelines**, i.e. 87% of the warm-up walk is pure overhead. CAVEAT ON THE ABSOLUTE NUMBERS: taken with two subagents and five vite servers live, so wall-clock is inflated; the RATIOS are within-run and unaffected. **ROUND 2 (headless, no browser — the user was playing): WHERE THE 628 ARE, AND THE 549 ARE ⊥ WHAT THEY LOOKED LIKE.** Census, renderer-free (ShipAssembly/ropes/islands/lanterns/combat/rain/spray/waterline built as main.ts builds them): main-pass renderables ≈ 227 — galleon 94 Mesh (ONE per piece, 34 kinds ∴ 34 shared materials), brigantine 66, ropes 5+4 InstancedMesh, islands 45 (5 × 9), lantern bulbs 2, combat fx 4, spray+bowSpray+rain 3, waterline 1, ocean 1, cloud composite 1, cloud mirror 1. The other ~400 are the SAME objects walked again by passes that `updateBefore` opens INSIDE compileAsync: `ShadowNode.updateBefore` → `renderShadow` → `renderer.render(scene, shadow.camera)` (ShadowNode.js:570-579) and `ReflectorNode.updateBefore` → render of `frame.scene` (ReflectorNode.js:440), both still routed through `_createObjectPipeline` ∵ `_handleObjectFunction` is only restored when compileAsync returns (Renderer.js:873/959). Shadow pass = ONE override material ∴ every caster is a 0-pipeline sample; ∴ '549 zero-pipeline objects' ≈ shadow pass (~190 casters) + reflection pass (~190) + ~160 shared-material main-pass pieces. **AND THE 16,256 ms WAS DOUBLE-COUNTED:** the profiler hook is re-entrant, so the sample of the first lit object CONTAINED the entire shadow + reflection passes and the per-material sum counted them twice. FIXED `compileProfile.ts`: every counter exclusive (children subtracted), new `depth` (0 = root list, 1+ = nested pass); `tests/compileProfile.test.ts`. PER-OBJECT PATH for an already-compiled material (r180): `RenderObjects.get` chain miss on (object, material, renderContext, lightsNode) (RenderObjects.js) → `new RenderObject` computes `getDynamicCacheKey()` + `getCacheKey()` at construction (RenderObject.js:248/255; `getMaterialCacheKey` :683-783 walks every material property incl. prototype getters + geometry attribute layout + `receiveShadow`; `getDynamicCacheKey` :847-873 = `Nodes.getCacheKey(scene, lightsNode)` = hash(light ids + light.castShadow, ENVIRONMENT node, FOG node, multiview, shadowMap.enabled) (Nodes.js:395-411, LightsNode.js:116-131, memoised per `info.calls`) + `object.receiveShadow`) → `_nodes.updateBefore` (the nested passes live here) → `_geometries.updateForRender` (GPU upload once per geometry) → `_nodes.updateForRender` → `_bindings.updateForRender` → `getBindings()` CLONES the non-shared bind groups per object (NodeBuilderState.js:119-148) + `backend.createBindings` = bind-group layout + bind group + uniform buffers per object (WebGPUBindingUtils.js:245-282) → `Pipelines.getForRender`: a fresh RenderObject has `data.pipeline === undefined` ∴ ALWAYS enters the update branch (Pipelines.js:455), `getNodeBuilderState()` → `nodeBuilderCache` hit by `initialCacheKey` (Nodes.js:180-196), program maps hit by WGSL string, pipeline map hit by `_getRenderCacheKey`. MEASURED HEADLESS with three's real `RenderObject` + a stubbed `Nodes`: `getMaterialCacheKey` is 0.02-0.03 ms steady, ≤1.9 ms on first touch; 45 distinct materials = 24.6 ms first / 1.1 ms steady ∴ key hashing is ⊥ the cost. What remains per object — bind-group/buffer creation, geometry upload, nested passes — needs a device; ⊥ measurable headless. **REAL BUG FOUND BY READING, FIXED:** `warmObject` compiled each deferred object with `compileAsync(object, …)` → `sceneRef = _scene`, three's private EMPTY scene (Renderer.js:860) → dynamic key WITHOUT our fog → nodeBuilderState built under a key the real frame never asks for → islands (5.1 s), enemy rigging (3 s), spray+rain, combat fx were each REBUILT SYNCHRONOUSLY on first draw after `scene.add` (fog=true materials also got a second GPU pipeline). NOW a mirror `Scene` carrying the SAME fog/environment objects is the compile root, lights still from app.scene (`tests/warmScene.test.ts` pins: bare ≠ fogged, mirror = fogged, equal-valued Fog copy ≠ — object identity is the key). VERIFY IN-BROWSER: `app.renderer._nodes.nodeBuilderCache.size` at '[boot] background warm-up done' vs two frames later — before: +~15 (one per deferred material); after: +0; and no frame spike on each deferred add. Expected boot delta: none on the splash (it moves work that was paid AFTER the splash, as hitches, to zero); the deferred `warm:*` phases may read slightly HIGHER ∵ they now build with fog. LIGHT/ENV MUTATIONS AFTER COMPILE: none at boot — sun+hemi (lighting.ts:88-107), lanterns (main.ts:522), flash light (main.ts:846) all precede the core compile; fog set in the App ctor; `sky.sunLight.castShadow` only moves from the user's 'shadows' switch (a full rebuild is inherent to that toggle); `island.ts:313` toggles OBJECT castShadow, which is in no key. MERGE OF SHIP PIECES: NOT SAFE without a refactor — every piece carries BASIC_STATES (intact/destroyed) ∴ `setDamageState` may hide ANY piece by id; `destruction.ts` targets masts (detach subtree), hull-sections (holed → 2 materials) and whatever `hit.pieceId` names; yards/wheel-disc/sails/pennants move or read per-mesh userData; sockets need the per-piece node. Merge-by-kind (rail 9→1, cannon 8→1, gunport 6→1, channel 6→1, stairs/pin-rail 3→1 …) would take galleon 94→~60, brigantine 66→~45 = ~55 meshes × 3 passes ≈ 165 of 549 RenderObjects, ~30% of this bucket, at the price of per-piece 'destroyed' becoming a merged-geometry rebuild. Left. `postPipeline.ts:370` comment rewritten: `warmup()` warms the pass target ONLY; canvas context + post quads compile on the first `presentAsync()` inside the 'first frame' phase; warming them early needs `post._quadMesh` (private in r180) compiled against the canvas — not done blind|V39,V79,V62
T80|~|**CAMERA STATIONS ON THE NUMBER ROW + A REAL TRANSITION ONTO THE HELM.** User: "we need some camera positions — maybe set them to buttons one, two, three, four. One that just sits on a cannon and looks along the barrel. One to the stern, one on the front, one in the crow's nest. Things we can jump through, and from which we can then adjust the viewpoint as we can on the helm. Additionally, taking the helm and leaving the helm — make sure that also has a camera transition, as we have going from free-flying to follow cam, so it's not stressingly abrupt but the camera actually MOVES there, so it becomes READABLE." FOR DEVELOPMENT AND RECORDING FOOTAGE, stated — readability of the shot is the point. THE STATIONS ! BEHAVE LIKE `helm`, ⊥ LIKE `free`: the helm branch parents the eye RIGIDLY to the deck so it inherits heel/pitch/heave for free with NO smoothing (damping the position slides the observer around his own deck; damping the aim makes the horizon lag the roll). Each keeps its OWN look offset (`helmYaw`/`helmPitch` shape) so cycling back returns you to how you left it. **POSITIONS COME FROM THE SHIP, ⊥ FROM TYPED-IN NUMBERS** (§V.71): `socketWorldPosition` exists and rig+gunnery already use sockets, and the gun station's bore direction ! be READ from gunnery ⊥ re-derived (§V.77), or it is wrong the moment a gun is re-laid or the hull heels. **AND IT REVERSES A DELIBERATE DECISION**: `followCam.ts:~201` makes entering `helm` an instant cut ON PURPOSE — "'POV the captain in an instant' is the whole point, and easing a 30 m move would read as a swoop, ⊥ a cut" — while LEAVING it already glides on `p.modeSwitchTime`. The user asked for the ease-in anyway and that is theirs to call; the comment is REWRITTEN ⊥ deleted, the duration is a PARAM ⊥ a feel, and the swoop risk is real enough that the duration probably wants to scale with DISTANCE. Digit1-9 ! be checked free first (freeCam eats W/A/S/D/R/F, combatArena B/V, jump J, gunnery arrows + RMB, the panel search box stops propagation) and declared in `CONTROL_GROUPS`, whose header says bindings and docs cannot be allowed to drift|V22,V16,V62,V71,V77
T81|~|**THE ENEMY SPAWNS BEHIND AN ISLAND AND DEAD UPWIND.** User: "we should change the spawn position of the other ship, so it's not always hidden behind the other island, and we have to go against the wind to get to it." TWO defects in one sentence: she is OCCLUDED at t=0, so the fight opens with a search; and she is UPWIND, so reaching her means beating — slow in this rig, and once literally impossible (`0f20b96`: in irons at 23.6° inside a ±30° no-go). Quantify "upwind" against `trimEfficiency`'s **±30° dead zone** in `shipKinematics.ts`, ⊥ against intuition. `ENEMY_SPAWN` is a hardcoded `[190, -150]`; **replacing it with a second magic constant fixes today's seed and breaks on the next** (§V.80: pinning a DECISION ⊥ a PROPERTY). It ! be DERIVED so that for ANY seed and ANY wind she is in clear line of sight, on a bearing comfortably outside the no-go, at a range justified against the gunnery envelope (`36c5a8d`: max range 133.6 m, and a spawn 1014 m away WAS the bug), and ⊥ on an island, in the §T.52 lagoon basin, or in water under her draft. Placement ! be a PURE FUNCTION of (seed, wind, island layout) — no `Math.random()` at spawn time, ∵ replay determinism. Player spawn stays PUT (§T.52 left it alone deliberately; moving it invalidates every framing reference in the repo)|V15,V62,V80
T82|~|**SHE SAT BOW-DOWN UNDER WAY — IT WAS HER OWN WAKE, PHASED AS A PRESSURE POINT.** User: "front heavy a lot of the time, 2-4°". MEASURED headless through main.ts's tick order (sailing→ocean→wake→buoyancy, +pitch = bow UP): flat sea, furled, 30 s: **0.000°**; full drive, NO wake, run 7.3 m/s / beam 9.8 m/s: **0.000°** ⟹ thrust makes NO pitch moment (applied on velocity) and nothing drifts over 60 s ⟹ (b),(c) CLEARED — sailing now recomposes yaw∘pitch∘roll so `mem.pitch` never fires. (a) CLEARED by geometry: hull sections at y=0 unrotated, loft bottom flat at −draft bow to stern (keel slope 0.000°). WITH the §T.78 wake wired: run 7.2 m/s **+0.40°**, beam reach 10 m/s **−1.20° by the head**, same offsets riding the calm/swell means. CAUSE: `slickMath.slickFieldCpu` transverse train η = −(a/k)cosφ with φ=0 AT THE CUTWATER = a TROUGH under the stem — the moving-pressure-point picture; a hull's bow is a stagnation CREST (thin-ship: source at the entrance, sink at the run ⟹ trough at the stern). a/k = transSlope·v²/g = 0.61 m at 10 m/s (0.50 after Smith), λ/2 = 32 m vs LWL 38.5 ⟹ stem in a 0.5 m hole, stern on a 0.5 m crest = the −1.2°; at 7.2 m/s λ = 33 fits the hull and nearly cancels. FIX: +π on φ_T, CPU AND TSL twin (`slickInjection`), sin+cos together so ∇η = slope holds exactly — the drawn train shifts λ/2 as one; also closes §T.78's apex gap (mound +0.5 m 1.6 m ahead of a −0.5 m trough at the cutwater). AFTER: run **−0.39°**, beam reach (Fn 0.51, past the hump) **+1.04° by the STERN** — what a displacement hull does there; target was ≤1° by the head (a real ship under sail trims ~0.5-1°). NOT 2-4° in the sim: the measured worst was 1.2°, the rest is §V22's to confirm. 2 wake tests were pinning the OLD phase by accident, fixed to their own claims: eddy `STAYS WHERE SHED` sampled trains+street at d=40 inside the 3 m `sternOnset` ramp (street alone DOUBLES between its 2 samples) → `SP_STREET` isolation, probe at 60 m; `wakeSea` Smith fraction 0.35 held only while the peak sat on the 6.4 m mound → bound is now e^(−(g/v²)·T), the law itself. `tests/trimUnderWay.test.ts` pins the PROPERTIES (§V.80): level keel, calm pitch 0, crest under the stem ∀ speed, by-the-head <1° ∀ point of sail, by-the-stern past Fn 0.45. ⊥ BROWSER-VERIFIED (§V22)|V8,V2,V62,V77,V80
T83|~|**THE RUDDER IS TOO EFFECTIVE — she U-turns hard at speed.** User: "the steering rotation is a little bit too intense, too effective basically. We can U-turn hard at those speeds." A 35 m galleon does ⊥ pivot; her turning circle is several ship-lengths and it COSTS her speed, which is the part that makes sail handling matter. Two things to separate before touching a gain: the yaw RATE the rudder commands, and whether a turn BLEEDS way (a real hull loses a third of her speed round a hard turn, and drag ∝ drift angle is what does it). ⊥ related to §T.77, which is the WHEEL following the rudder too fast — that one is the input ramp, this one is the hull's response to the blade. Both were reported; do ⊥ fix one and claim the other — MEASURED headless (beam reach, full drive 9.8 m/s, helm hard over): steady **28.6°/s**, 180° in **8.4 s**, the whole circle **1.0 LWL** across (38 m), and the SAME 28.6°/s at every speed above 4 m/s ∵ `steer = clamp(way/rudderRefSpeed 4, …, 1)` SATURATED below cruising speed. Way-loss existed (36–69% at 180°) but it was the POINT OF SAIL changing under her (starboard circle passes through irons), ⊥ hull drag: `turnDrag` was not a thing. Sideslip existed (`l` + `keelGrip` 1.5/s, drift 14–20° at 28°/s) so the rail-car diagnosis was HALF right — the hull had lateral give but the blade had no speed law. MODEL NOW: blade moment ∝ v²·δ against damping ∝ v·r ⟹ steady r = v·δ/R, R = `rudderRefSpeed/rudderRate` = 12/0.126 = **95 m ≈ 2.5 LWL**, speed-independent; `rudderRefSpeed` sits ABOVE top speed so the clamp is never hit under sail; `yawResponse` τ = 4 s at 10 m/s and ∝ 1/way (`yawDampFloor` 0.4 keeps a swing on a stopped ship dying) so she CARRIES her swing through the eye of the wind; `turnDrag`·r²·f forward decel (r² = induced drag of a lifting body, so the swell's hunting costs 0.25%, a hard turn ~half her drag). `minSteerFactor` 0.4 KEPT as a gameplay floor below ~4.8 m/s (2.9°/s) for §B.49's exit from irons — stated: below that speed the radius closes up again; the honest fix is a yaw moment from aback canvas, not here. AFTER: steady **4.2°/s**, 180° in **48.8 s** (wearing, away from the wind) / 58.8 s (tacking, she hangs in stays), tactical diameter **5.1 LWL** (197 m), speed at 180° **71%** of entry, drift 1–6°; furled and coasting 10 s the helm over costs 21% more way than straight — the hull, ⊥ the sails. Half drive: rate 2.6°/s, radius within 20% (above the floor). Polar with helm amidships IDENTICAL to 1e-3 at 7 points of sail (pinned against the old-model knob values). TACK now takes ~34 s and she stops dead in stays — a square-rigger does, and WEARING is faster (49 s round the long way), which is the historical choice. `rollYawGain` 0.35→0.8: the slower yaw lag halved the hunting wander (0.80°→0.38° RMS), 0.8 restores ~0.7°. AI: P-helm (`rudderGain` 1.5) is BETTER damped now (ζ 0.41→0.7, 14 vs 77 rudder reversals/10 min) but 80–220 m patrol legs / 15 m arrival were INSIDE her 95 m radius — median heading error 51°, orbiting, 12% of the time in stays; `patrolLegMin/Max` 250/500, `waypointRadius` 40 ⟹ 3.3°, 4%, a waypoint every ~100 s; engage/broadside unchanged (combatWiring's 3-minute close still fires and hits). 3 old tests pinned the DECISION (0.3 rad/s, 5 s wind-down, 100 s circle) and were re-cut to the property (§V.80); 6 new in `sailing.test.ts` §T.83: diameter ≥3.5 LWL, t180 ≥45 s, v180 ≤75%, coasting side-drag, r ∝ v with R fixed, polar byte-for-byte, yaw rate 0 at rest. 2237 pass. ⊥ §T.77 (input ramp) — untouched. ⊥ BROWSER-VERIFIED (§V22)|V2,V62,V80 **RETUNED after the user sailed it: "very, very stiff now — a little stiffer was wanted, this is too extreme for a game."** `rudderRate` 0.126→0.2 (R 95→60 m), `yawResponse` 0.3→0.45 (τ 4→2.7 s), `turnDrag` 26→16, `minSteerFactor` 0.4→0.3 (floor 4.8→3.6 m/s — at the tighter rate she bled under the old floor by 90° and the ∝v property died). MEASURED: diameter **3.1 LWL** (was 5.1, before T83 1.0), 180° in **33 s** (was 49, before 8.4), exit at **65%** of entry (was 71%). Irons exit + AI patrol/engage suites green unchanged. Still awaiting §V22 feel
T84|~|**THE SAILING GIZMO — three angles on one face, and the true-wind row is gone.** User (after `5221b70` made Q/E brace the yards): "now that sail rotation and boat rotation are separated this becomes very confusing … are our sails aligned with the wind or not, which way is the boat — a miniature of boat, sail rotation, wind rotation combined into a single needle gizmo. Ideally incorporate wind strength so we can get rid of the second row." The rose drew hull-vs-wind only; hull heading, yard angle and wind are now THREE independent angles. `src/ui/windDial.ts` REWRITTEN as one 96px SVG face, bow up: (1) vane on the rim = APPARENT wind FROM, same `apparentWind()` as the flags, shaft LENGTH ∝ strength; (2) a yard bar through the mast rotated by `ShipState.brace` (fed from main.ts, the number `setRigTrim` poses the rig at), with the sail hanging aft of it, depth = trim; (3) a dashed GHOST YARD with chevron caps at `autoBrace(γ)` — the player turns Q/E until the bars lie together — and the live bar coloured+glowed by `braceDrive(β)/braceDrive(β*)`, the EXACT `braceRatio` `stepShipSailing` scales thrust by (§V.77: imported, ⊥ re-derived); (4) `is-aback` when `braceBack(β,γ) > 0` — sail flips FORWARD of the yard, wax red, caption reads "aback"; (5) no-go wedge in two tones = `deadZone` and `deadZone+deadZoneRamp` from params/sailing.ts (30°/50°), live-tracked, ⊥ the HUD's own 42° label threshold (which still drives `pointOfSail`/`stalledReason`, left alone). Colour is never the only channel: bars converge, sail flips, shaft grows (§V21). Caption beside it: apparent kt, point of sail / aback, and a small `true 21.4 kt · F6` line — so the standalone true-wind row above the plaques is REMOVED; knots|heading plate, canvas plaque and anchor button unchanged. `sailingGizmoModel()` is pure and exported; §V.80 properties in tests/ui.test.ts: ghost yard = sampled argmax of `braceDrive` within `braceMax` ∀ bearing outside the no-go (0.25° grid), efficiency 1 at β* and monotone away from it, colour class monotone in drive, aback ≡ `braceBack>0` over a bearing×brace sweep, yard rotation ≡ brace, NaN rejected on every setter (§V.28). Rendered standalone (9 states, headless static page — no WebGPU) ⊥ IN-GAME (§V22 pending)|V77,V80,V28,V21,V22
T85|~|**THE SAIL IS A MEMBRANE UNDER PRESSURE, AND FULLNESS IS THE LOAD — ⊥ a centred dome with a has-wind switch.** User (screenshot, galleon ~15 kn apparent, beam reach): "the bulge on our sail still seems very much centralized in the middle — it doesn't look like the whole fabric as a thing is stretching forward… we don't see the amount of force we're catching: it should be much fuller, fully stretched out at 20 knots and above, and as the wind power we're capturing lowers we should visually see that — not a binary has-wind / has-no-wind." MEASURED on HEAD (main course 12.17×6.3, CPU mirror `sailClothPoint`): camber of chord **11.3% at 2 kn, 14.9% at 5, 17.2% at 10, 18.4% at 20** — 61% of full in 2 kn, the binary; shape `across(u)×down(v)` + a separate leech term: peak 18.3% at v=0.45-0.5, **foot 9.3%**, leech mid 4.9%, every free edge ~0 ⟹ a dome dying before the edges. TWO CAUSES, TWO FIXES. (1) LOAD: `sailDrive` was `(1−e^(−v/8))^0.45` under a √smoothstep in `sailCamberRatio`. NOW `drive` IS the fullness: q = ½ρv²·cp, fullness = 1 − exp(−(v_app·gust·√|cp| / sailWindRef)²), sailWindRef 8→**6.43 m/s (12.5 kn)**, signed by cp; cp = max(trimEfficiency(θ_hull,TRUE), along_apparent)·**braceGain** − sailBackBillow·aback, where braceGain = 1 − authority·(1 − braceDrive(β)/braceDrive(β*)) is `stepShipSailing`'s OWN expression (`autoBrace`/`braceDrive`/`windBearing` imported, §V.77; β = the sail mesh's yaw − hull yaw, i.e. the angle `setRigTrim` draws the yards at). `sailCamberRatio` is LINEAR in drive (C1 through zero, §V.71). CURVE (camber of chord, square to the wind): **2 kn 0.5% · 5 kn 3.1% · 8 kn 7.0% · 10 kn 9.7% · 12 kn 12.1% · 15 kn 15.0% · 20 kn 17.5% · 25 kn 18.3% · 30 kn 18.5%** — ~0, slack, half at ~10, drum-tight ≥20, saturating (Δ20→25 = 0.12× Δ5→10). `sailLoadCurve` DELETED (§V.62). Beam reach 15 kn: crew's brace 0.81, half-braced 0.47, yards square **0.00** — same as her thrust. (2) SHAPE: ONE 2D function `sailSection(u,v) = (1−v²)·mix(leechOpen·4v(1−v), 1, parabola(u, lead))`, ÷ the §T.74c corner-tension field — head 0 (laced), foot a clean clew-to-clew arc (free edge, held at 2 points), leeches fly forward between yardarm and clew, vertical profile deepest LOW with curvature the whole way (⊥ §T.34's tilted ramp). MEASURED at full load: **foot centre 17.3% (peak 18.0% at u=0.4 — the draft lead — v≈0.04), mid-height 14.6%, v=0.9 4.1%, leech mid 3.7% (0.45 m)**, peak/`sailCamberRatio` = 0.977-0.979 over six aspects (18×6…12×12; `SAIL_BELLY_REF` 0.45→0.12). Vertical strips get their OWN arc coefficient `SAIL_ARC_COEFF_V = 2/3` — derived (∫½z'² of 1−v²), ⊥ fitted: with the horizontal 1.9 on them the strips at u=0.25-0.5 read **7-9% SHORT** of cut and the foot rose 1.17 m; now worst strip 2.83%, none short, foot rises 0.46 m = exactly √(6.3²−2.05²). (3) SLACK: `sailSlackFold` 0.02 of chord — two incommensurate sines across, ∝(1−v), zero at both clews by construction, ×(1−fullness) ⟹ becalmed canvas hangs in folds and the wind pulls them out; flutter ×(1−fullness) too, so a drum-tight sail is still. PER-SAIL: all of it rides `drive`/`luff`, already OBJECT-group uniforms (sailDriver.onObjectUpdate per mesh, same matrixWorld-derived β) ⟹ the brigantine fills on her own apparent wind and brace with no new per-mesh plumbing; ⊥ wind gradient aloft — the sim has none, stated. COST: same trig count per vertex as before (6 sin + 1 pow per `clothZ` eval, was 6 sin + 2 pow; `downAt` lost 2 smoothsteps), 1 new shape uniform (shared group), 0 draw calls/textures/pipelines, 0 CPU allocations. TESTS (§V.80 properties, `tests/sailMembrane.test.ts` §T.85 + re-cuts in `ship.test.ts`): monotone+saturating in kn, ~0 at 2 kn / <30% at 5 / >40% at 10 / >90% at 20; foot > mid > head, peak in the lowest quarter, monotone above it; leech mid >2% chord forward at full and 0 becalmed; peak within 10% of √(excess/COEFF); aback flips sign; braced/half/square differ by >0.1 each and reach the cloth; folds: becalmed ⊥ flat, 0 at head+clews, gone at full; flutter ripple 0 at full; NaN on every input. RE-CUT (decision→property): 'fills EARLY concavity' → linear camber law; 'deepest mid-height, 55% at foot' → deepest at foot; 'leech inset 4.9% of drop' → clew inboard + monotone; 'beam reach draws with yards SQUARE' → draws braced, ⊥ square (the sim says so too); 'double-count' → hull-into-irons with sail angle fixed; scallop bar 5%→2.5% of chord (the 9.4% was the over-shortening). 2271 pass. ⊥ BROWSER-VERIFIED (§V22): signoff should see the foot of every square sail bow forward as one arc with the clews held, leeches curling, fullness visibly dropping as she bears away onto a run (apparent wind falls) and sails going slack/folded on a furled-down or backed yard, ⊥ a flat sheet|V77,V80,V71,V28,V62,V66,V22
T86|~|**DEBRIS VANISHED OFF THE WATER IN SECONDS.** User: "the debris ending up on the ocean needs to stick around and float a little longer, it vanishes too quickly." READ, ⊥ guessed: `debris.ts` ran ONE clock — `age` kept counting after splashdown against the AIRBORNE `chunkLife` 6 s, the shrink window opened at 0.75·life = 4.5 s whether the plank was flying or floating, and `wet ≥ chunkFloatLife` set `age = life` so the plank was removed THAT FRAME with no sink. ∴ a plank that flew 1.5 s bobbed ≤4.5 s, one that flew 5 s bobbed 1 s, and `chunkFloatLife` 3 was a dead knob (§V.62 shape). NOW landing RESTARTS the clock (`age = 0`, `life = floatLife`, and the idle-skip `liveFor` is extended or the pool FREEZES at 6 s — found by the new test), so float time is independent of flight time, the shrink window at the end is the plank going UNDER (sink adds `(1 − fade)·sy`), and `chunkFloatLife` 3 → **25 s**, `chunkCount` 40 → 64 so a fight does ⊥ evict its own evidence. Pinned: two drops with flights 1.5 s apart float within 0.05 s of each other and of `floatLife`; the pool drains to zero. Awaiting §V22|V62,V80,V28
T87|~|**THE MUZZLE CLOUD WAS INVISIBLE AND THE IMPACT PUFF DID NOT READ — BOTH WERE ADDITIVE.** User (in-game): "the smoke coming out at the barrel of the cannon on shot is almost not visible at all. Maybe we're overdoing it being blown away by the wind, but we definitely need a decent puff … Impacts also still don't read — we want a little smoke cloud in addition to the debris." Every knob existed (`smokeLife 3.2 smokeSize 0.8 smokeGrowth 7 smokeSpeed 14 smokeDrag 4.5 smokeWind 0.7`, 14/shot) and none could reach it. MEASURED with a HEADLESS COVERAGE RASTERISER (scratch `smoke/raster.ts`, ⊥ browser: real `createCombatFx` → emit → 60 Hz update, 10 m/s true wind, each live sprite a disc of `size/2` at the SHADER's own falloff `discCoverage` = TSL `shape` via the shared `DISC_FALLOFF_POW`, stacked as the blender does, `1−Π(1−cover·alpha·fade)`): (a) **accumulated OCCLUSION 0.000 for the whole life of both clouds** — `smoke`/`breech`/`impactSmoke` were `alpha` 0 = ADDITIVE, tint 0x9a958c = 0.32 linear; additive peak luma 1.98 at 0.3 s in the core, ~0.3–0.5 over the rest, added to a 1–3-linear sky = nothing, and the same §T.16 defect the column and the splinter had already been cured of; (b) jet: centroid 1.2 m out at 0.3 s, 2.4 m at 1 s, cone ±17°, cloud ~2.5 m at 0.3 s — ⊥ the cause; (c) wind: TRUE wind (`state.wind`), WORLD frame — correct frame, the ship leaves the cloud behind — but at coupling 0.7 the bank was **5.7 m from the muzzle at 1 s, 12.7 m at 2 s** (7 m/s), carried off before it had billowed; (d) = (a); (e) depth: `depthTest` on, `depthWrite` off, muzzle is `muzzleForward` 1.4 m outboard of the half-beam plating (hull guns, `cannonInset` 0), disc r ≤ 0.9 m at 0.3 s ⟹ no hull clipping at the muzzle; the IMPACT spawned AT `hit.point` = the swept-sphere centre 0.4 m off the face with r 0.57–0.78 m in its first 0.3 s ⟹ hull-side half depth-clipped at grazing views; (f) ruled out by the geometry in (e); (g) no size clamp and NO soft-particle/depth fade exists in the material; (h) life ±45%: 13/20 alive at 2 s, 10/20 at 2.5 s — secondary; the real killer is the FLASH fade `(1−t)²` on a 3.2 s puff: 0.25 by half-life, <0.05 at t = 0.78. FIX AT THE CAUSE: smoke kinds are a BODY now — `smokeAlpha` 0.85 / `impactSmokeAlpha` 0.8 source-over through the existing one-draw custom blend (composite converges to tint/alpha ≈ 0.86 linear, bounded, §V.44), tints 0xdedad2 white-grey and 0xc4ac86 tan dust; `brightnessAt(t, linger)` = `(1 − t^linger)²` (linger 1 ≡ the old curve, identical code path), `smokeLinger` 2.0; `heatAt` lerps the first 0.12 s of powder smoke toward a flash tint; `smokeWind` 0.7→**0.35**; `impactStandoff` 0.5 m along the outward normal; `impactSmokeLife` 3.4→2.4, `impactSmokeSize` 0.65→0.75. AFTER (same rasteriser): muzzle core occlusion **1.0 from 0.3 s**, ≥0.1 region **6.6 m at 0.3 s / 7.9 m at 0.6 s / 8.1 m at 1 s**, still **0.91 at 2.5 s**, <0.15 at 3.9 s; drift **3.0 m at 1 s, 6.5 m at 2 s** (0.3× the air); impact **2.9 m at 0.3 s / 3.8 m at 0.6 s**, 0.64 at 2 s, <0.15 at 2.8 s, born 0.5 m outside the face. BUDGET: 0 new particles — 8 guns + 4 hits = 356 slots, + 8 splashes 644, of 1536. 3 tests in `combatSmoke.test.ts` were PINNING "smoke must stay additive" (§V.80: the decision that WAS the defect) → re-cut to flash/spark/impactFlash; `tests/combatSmokeCoverage.test.ts` pins the properties from the same math: core ≥0.7 at 0.5 s, ≥4 m, visible ≥2.5 s, drift ∈ (0.5, 0.6·wind), warm→neutral, dust outside the hull by the standoff, ≥2.5 m / opaque at 0.5 s, gone by 3.5 s, linger ≡ old at 1 and bounded, broadside budget. ⊥ BROWSER-VERIFIED (§V22): signoff should see a fat white-grey cloud hide the sea behind the gun for a second or more with a brief orange cast in its first frames, billow to ~8 m, rise and drift slowly to leeward instead of streaming away; a tan dust puff standing off the planking at each hit that hangs ~2 s beside the splinters; the flash and sparks unchanged; and no bloom glare off the cloud|V44,V62,V77,V80,V22
T88|~|**R0 · LOOKDEV GATE.** 3 frames (dawn 06 / noon 12 / dusk 19) from current engine at lagoon with prototype LGG grade toward doc §07 palettes (⊥ new systems yet; params only). Save `docs/raft2100/lookdev/R0/`. User signs palette direction before R1–R2 geometry. Owner: lookdev agent (only browser user in R0)|V88,V22
T89|x|**R1a · RAFT PARAMS + BLUEPRINT.** `src/params/raft.ts` (`registerParams('ship-raft')`, every dim cites ref row, `// EST` where uncertain) + `src/ship/raftBlueprint.ts` `buildRaftBlueprint(p)`: 9 logs (13.7 centre → 9.1 outer, Ø .5–.6, seeded 2–8 cm chinks, V bow, square stern, 3 centre project .6 aft), splashboards, 9 crossbeams Ø.3 @ .91, foot-rail logs, bamboo deck (fwd + stbd only), cabin 2.4×4.3 ridge 1.4 aft of mast stbd opening 1.4, thatch roof .25 overhang, bipod mast 8.8 legs 4.5 apart + platform + ladder + topsail pole, yard, main 5.5×4.6, topsail, mizzen pole+sail, flag pole, 5 guaras, stern block + thole-pins + oar 5.8, crates/jerrycans/drum/dinghy/cage (2100 set, simple boxes first). New `PieceKind`s. Sockets: `station-tiller|radio|chart|mat|sheet-p|sheet-s|halyard|guara-1..5|ladder|lookout|gangway-*`. Deterministic. Tests `tests/raft.test.ts`: §V82 properties, build-twice equal, sockets resolve, tri count < brigantine. Owner: `src/ship/raft*` + `src/params/raft.ts`|V82,V13,V18,V80,V84
T90|x|**R1b · RAFT MATERIALS.** TSL procedural, tri-planar piece-local (as `woodMaterial`): balsa (grey-brown, end-grain checks, rope grooves, weed band at waterline), bamboo (node rings, yellow-tan), woven split-bamboo (basket weave, band-limited §V48), thatch (layered strands), hemp rope (reuse ropes material), weathered plank (guaras/splashboards/block), canvas w/ sewn seams + reef points + Kon-Tiki face decal (rust red, 8 rays, SDF ⊥ texture). Binding budget ≤ deck family (§V40). Tests: material factory per new kind, binding count, face decal SDF CPU mirror symmetric. Owner: `src/ship/raftMaterials*`|V40,V48,V16,V66
T91|x|**R1c · SELECTABLE 3RD VARIANT + PREVIEW.** `preview.html?ship=raft` + stations `tiller|radio|bow|nest|cabin`; pirate `main.ts` `?ship=raft` → player blueprint = raft (ONLY pirate edit, additive, ≤ 10 lines). `tests/enemyBrigantine.test.ts`-style end-to-end walk for raft: assembly → rigging plan → ratlines(0 ok) → blocks → deck field (raft writer, T92) → hit targets. Sail membrane (§T85) drives main/topsail/mizzen unchanged. Owner: `src/ship/preview.ts` + 1 hunk main.ts|V81,V71,V77
T92|x|**R1d · RAFT DECK FIELD.** `src/ship/raftDeckField.ts`: writer → `DeckHeightfield` (same type) from raft pieces: round logs, mats, cabin floor, `solid` mask, `mask` water domain (chinks drain). Tests: heights at 3 surfaces ordered; chink ⊥ fall-through at foot r .12; solid at cabin wall, open at doorway. Owner: `src/ship/raftDeckField.ts`|V83,V73
T93|.|**R1 SYNC · RAFT LOOKDEV.** Batched §V22: `preview.html?ship=raft` × stations × tod{6,12,19,23} + side-by-side w/ museum photos (ref §8). `docs/raft2100/lookdev/R1/`. User signoff → R1 `x`. Owner: lookdev agent|V88,V22,V82
T94|x|**R2a · FP CONTROLLER (galleon first).** `src/player/`: `SimState.player` additive; capsule r .3 h 1.7/1.1; pointer lock (`camInput` has none); yaw/pitch; walk on `DeckHeightfield` in ship frame (§V73/§V85): sample height+solid, step ≤ .4, slope ≤ 40°; crouch auto by head clearance; jump small; fall off edge → swim state (ocean `heightAt`, bob, speed 1 m/s); climb-back at foot-rail socket. Camera mode `'fp'` in followCam. Pure core `playerStep.ts` ⊥ three. Tests `tests/player.test.ts`: §V85 props on synthetic rolling field; galleon deck walk bow→stern w/o clipping bulwark. Owner: `src/player/` + `src/params/player.ts` (+ 1 mode in `followCam.ts`)|V85,V73,V3,V62
T95|x|**R2b · INTERACT + STATIONS + HANDS.** `src/player/interact.ts`: reach 1.6 m, look-ray vs station sockets (§V71 live), `E` grab/release, station binds hands pose + action channel (oar angle, guara depth, sheet, tune, sleep, climb). Placeholder hands: 2 low-poly mitts, 3 poses (idle/grab/turn), attached to camera. Hotkey path behind devLayer. Tests: §V84 — ∀ `raftActions` station resolvable + fires headless; hotkeys inert w/ dev off. Owner: `src/player/`|V84,V71,V62
T96|x|**R2c · RAFT SAILING MODEL.** `src/sailing/raftKinematics.ts` pure: thrust = square-sail polar (⊥ point < 100° TWA in `true`; `accessible` widens to 70°, ×1.6 speed), leeway, guara lift by depth×position → yaw moment (fwd-down ⇒ bow LUFFS UP toward wind; aft-down ⇒ bears away — B64), oar torque (angle × speed²) with rope-limited sweep, drift when sails down. Two tuning sets in `params/raftSailing.ts` (own file — params/raft.ts owned by T89). Tests `tests/raftSailing.test.ts`: speed 3–4 kn default downwind; can't beat in true; guara fwd-down luffs up, aft-down bears away (physics: boards pin CLR, sail side-force at CE swings the free end); oar sign; NaN-safe; pluggable = both sets satisfy same props. Owner: `src/sailing/raft*`|V2,V80,V62
T97|.|**R2 SYNC · WALK REVIEW.** Batched §V22 via `preview.html?ship=raft&fp=1`: walk tiller→cabin (crouch)→door→deck edge→swim→foot-rail→ladder→lookout. GIF + stills → `docs/raft2100/lookdev/R2/`. User signoff|V88,V22,V85
T98|x|**R3a · RAFT ENTRY.** `raft.html` + `src/main-raft.ts` (≤ 300 lines: boot only; systems imported): App, sky w/ DAY CLOCK (`raftParams.dayMinutes` 24 → advances `skyParams.timeOfDay`), ocean, clouds, weather (storm presets excluded), archipelago(sierra), raft assembly + deck field + ropes, player FP, audio, post+grade, settings UI. ⊥ combat/ai/enemy. `tests/raftIsolation.test.ts` (§V81) + vite build both entries. Owner: `src/main-raft.ts`, `raft.html`, `vite.config.ts` input|V81,V88
T99|~|**R3b · SIERRA ARCHETYPES.** `src/island/sierra*`: dome (exfoliation terraces), drowned ridge (treetop line to lee), cirque (half-ring + still lagoon), 256² grid, DG-sand beach band; vegetation swap palms→pine/juniper/manzanita (reuse scatter/windSway, new geometry); granite material (polished, jointing bands); Half-Dome silhouette island flagged `unreachable`. `generateSierraSites`: 3 slice islands 500–700 m apart + start. Tests §V90 on heightmaps. Owner: `src/island/sierra*`, `src/vegetation/pine*`, `src/params/sierra.ts`|V90,V80
T100|x|**R3c · BEACHING + STEP ASHORE + TERRAIN WALK.** Raft grounds on apron (reuse `grounding.ts`), holds; player `gangway` → world-frame walk on `Island.heightAt` (slope ≤ 35°, step .4), back aboard at any edge. Push-off action at bow station. Tests: beach→walk→return round-trip headless; raft ⊥ drifts while beached. Owner: `src/player/` + `src/sailing/raft*` (shared contract: `PlayerFrame = 'raft'|'world'|'swim'`)|V85,V90
T101|x|**R3d · GRADE NODE.** `src/core/grade.ts`: LUT 32³ DataTexture + LGG uniforms, slot after tonemap (`postPipeline.ts:289`), tod-band blend (dawn/noon/dusk/night LUTs, params), bypass when off. Author LUTs in-engine: `__game.grade.bake()` from current LGG → PNG strip under `assets/lut/`. Tests §V89 CPU mirror. Owner: `src/core/grade*`, `src/params/grade.ts`|V89,V17,V16
T102|.|**R3 SYNC · WORLD LOOKDEV.** `raft.html?at=<island>&tod=<h>` × 3 islands × 4 tod + a full 24-min day timelapse capture. `docs/raft2100/lookdev/R3/`. User signoff on palette per hour + island forms|V88,V22
T103|.|**R4a · RADIO.** `src/radio/`: stations (from teachings manifest), `tuner.ts` (§V86), `radioAudio.ts` (static = filtered noise synth on ambience bus; fragments = 2-deck player on music bus, HRTF at radio socket; night pool), lock → `markers` store. Radio prop interaction (T95 station): knob = mouse-x while grabbed. Tests §V86. Owner: `src/radio/`, `src/params/radio.ts`|V86,V84,V87
T104|.|**R4b · TEACHINGS + CLUES.** `src/teachings/`: manifest (3 placeholder teachings, placeholder MP3s generated via `scripts/generate-sfx.mjs`-style tone+TTS-free beeps w/ titles), clue props (bell, cairn, speaker, plank, notebook) as merged geometry on island, proximity trigger (HRTF emitter), object pickup → text panel (DOM overlay, diegetic styled), progression in `SimState.teachings`, night callbacks gated. Tests §V87. Owner: `src/teachings/`, `assets/audio/teachings/`|V87,V3,V62
T105|.|**R4c · MARKERS + CHART + SLEEP + SCORE.** Horizon sprite in haze for locked station; chart overlay (toggle when looking at chart socket; later prop); sleep at mat → skip to dawn (optional); music situations `voyage|approach|island|night` wired (`landDistance` hook), full duck under any teaching clip. Tests: marker bearing = station bearing; sleep advances clock to 06; duck envelope. Owner: `src/ui/raft*`, `src/audio/` (playlist only)|V84,V62
T106|.|**R4 SYNC · LOOP PLAYTEST.** One full cycle ×3 islands with placeholder content, recorded. User signoff on loop feel (sail time, lock difficulty, clue spacing)|V88,V22
T107|.|**R5 · POLISH.** Final per-hour LUTs; 2100 dressing detail (jerrycans, solar panel, rocket stove, cage); sculpted hands + per-station anims; paper chart prop; real Watts clips via manifest (Foundation); score tracks. Each sub-item own task when reached|V87,V88
T108|~|**THE SUN TURNED INTO THE MOON AT SUNSET.** User: "sun turns into the moon and vice versa instead of the moon being on a different trajectory… sun barely crossing the horizon at sunset but looking like a moon already while providing sunset colours" + ⊥ render bodies/light from below the horizon through ocean/world. READ: `moonCycle.ts:103-108` `moonDirection = sunDirection(tod − 24·phase)` = sun's track time-shifted; `keyLight` swings key sun→NADIR→moon each dusk (dark-window argument, keep). FIX: own moon orbit (§V91), phase-lit disc (new moon dark), horizon gating of disc/glow/halo for both bodies with margin, measure over full 24 h × phases on CPU (no browser) that: moon disc·lit never visible within 15° of a sun at elev > −6°; no disc term > 0 below horizon margin; key .y ≤ 0 throughout handover unchanged. Tests `tests/moonOrbit.test.ts`. Owner: `src/sky/` only. Then batched §V22 at next sync|V91,V28,V44,V72,V80,V88
T109|.|**R3e · RAFT ENTRY WIRING, PART 2.** Bind T100 into `main-raft.ts`: `createTerrainSurface` (ashore walk), frames ship↔world↔swim, `raftContactPoints` + `stepRaftBeaching` replacing the galleon-draft grounding, `push-off` sink; raft `SeaPhysicsParams` (mass ~20 t waterlogged, draft = log radius, low freeboard — T98 floats the raft on galleon mass/draft, TODO in `raftFrame.ts`); T99 `generateSierraSites` replaces pirate sites when landed. Tests: entry isolation still holds; headless boot-free wiring test that the sinks are bound (§V62). Owner: `src/main-raft.ts` + `src/raft/`|V81,V85,V62,V90

## §B BUGS
id|date|cause|fix
B1|2026-08-11|sky dome brown: `a.mix(b,t)` chained TSL = mixElement(t,e1,e2) → receiver read as factor, blend inverted+washed|V23
B2|2026-08-11|ocean sparkle 88% dense not 12%: `x.step(0.12)` = step(edge=0.12,x) — edge/x order misread|V23
B3|2026-08-11|water transparency mint-wash: linearDepth normalized 0..1 treated as meters — absorption meaningless|-
B4|2026-08-11|3s global ocean pulse: sparkle twinkle shared one time phase — all cells flipped in sync|-
B5|2026-08-11|GPU wedge: spray life=0 → 0/0 NaN age → NaN-size additive quads ×4096 = fill-rate hang; also dead particles rasterized at sizeMax|V28
B6|2026-08-11|ship never pitched: sailing recompose = pure yaw∘heel, erased buoyancy pitch 60×/s; fixed: Tait-Bryan decompose preserves pitch, heel=offset, yaw single-owner|-
B7|2026-08-11|ships floated on launch-time sea: CpuOcean never rebuilt h0 on param change while GPU did → V8 divergence under weather|-
B8|2026-08-11|`.toReadOnly()` mutates shared StorageBufferNode (setAccess→`this`, no clone) → render-side view downgraded the compute's WRITE binding → ropes invisible many sessions (mesh.count=768, descs uploaded, points all-zero) + whole spray pool frozen @ buffer-zero. Silent: no error, no NaN|V29
B9|2026-08-11|sky/sun/hemi/fog all washed white-grey: `setRGB(r,g,b)` + `new THREE.Color(r,g,b)` write the LINEAR working space, so sRGB-authored params entered ~2× bright & desaturated. Fog inherited it → far water whited out too|V31
B18|2026-08-12|`params/clouds.ts` never registered w/ the params registry — used a legacy runtime `import(/* @vite-ignore */ var)` hack from before `registry.ts` existed. TWO silent victims: clouds may not appear in Tweakpane at all in a prod build, and `applyWeatherPreset` does `getParamsEntry('clouds')` and GUARD-SKIPS unregistered systems, so every cloud value in every weather preset silently never applied. 6th silent no-op|-
B19|2026-08-12|clouds had zero tonal range (read as flat soft blobs): composite is `sunColor*(R/B) + skyColor*(G/B)` — an ADDITION — and both colours were near-white (`0xfff1d4`/`0xe9eff5`) w/ R/B≈G/B≈0.9, so the sum ≈1.8 linear pre-ACES. Lit faces clipped to white AND shadow faces landed near white. `skyColor` is not "the sky", it is ambient skylight bouncing INTO the cloud, and must be clearly darker+more saturated than the sun term|-
B15|2026-08-12|hull red/black speckle from caustics (ship-only: `mode:'both'` vs terrain's `'below'`). REFLECTED sun ray has no Snell bound (refraction does: 48.6°, tan 1.135) — a wave face throws it horizontal/downward. `MIN_VERTICAL=0.05` floored the DIVISOR but not the QUOTIENT → drift pinned at 20 over 27.8% of the surface @15° sun → finite diff |∂w|=51 → detM coefficients in the hundreds, sign-flipping between neighbouring pixels = per-pixel gain oscillation. Red residue = per-channel Jerlov extinction kills blue/green fastest|V44
B16|2026-08-12|shadowed topsides read as a flat slab of sea colour: diffuse bounce fill added to `emissiveNode`. Emissive does NOT scale with albedo, so where the sun contributed nothing it became several× the residual ambient. NOT the depth/submersion logic (absorption was correctly gated on `depth.max(0)`)|V44
B14|2026-08-12|audio dead silent, no error: `attachGestureResume(engine)` called `engine.resume()` (unlocks AudioContext only) instead of the OUTER `resume()` that builds bed/ship/loader. Post-gesture `ctx.state==='running'` so `update()` passed its guard, then every layer was `bed?.update()` on a permanent null. 5th silent no-op this project|-
B25|2026-08-12|"713ms ocean regression" that never existed. Measured full frame 760.8ms / 47.4ms with ocean hidden ⟹ "ocean = 94% of frame", spawned a whole agent workstream. Tab was `visibilityState:"hidden"`. §V39 already forbade rAF fps there but was ONE LEVEL SHORT: `renderAsync()`+`onSubmittedWorkDone()` is ALSO wall-clock, and hidden-tab swapchain acquisition throttling dominates it. GPU timestamp queries on the SAME scene: 8.19ms total, ocean 7.1ms, ~119fps — nothing had regressed. Tell was present and missed: hiding the ocean moved wall clock only 28.2→22.65ms, and one baseline repeat read HIGHER than the ocean-hidden case, i.e. content-independent. Same run: `renderer.info` read 3322 calls/7.13M tris without `info.reset()` (accumulates across renders); true 367/978k. Cost: a false hypothesis (Fresnel tap on an uninitialised RT) relayed as a lead — disproved, `reflectionStrength=0` measures identical since WebGPU zero-inits resources|V39
B21|2026-08-12|"sails have no billow" (3rd sail complaint). Belly WAS computed — 3 separate causes stacked: (a) vertical profile `smoothstep(0,0.9,1−v)` = MONOTONE, zero at head, deepest at the free foot. No inflection ⟹ lower sail swings forward as one piece and shades like a tilted plane however deep the number is. Fixed: peak at v≈0.45, taper to 0 at head, ease to 55% at the free foot; (b) FLAG BUG AGAIN (cf. flagStreamRef 7 vs wind 11) — `sailWindRef` 11 against default wind 11 ⟹ press=1.0, fill≈0.9, drive≈0.9 of 1.0 in ordinary conditions, so breeze and gale drew identical canvas. Ref ! sit ABOVE the working wind; now 16 ⟹ belly 13%→83% of drop across 3–22 m/s (was ~31% flat); (c) §B.16 class — `sailAmbientLift` added `backlitColor×0.1` (near-WHITE, constant, normal-independent) into `emissiveNode`, a flat pedestal under the whole sail that compresses the bright-shoulder-to-shade contrast a belly reads by. Now tinted by the cloth's own albedo|V44
B20|2026-08-12|dense white speckle on deck AND all down the hull side, specks lying ALONG the planking lines (⊥ spray). Every procedural EDGE in the wood height field — caulk seams, butt joints, wale strakes — was unfiltered, and `reliefNormal` differentiates that field in screen space. Seam 0.044m wide inside a 0.55m plank: at ≥0.022 m/px two neighbours land on opposite ends of the groove wall, diff = full step regardless of position ⟹ random normals. The earlier mast fix gated on `fwidth(plankCoord)` vs the PERIOD, which is 12× too late here. Also found: wale band was `smoothstep(ratio±0.03, fract(y·f))` = smoothed at the top edge, RAW STEP at the fract wrap → 1-px hairline the length of the ship|V48
B13|2026-08-11|hull open to interior 2 ways: (a) transom cap sampled its curve in 10 even steps vs shell's 8 over a different range → polylines agree only at ends → seam gaping ≤0.35m up both quarters; (b) hull had NO BOTTOM — side shells stopped at the keel line leaving a 0.125m slot/side down most of the length. Containment-sweep test passed both: it proves pieces are inside the silhouette, ⊥ that the shell is closed|V37
B11|2026-08-11|"GPU wedge" that killed Chrome tab groups = ocean clipmap index buffer emitted RING-BY-RING (front-to-back, an immediate-mode early-Z win). On TBDR Apple Silicon consecutive tris span all screen tiles → tiler holds every tile list open → frame 1 never completes. Row-major: boots instantly @100.6fps. Same mesh/shader/everything|V35
B12|2026-08-11|"cow pattern" turquoise blotches: `uSssAmbient` added sun-INDEPENDENT SSS colour (green 4.15×, blue 2.78× body) through absolute `smoothstep(0.25,0.95,h)` gate. Meant top 9% of sea when written; after swell retune fired over 36%, saturated 9%. Magic metre constant that silently changed meaning|V36
B10|2026-08-11|phantom pale sphere + black void sky: dome = BackSide sphere r4200 fixed @ world origin, ship sails away → far wall crosses camera.far=5000 → clipped cone renders clear color; past 4200m camera exits dome entirely → far wall reads as one pale ball|V32
B22|2026-08-12|"movement janky, dips into the water too heavily and too easily, doesn't feel dynamic, not interacting with the water enough" = 3 stacked causes, none of them jank. (a) §V50: the 8-probe buoyancy layout aliased the swell — measured heave RAO 0.32/0.18/0.30/0.42 at λ40/50/60/70 where a 35 m hull should ride 0.8–0.95, with a SIGN INVERSION at λ40, so she answered the sea weakly and out of phase and the water went over her instead of lifting her (peak immersion 2.75–3.10 m vs 2.6 m freeboard = green water). Fixed: 18 waterplane slices × port/stbd, weighted by local half-breadth, levers b/√3, centroid-centred; RAO now 0.80–1.03 flat across the band and it costs LESS (180 heightAt/tick vs 200). (b) §V52: one damper for heave+roll+pitch → ζ_roll 0.41, dead in 1.5 cycles. Split into per-DOF scales → ζ_roll 0.11, ζ_pitch 0.41. Cutting roll damping alone then CAPSIZED her in storm (roll RMS 105°, max 180°), so roll needed its nonlinear half back: eddy damping ×(1+q·|ω|), roll only — pitch radiation damping really is linear, and boosting it took the bow back out of her. (c) §V51: sailing and buoyancy both integrated planar position → 2.000× ground speed. NOT jank: measured 1–10 Hz content is 0.01–0.1° and the sim signal is clean; the "back and forth" the user saw is the out-of-phase heave of (a)|V50,V51,V52
B23|2026-08-12|"it goes always way too straight constantly" — measured leeway 0.00° at every point of sail and 0.14° of heading change in 120 s. Nothing in sailing pushed her sideways (the lateral wind force only HEELED her) and nothing but the rudder touched yaw. Fixed: side force = drive·cot(θ/2)·leewayRatio (geometric — sail force ⊥ the canvas, so pointing up costs; 0 dead downwind) → 1°/3°/7° running/beam/close-hauled; weather helm off the WIND heel, hunting off the ROLL RATE. Rate, not angle, is load-bearing: `waveRoll` carries a DC offset because sailing re-imposes heel as an offset while buoyancy's righting moment pulls total roll back, and the two settle degrees apart — feeding that to the helm turned a wander into a steady 0.48°/s turn (58° in 120 s of dead calm). Sea's share of the yaw target hard-bounded (§V44) or a storm steers the ship|V51,V44
B33|2026-08-12|"foam caps too round, too regular, evenly spaced, gritty" (5th foam complaint). FOUR independent lattice sources, three of them in fields that existed to PREVENT lattices. (a) injection from det J — direction-free, so caps are round and one-sized, see V58; (b) `uAcrossX/uAcrossZ` a 5.2:1 diffusion tensor in ONE world direction applied ~270× over a cap's life; (c) `crestAnisoCoord` stretched the DETAIL NOISE 3:1 along that same single global axis at full screen res; (d) `capVariationNode` — THE ANTI-LATTICE FIELD WAS A LATTICE: 2-octave value noise puts its maxima near its own cells, nearest-neighbour spacing CV 0.292 vs 0.523 for a random point process, mean spacing ~25m. Grittiness was a separate §V.48 sixth occurrence: crackle 3rd octave 0.104m sharpened by smoothstep to 0.036m, sub-pixel at 60m head-on / 6m at 10× grazing, while the only guard was a camera-DISTANCE fade measured against the BASE feature starting at 108m — band-limited against the repeat instead of the sharpest feature, §V.48's own lesson verbatim. The 16× foam-tier jump (0.82m→13.1m texels) was fixed for FREE by sampling the 128² tier and deleting the 32² one: no sampler needed, −1 dispatch|V58,V48
B32|2026-08-12|all 486 ratline rungs vanished, silently. A TSL var written inside a `Loop` read as ZERO: arc length accumulated into a `float(0).toVar()` assigned in the loop body, in a VERTEX stage graph built outside any `Fn()`. A VarNode's declaration is emitted where it is first GENERATED, and the only reference was inside the loop ⟹ declaration landed in the LOOP's scope, reset every iteration, read after the loop saw 0. Total arc 0 sent both rung ends to sample 0, and both shrouds of a fan SHARE their masthead, so every rung collapsed to a point and `MIN_RUNG` correctly drew nothing. No error, no NaN — §B.8 shape. Fixed by unrolling to a pure expression tree (segments is a startup constant): no Loop, no If, no vars, stage-agnostic by construction — removes the risk class rather than containing it inside `Fn()`. PROCESS: the author flagged "vertex-stage Loop/If has no precedent here" as their top risk and shipped ahead of the browser slot; the absent precedent WAS the warning. A shader change carrying a self-identified novel risk ! block on the slot. Unit tests cannot catch this — the CPU mirror was correct; what diverged was shader-from-mirror (§V.29's readback is the only guard)|V57
B31|2026-08-12|SEVENTH silent no-op, and the widest: EVERY `addAssign` in `surfaceMaterial.ts` was dropped. The shading normal/slope graph was built at MODULE SCOPE, so `Node.prototype.assign` found no `currentStack`, logged once and returned `this` unchanged. The material still built, compiled and rendered — it simply had no turbulent sub-noise (§V.5, every `microDetail*` param) in it, EVER. Params read as tuned, tests passed, and the only signal was two `THREE.TSL: No stack defined for assign operation` lines at boot that read as noise among the other boot chatter. Contributed to the standing "the sea is too clean / too smooth" complaint while the churn term measured correct on the CPU side. Fixed by moving the build into `buildSurfaceSlope()` called from inside `colorNode`; the two console errors disappeared and the churn is visible on wave faces. Found incidentally while wiring `skyDomeColor`, not by any test|V57
B30|2026-08-12|"flags flapping with super ultra high frequency wind… like 1000 kilometre an hour" (4th flag complaint). NOT the suspected shapes — not a saturated reference (cf. flagStreamRef 7 vs wind 11) and not frame-time-vs-SIM_DT. The carrier was `time × rate` with `rate = flagWaveFreq·(0.35+strength)`: a PRODUCT where a phase needs an INTEGRAL. Gusts move `strength`, so elapsed time scaled every rate wobble — 0.93Hz → 59.5Hz at ten minutes, and negative at times so the travelling ripple reversed. Same expression found in the SAIL flutter (`sailFlutterFreq·(1+luff)` × time): 0.56Hz → 12.4Hz. Fixed by per-flag phase accumulation wrapped to [0,2π); the crack harmonic got its OWN accumulator because multiplying a wrapped value by 2.3 breaks continuity at each wrap. Found: sailShape has TWO evaluators on TWO clocks (node `time` vs `performance.now()`), so CPU/GPU flutter phase already disagrees by up to ~0.28m of rope anchor — §V.45-class, unfixed|V55
B29|2026-08-12|"we lost all transparency, can't see the ships through the surface" — and the §V.24 channel was DEAD, not merely outweighed. `surfaceMaterial.ts` took `const ownDepth = viewportLinearDepth` as this fragment's depth. It is not: three defines it as `linearDepth(viewportDepthTexture())` — the SCENE's depth at screenUV — while the docstring one line above it in ViewportDepthNode.js reads "the linear depth value of the current fragment". So ownDepth was byte-identical to `straightDepth`, `probeThickness ≡ 0` ⟹ `refractRamp ≡ 0` ⟹ `validRefraction ≡ false` ⟹ `seeThrough ≡ 0`. Refraction offset, absorption-by-thickness and the transmitFloor cap on the mirror were ALL multiplied by nothing; the water could never show a submerged hull however Fresnel was tuned. Two bindings bound and multiplied by zero. Fix: `linearDepth()` with no argument. Compounded by a real §V.49 product — Fresnel went from `pow(1-cos,5)*0.13` to `0.02+0.98*pow(...)`, i.e. 9x MORE reflection at a 20° view, not just at grazing|V40
B24|2026-08-12|grounding CATAPULTED the hull: bed reaction `spring·pen` is unbounded in penetration and penetration is unbounded in geometry, so a cliff shelf gave 1.5e8 N = 100× her weight → thrown 16 m into the air, precessed 215° while rotating hard about a horizontal axis (yaw drifts from quaternion integration even with zero yaw torque, §V33 notwithstanding), came down on a new heading and sailed away. Read as the §B.6 sails-over-the-mountain bug returning; it is not — the force balance held, she was stopped dead the whole 8 s she was in contact. Hidden until §B.22 lowered the modelled inertias (waterplane 2nd moments fell 7×, so the same bed torque span her further). Fixed: total bed reaction capped at 3× ship weight, force AND torque scaled together so the list geometry survives|V44
B27|2026-08-12|"the ship feels a little bit too light… it can kinda catch airtime over the waves". 2 causes, one scale and one structural. (a) §V53: mass 150 t where a 35 m × 8.5 m galleon displaces ~600 t. Periods had been tuned right (roll 7.1 s, pitch 4.1 s) by inflating the inertias around it, so pitch gyradius read 0.73·L and roll 1.32·B (real 0.25·L / 0.40·B) and heave bobbed at 2.30 s against the textbook 4.91 s — twice a ship's frequency, which is what "light" IS. Fixed by deriving the scale instead of choosing it: A_wp = 241.7 m² from the station model itself → K = ρg·A_wp = 2.43e6; M = K(immersion+draft)/g = 604 t; inertias re-derived to hold the same periods → gyradii 0.34·L / 0.56·B; ζ_heave held at 0.90 (NOT damped harder — that is what pinned roll in §B.22). Ride height unchanged at 0.44 m, so hullContact/wake/deckwater see the identical signal. (b) §V54: every station sat on the y=0 plane with k·max(d,0), so buoyancy VANISHED 0.44 m above equilibrium and she went ballistic at exactly −g — storm 15.4% of ticks, 1.87 s of continuous air, peak vertical accel 73.5 m/s² (7 g). Stations given the hull's body (immersion = clamp(d+draft, 0, draft+freeboard)): zero support now needs the keel clear, 2.44 m up, and she still carries 82% of her weight with the waterline plane exactly at the surface. Airtime at swell 0.0%, storm 15.4→1.9%, vertical accel RMS at swell 3.47→1.41 m/s², peak 19.2→3.4. Fallout, both re-measured not assumed: the extra forcing needed quadraticDampingRate 10→25 (storm was capsizing again) and the grounding tests' magic bed depths stopped meaning "hard aground" (she is 83% afloat on a bank that used to leave her nothing), so they are now written as the fraction of her weight the bank carries|V53,V54,V52
B28|2026-08-12|leeway (§B.23) was computed off `thrust`, not `drive`, so a hull hard aground with her forward channel fully gated was still shoved SIDEWAYS by her own sails and crabbed off the bank at 0.35 m/s. Exactly §B.6's "sails magically continue to propulse us" in the other axis, reintroduced by the module that had just fixed it. ∀ force the seabed's grip is meant to gate ! read the GATED drive, whatever axis it acts on|V51
B34|2026-08-12|the ship floated on a sea 6.90 m from the one rendered, for as long as the mirror has existed (§V.8's own claim, never checked). `MirrorCascade.sample` used a literal `-0.5` half-texel — correct for a texture read AT THE GRID'S OWN RESOLUTION, and the mirror is 64² where the GPU is 512², so the offsets differ by domain·(1/128 − 1/1024). 2.9 m at the old 420 m cascade; the swell train pushed cascade 0 to 1010 m and it became 6.90 m, a fifth of the hull's length — she was answering waves a third of a beam from where they were drawn. Measured as 58% of σ RMS divergence against a full-resolution mirror of the SAME modes; the block extraction was proven bit-exact (100% of band energy kept) and the IFFT proven exact (correlation 1.0000 at common grid points), which is what isolated it to registration. Diagnostic tell worth keeping: better interpolation made it slightly WORSE, because reconstruction error and registration error are different animals. Fixed by expressing the offset as half a GPU texel in metres (`redN/(2·resolution)`); residual 6.2%, honest reconstruction. Also found in the same pass: `extractCentralBlock` read out of bounds and returned a silently empty spectrum for redN > resolution — now throws|V61,V8
B35|2026-08-12|cpuOcean's bilinear reconstruction fell apart when cascade 0's domain went 420 → 1010 m: its 40 m band edge dropped to 2.53 samples/cycle and bilinear error reached 15.2% of σ even with the registration correct. Raising the mirror grid is the obvious fix and is unaffordable — the FFT is O(N² log N), measured 1.40 ms/tick at 64², 6.06 at 128², 23.99 at 256². Catmull-Rom instead: 6.2% of σ for 16 taps on a step that is not the bottleneck. Applied per cascade from the measured samples-per-cycle (< 6), not hardwired, so the next domain change re-decides it — cascade 1 gets 5.42 and correctly stays bilinear, which costs nothing in accuracy and half the taps|V61
B36|2026-08-12|three feel bounds went red on a sea whose resonant forcing had FALLEN 9.5% (bow-stern Δh RMS, λ 25–70 m, matched σ). Not a contradiction and not a hull change: frequency-dependent directional spread collapsed the heading ANISOTROPY of the pitch drive 1.76× → 1.21×, so the sheltered headings a long-crested sea used to offer are gone and every heading now gets the forcing. The bounds were single-heading single-seed samples (§V50) and were reading p85 of their own distribution; re-cut as heading ensembles (median for feel, ensemble max for runaway), all green with no physics change|V50
B37|2026-08-12|ζ_pitch had been held at 0.62 against a physical 0.2–0.4 for two rounds as a "lumped model has no pitch added-inertia" concession, on the UNTESTED assumption that damping the bow harder at least keeps her dry. Swept against the short-crested sea it is backwards — median green water: ζ 0.41 → 1.33%, ζ 0.61 → 2.17%, ζ 0.85 → 2.94%. A bow held rigid PLOUGHS a crest; a bow free to lift rides over it. So the physical value is ALSO the one that answers "it dips into the water too heavily", and it costs only the pitch motion the user asked for more of (RMS 4.9° → 6.0°). A tuning compromise that was never measured, kept alive by a plausible story about which direction it traded in. **CLOSED 2026-08-13 by §B.47**: the concession's own premise ("a lumped model has no pitch added-inertia") was a TODO read as a constraint. Given the added moment of inertia the model was missing, ζ_pitch falls 0.412 → 0.342 — inside the physical 0.2–0.4 — with `pitchDampingScale` NOT TOUCHED. The damper was never the wrong number; it was standing in for a mass|V53,V69
B38|2026-08-13|vertical screen-aligned striping on island AND water: a bilinear resample along X, per-column weight f(1-f) periodic in x, constant in y, times local d2I/dx2. Proven by column profiles 180px apart cross-correlating r=+0.99 at lag 0 (parallel, not radial → not god rays) and by being EXACTLY zero over the sky (0.0165/255) but ±8/255 on the cliff — bilinear reproduces a linear ramp exactly, so an ADDITIVE term cannot do that. Reproduced on the real framebuffer: present ratio 1.053→20.0px period/10.4%, 1.111→10.0px/8.7%, 1.250→5.0px/3.8%, and integer 2.000→none. s=0.50, the LOWEST quality setting, is the cleanest. Root cause is a fractional present ratio; dpr is never re-read (V62). NOT CONFIRMED as the user's occurrence — their persisted scale was 1 at dpr 1.6, and their screenshots were themselves downsized off a 3200x1713 buffer, which produces the identical signature|V62,V63
B39|2026-08-13|foam read as discs: bandLimitedFbm2 stood in for the talk's stage-3 art textures, and VALUE NOISE HAS ROUND LEVEL SETS. Same shape as V58 one stage upstream (det J is direction-free → cannot make oriented caps) arriving one stage later as SHAPE-free. Compounded by the silhouette never being touched at all|V65
B40|2026-08-13|crestElongation 2 combed the new art texture into flow-lines across the whole sea: crestAnisoCoord is rotate-then-stretch through a SPATIALLY VARYING angle field, so above 1 it is a shear whose local anisotropy far exceeds the nominal ratio. Survivable while detail was low-contrast round fbm; a worley WALL NETWORK is thin high-contrast lines and drags. The dissolve threshold samples the same space, so it combed the SILHOUETTE and partly reverted the fix in the one respect that mattered. Predates the art texture — the texture made an existing defect visible|V65
B41|2026-08-13|moon glint road rendered in daylight cream at noon brightness: surfaceMaterial:1079 multiplied road and sparkles by a hardcoded vec3(1.0,0.95,0.82) — the ONE place a key-driven term escaped uSunLightColor — while moonCycle:272-280 states the contract outright ("the colour alone sets how bright the moon's road burns"). The moon key and the swinging lanterns were ALREADY built and wired (8b60343, commit message never mentions them); night was not missing machinery, the machinery was being fed the wrong values|V62
B42|2026-08-13|test suite 92.6s with ONE test at 63.4s (69%): CpuOcean.update(t) is a pure function of TIME (reads no ship state; buoyancy/hullContact only READ heightAt — no ship→ocean coupling), yet three sites built a fresh bit-identical ocean PER HEADING and drove each through the same time sequence. hullContact's two "damping variants" built two IDENTICAL oceans because buoyancyDamping is not in mirrorSignature. Fixed by one shared mirror with ships interleaved tick-by-tick; bit-identity proven with Object.is, 0 differences across 6400 samples. The 13.9s "collect" was never transform cost (73ms) — it was a fixture executing in the describe body|V63
B43|2026-08-13|sparkles are literally squares: sparkleHash is ONE hash per CELL, thresholded, so a lit sparkle is an entire axis-aligned cell filled uniformly, and cellSize = exp2(floor(log2(pixWorld*2.6))) puts it at 1.3-2.6px. At 1.3px a 0.012-wide smoothstep on a uniform hash is effectively a per-pixel random BINARY field carried to 4.2km — so the "squares" complaint and the "distance is noisy" complaint are substantially the same term. The V48 tripwire missed it because a hash-per-cell reads as filtered to a lexical scan|V64,V48
B44|2026-08-13|god rays read as a smooth cone widening from the sun: walk += toSun*(L/N) with L=0.65 means the smear reaches 1/(1-L) = 2.86x the radius of whatever is above threshold, and with no OCCLUDER there is no shaft structure to reveal — only magnification of the aureole. The up/down asymmetry is the sea: below the sun the frame is ocean, mostly below threshold, so downward rays have nothing to smear. The march direction is NOT aspect-skewed (a radial blur toward a point is affine-invariant) but the FALLOFF is, and at 0.9 UV it is ~1 everywhere the effect is non-zero — inert, not hugging anything. Separate real bug: rtt() defaults to ClampToEdge while updateSunScreen deliberately holds sunState outside 0..1 for godRayEdgeFade 25° past the corner, so out-of-range taps return the EDGE TEXEL smeared along the ray — a bright streak with no source that appears and vanishes as the sun crosses the frame border|V48
B45|2026-08-13|latent MSAA trap: aoEnabled/dofEnabled are false today so postPipeline:136 leaves MSAA live at 4x, but switching AO ON silently drops the whole frame to samples:0. Also confirmed: MSAA resolves geometric coverage only and three's WebGPU path does no sample-rate shading, so every shading term aliases exactly as if it were off — `antialias: true` does NOTHING for the user's water noise|V62,V63
B46|2026-08-13|TAA/FXAA/SMAA are the WRONG cure for ocean specular aliasing and would destroy the detail the user asked to keep: there is no velocity buffer and the FFT surface is not reprojectable (displacement changes every frame independently of camera motion), so history reprojection smears exactly the sub-pixel structure; edge detectors blur the shading field for the same reason. Recorded because it is the obvious first answer and it is wrong|V64
B47|2026-08-13|"it gets thrown around by waves too easily, as if it's still not heavy enough… not tracking the absolute outer curve of the wave shape" + "it can flip forwards and change its momentum basically instantly… it has more inertia than that" = TWO missing terms, one in the FORCING and one in the MASS, and the previous two rounds had answered both with DAMPING. (a) §V.68: no depth attenuation existed anywhere — buoyancy read `heightAt`, so a 2 m-draft hull felt an 8 m ripple at 100% of its surface amplitude. The only thing shaped like the Smith effect was `hullFootprintBeam`, a Gaussian across the BEAM, which is flat in the direction a HEAD sea varies ⟹ the entire pitch drive was uncorrected, and its σ 3.5 m over-suppressed beam chop 10× (λ8 at 0.02 against a true 0.21) while costing 180 heightAt/tick. Fixed per-mode in the mirror's spectrum, packed into the Dz IFFT's unused imaginary half: no extra transform, buoyancy step 0.292→0.084 ms/tick/ship. (b) §V.69: pitch and roll had no added moment of inertia, so T_pitch 4.48 s sat BELOW T_heave 5.43 s — a hull that reverses angular momentum faster than it bobs. Derived from the heave coefficient + the station geometry (b²-weighted), which also let the rigid gyradii come back to 0.25·L / 0.45·B from 0.34·L / 0.56·B. Peak pitch angular accel 60→34 °/s², peak pitch 16.4°→8.1°, peak vertical accel 2.60→1.65 m/s², heave tracking 0.877→0.919 — she moves LESS violently while riding MORE of the swell, which is the signature that separates "heavy" from "damped". THREE tests were asserting the workaround rather than the ship and had to be re-cut as ratios (§V.66): a beam-chop bound of 0.5° that only held because of the Gaussian, a heave RAO floor of 0.85 measured against the SURFACE where the hull is only offered 0.87 of it, and a "one-station hull is a cork" guard that named only the length mechanism — now a 4-cell table proving length and depth reject chop INDEPENDENTLY (λ8 RAO 0.703 neither / 0.146 depth only / 0.024 length only / 0.005 both). NOT confirmed in-browser: no slot, all measurement headless (§V.63)|V68,V69,V66,V50
B48|2026-08-13|"planks shouldn't just look like flat surfaces with painted-on wood lines" + "everything is too perfectly aligned" (2nd raising of the same complaint). Deck read as painted-on flat wood DESPITE `deckHeightfield.ts` (411 lines) carrying a per-board `plank` height channel AND that height correctly reaching a NORMAL — `woodMaterial.ts:596` `reliefNormal`, a correct scale-invariant Mikkelsen surface gradient. ⊥ the albedo-only defect class of the foam/wake: that hypothesis was checked and REFUTED before filing, ∵ a false 3rd instance would have buried the real cause under a pattern that does not apply. TWO causes. (a) `crown = sin(f·π)` at `woodMaterial.ts:388` multiplies EVERY per-board term (plankLift :389, plankTilt :399, buttGroove :439), and `deckHeightfield.ts:324-337` does the same to proud/cup/butt. sin(π·0)=sin(π·1)=0 ⟹ **the height step between adjacent boards is EXACTLY 0.000 m, ∀ board, always** — a perfectly flush deck BY CONSTRUCTION. Measured max slopes: plankLift 2.0°, plankTilt 2.6°, deckField channel 1.6°, grain 2.3°; the only strong feature is the seam groove at 22.2° and it is a SYMMETRIC V, i.e. A LINE. "Flat surfaces with painted-on wood lines" is a mechanically exact description of the output. (b) NO ao anywhere on the ship + NO environment map repo-wide (`PMREM`/`scene.environment`/`envMap`/`CubeCamera` = 0 hits); lighting = 1 DirectionalLight (3.4) + 1 HemisphereLight (0.7). A HemisphereLight's irradiance on an up-facing surface is a fn of N.y ALONE, and a 2° tilt moves N.y by 0.0006 ⟹ the ambient term is CONSTANT over the whole deck: ~24% of a sunlit deck's light and **100% of a SHADOWED deck's** — and a galleon's deck is mostly under sail shadow, so where the user stands the relief normal buys NOTHING. THE SHAPE WORTH REMEMBERING: this was deliberate, documented ("CROWNED, not stepped"), and ENFORCED by a test (`tests/shipDetail.test.ts:690`, worstJump < spread·0.6). **§B.20's aliasing fix bought its safety by trading away the exact feature the user was asking for — a CORRECT band limit can be load-bearing for a defect.** Same shape, opposite direction, same day: `2e5dd70`, where a fade measured against the WRONG thing was still load-bearing for the look. Cure = `bandLimitedStep` (widen-only, period-gated amplitude, §V.70) + procedural material `aoNode` — ⊥ post AO, ∵ the post pipeline has never run|V70,V48,B20
B49|2026-08-18|blue glare 920 scene-linear (230× ACES white): WETNESS APPLIED TWICE — sandRoughnessWet 0.3 × wetRoughness 0.22 = 0.066 ⟹ submerged seabed a mirror, sun's disc landed whole. SIX attributions, five wrong (god rays, SSS lobe, pigment floor, caustics, the glint road itself)|V74
B50|2026-08-18|deck moiré: bumpScale 20 multiplies TRUE SLOPE ⟹ 2.0° plank crown → 34.4° normal swing, reconstructed from 2.5 samples/period. §V.48 gated the HEIGHT; FILTER_PIXELS=2 is calibrated for unit gain|V75
B51|2026-08-18|terrain reconstructed the sea WITHOUT shoaling: raw −1.945..+1.610 m against a drawn sea at 0.000..0.281 m. ONE sign error → four user reports (caustics on dry sand marching with the swell, no darkening when genuinely submerged, wet band 46 m inland, speckled boundary)|V77
B52|2026-08-18|she could not sail AT ALL: boot heading 23.6° off the wind = INSIDE trimEfficiency's ±30° no-go ⟹ thrust EXACTLY 0, and the rudder does nothing below steerageSpeed ⟹ no thrust → no way → no helm → no thrust, A CLOSED LOOP. 120 s of full canvas and full helm gave 0.000 kt. "Changing the wind does nothing" ∵ wind² × 0. Cure: an ABACK term (canvas flat to the wind drives her ASTERN, sternway gives the rudder bite) — how a real square-rigger escapes irons|-
B53|2026-08-18|every player shot went 34.8° into the sky: layGuns auto-lays any order with no explicit elevation AT THE NEAREST OTHER SHIP; the player's order never carried one, spawn-to-enemy is 1014 m against a measured 133.6 m max range ⟹ elevationForRange bisects out of range and returns the maxElevation clamp FOREVER|-
B54|2026-08-18|Snell's window 3× too dark at the rim: no L/n² scaling anywhere in the tree, and the window indexed by upDot ⟹ only the top 34% of a two-stop ramp, never reaching the horizon colour|V78
B55|2026-08-18|HUD overlap: FOUR elements each absolutely positioned against the frame at hand-measured offsets, tuned against a row whose height depends on its own text (58 or 84 px). Three of six pairs overlapped; true-wind sat INSIDE the compass band, 240×12 px, every state every width|-
B56|2026-08-18|bow spray collapsed to a line: sized from `cutwater.halfWidth`, and the crossing is normally THE STEM, where a hull comes to a point ⟹ halfWidth 0 on 53% of ticks, sheet 0.33 m across the beam. The 1.6 m constant was documented as a "fallback until the sampler supplies the real half-breadth" — the premise was wrong; it is a MINIMUM|V62
B57|2026-08-18|sea-state change stalled 515 ms in ONE tick (a preset transition: 16 rebuilds, 8140 ms over 4 s): SIX N² passes each re-evaluating the SAME waveSpectrum at the SAME texel, 92% of the pass, with two lgamma calls per texel inside directionalIntegral. Fused to one pass, 378→103 ms, BIT-IDENTICAL. The documented "port h0 to the GPU" fix addresses only 23% of the cost|V8,V79
B58|2026-08-18|wake wrong at every frame rate: decay used decayFactorPerFrame but the 3×3 blur ran once per frame at a FIXED weight ⟹ diffusion ∝ frame rate (60 fps diffused ~13× more per second than 4.6 fps). And variance/step ∝ texel² ⟹ ONE shared weight diffused the 2.5 m far tier **114× faster in metres** than the 0.234 m near tier. Legacy blur had **0 of 6292 legal knob cells** — every tuning failed the no-hard-cut step, invisible to tests whose CPU models have no diffusion term|V2
B59|2026-08-19|frame time "accumulated" while cruising with nothing on screen — NOT a leak (9,782 frames: geometries 256→258, textures 79→79, all 208 container lengths flat, frame time went DOWN). The ocean spectrum re-cut fires on RELATIVE motion at ~884 ms of excess main-thread work each, 34 per 805 s cruise, and 28 AT ANCHOR ∵ cells drift downwind at 0.6×wind past a stationary hull. 3ad1212's amortisation had converted a visible 204 ms hitch into a SUSTAINED TAX|-
B60|2026-08-19|the water's idea of "how bright is the key" was the key's HUE: oceanSurface copied `sunLight.color` and nothing else, so after dark the road burned at moonColor's luminance 0.390 vs a noon sun's 0.904. Two subsystems, one light, 4.53× apart — caustics' `setCausticKey` already had it right|V72,V74
B61|2026-08-19|§V.79 asserted CPU-bound, then vertex-bound. BOTH WRONG, and both were cited into agent briefs: the CPU is idle 44% of wall time, draw calls are 3.8 µs ⊥ 34, and the ENTIRE vertex stage is 0.48 ms (4.7%). It is per-primitive tiling work at a grazing horizon — proved by the sea filling MORE of the frame at nadir costing 2.26 ms against 10.20 at the horizon|V79
B62|2026-08-19|the wake↔buoyancy mirror's "sub-millimetre residual" was a HANDOFF CLAIM, ⊥ a measurement — real worst error **0.311 m at the wedge apex**, 300× the claim, ∵ `innerFade` confines the transverse train to the Kelvin wedge and its full-amplitude half-width `0.265·d` is HALF A TEXEL at d = 0.5 m while carrying the full `a/k ≈ 0.30 m`. A prose residual that no test evaluates is a GUESS THAT LOOKS LIKE A RESULT, and it survived a handoff unchallenged|V8,V76

B63|2026-08-21|moon direction = `sunDirection(tod − 24·moonPhase)` (moonCycle.ts:108) — moon on the sun's track, phase as a TIME shift ⇒ near new/old phase moon disc sits beside the setting sun, reads as "sun became moon" while palette still sunset; disc terms ⊥ horizon-gated by elevation margin|V91
B64|2026-08-21|§T.96 row stated guara sign BACKWARDS ("fwd-down ⇒ falls off"). Physics + runasimi.net + Heyerdahl p.127: boards down FORWARD pin the bow, stern slides to lee ⇒ LUFF; boards aft ⇒ bear away. Spec text written from memory ⊥ source; impl derives sign from moment = Σ lift·(pos − CE), no hand-set sign|V80
B65|2026-08-21|BIGGER cause of "sun became moon" (⊥ B63 alone): `skyBackground.ts` drew the sun disc/glow/halo along `uSunDir` = THE KEY — after handover the key IS the moon direction ⇒ 14× HDR ember disc sat on the moon every night at DEFAULT phase 0.5, travelled through nadir mid-swing. Also no disc term gated by elevation (sun disc at −7° luminance 5.2 through Snell's window). Fix: `uSunBodyDir` = true sun for the disc, `bodyHorizonGate` on both bodies, moon own orbit|V91
B66|2026-08-21|OPEN (found by T108 sweep, ⊥ fixed): nadir handover has a fast leg at quarter phases (moon .y 0.96 at dusk ⇒ 165° lerp, 0.15 rad per 0.001 h). Inside dark window, BUT `keyRadianceScale` = 1 − dirW mid-swing ⇒ ocean colour-only path can read a partially-bright key pointing at nothing. Needs own task|V91,V44
B67|2026-08-21|OPEN (T88 lookdev): far-LOD island (~1.5 km) renders SHARP while the 0.75 km lagoon is fully hazed — distant-LOD island path skips the aerial-perspective/haze node ⇒ Firewatch far-plane flattening unreachable until fixed. Also: sun sets at 18:00 sharp, 19:00 = full night ⇒ doc "dusk 19" = 17.8 in-engine; sea state ⊥ calmable from params (weather field pins wind ~8 m/s over preset 4)|V22,V88
B68|2026-08-21|NOTE (T92, ⊥ bug): `mask` channel is cut at 0.5 by BOTH `deckSurface` (walkable) and `deckwater` (drain) ⇒ a draining chink would be a hole the walker falls through. Chinks realised as height dips; water leaves over log edges/stern. True drain-through needs a separate channel. Also `buildDeckHeightfield(raft)` returns null — callers branch on ship class|V83,V85
B69|2026-08-21|NOTE (T100): gangway sockets sit at log-top (~0.3 m) while the eye is 1.6 m up ⇒ `reach` 1.6 only met crouched/on top of the socket — gangways will feel hard to focus in-game. Fix = raise gangway sockets to ~1.0 m or measure reach from feet (T109/R3 sync decides)|V84,V71
